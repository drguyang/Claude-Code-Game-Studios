# 随机事件导演 (Random Event Director) — 52

> **Status**: In Design · **已复核并修订**(2026-09-14 首轮 `MAJOR REVISION NEEDED` → 10 项 blocking + 4 项用户裁定已落盘)
> **Author**: dr_guyang + game-designer
> **Last Updated**: 2026-09-14
> **Implements Pillar**: 支柱一(判断为骨)· 支柱四(时代即关卡,危难即 Boss)· 支柱五(史实为骨)
> **上游**: 5 时间天气 · 6 世界与生态区 · 27 敌人 AI · **21a 物品库(`CarryLoad` 形状)**
> **下游(注入方)**: 27 遭遇 · **37 病例(P0 · 医疗事件模板)** · 34 公卫与疫情(P1a)· 35 时代事件(P1a)· 36 NPC 与村落(P1a)· 45 网络层(P1b)
> **横向**: 9 疾病与伤情模拟(事件落地接收点)· 23 建造(P1a 损毁)· 42 拟物 UI(预告通道)· 44 音频(预告线索)
> **参照**: 《英灵神殿》(raid events / 星级 / 夜行)·《我的世界》(流浪商人 / 不祥之兆 / 幻翼)
> **架构约束**: `docs/architecture/adr-005-deterministic-sim.md` · `adr-006-fixed-point-boundary-contract.md`(**本文件受二者约束,见 §确定性契约**)
> **上游文档**: `design/gdd/game-concept.md` · `design/gdd/systems-index.md`(§5 注)· `design/gdd/concept-benchmark.md` · `design/gdd/reviews/random-events-review-log.md`

---

## Overview

随机事件导演是**一个不生产内容、只决定内容何时降临的系统**。它持有四个轴 ——
**内容**(抽到什么)· **时机**(什么时候抽)· **强度**(来多强)· **密度**(还能来多少)。
骨架全档通用,每一档只是**往内容池里注入更多东西**。

它存在的理由是**一个缺口**:概念 L161「出诊途中可能遇敌/遇险」与
`concept-benchmark.md:46` 的「世界事件 ✅ 沿用结构」都要求有个系统拥有
**「事件的触发与强度」**,而全系统表里没有 —— 27 敌人 AI 只管**行为**(追击 / 包抄 / 逃跑)。

它同时承担本作对《英灵神殿》的**一处关键改写**:英灵神殿的进度锁是**杀怪**,
本作的进度锁是**行医**。盯上你的人不是因为你强,是因为你**有用**。

> **边界一句话**:52 决定**「要不要来、来多少、来多强」**,不决定**「来了做什么」**。

## Player Fantasy

玩家不该感觉到「有个系统在刷怪」。他该感觉到的是**这世界是活的,而且它在找你**。

三个锚点:

1. **门外有人** —— 出诊路上被拦住:「先生,救救我爹。」
   **事件的第一情绪不是危险,是「有人来了」。** 这是本作与《英灵神殿》最根本的差异 ——
   那边的世界只会来打你,这边的世界也会来求你。P0 的**机会档**就是为这一条立的。
2. **先听见,后看见** —— 狗吠、马蹄、锣声、远处一片哭喊。**预告制**让每个事件都有前奏,
   而这个前奏**正好是拟物 UI 能画出来的东西**(视觉锚点支撑原则二)。
3. **你做了什么,世界就回什么** —— 你救过一个逃犯,官兵后来上了门。
   随机事件因此不是骰子,是**因果**(规则九)。

> **反幻想**:这不是「刷怪塔」也不是「事件农场」。玩家**永远不会**看到
> 「今日事件:3 / 5」这样的计数。**事件一旦可被计算和刷取,它就不再是世界的一部分。**
>
> ⚠️ **「不可被计算」是 UI 层的否定声明,不是模拟层的约束**(2026-09-14 复核补)。
> 模拟层恰恰相反 —— ADR-005 要求它**逐位可计算、可回放**(见 `§确定性契约`)。
> 两者不冲突:数值必须确定,**呈现必须不透明**。任何把本条读成「抽取过程可以不确定」的实现都是错的。

## Detailed Design

### Core Rules

**规则一:一个导演,四个轴。**
```
时机轴 → 抽池(内容轴,按 key 过滤) → 强度轴定档 → 密度轴查预算 → 生成
```
即:`EventRoll = 抽一(ContentPool × 时机权重 × 进度锁过滤 × 历史乘子)`
**抽池在先,定档在后** —— 强度轴**不参与**抽取(见 F1 注:`强度系数` 若进乘子
会被归一化约去,且与「抽到什么」无关)。完整定义见 **F1**。

**规则二:导演不生产内容,只调度。** 内容由各系统**注入**:

| 注入方 | 注入什么 | 档 |
| ---- | ---- | ---- |
| **27 敌人 AI** | 人形遭遇(兵痞 / 山匪 / 溃兵)与野兽 | P0 |
| **37 病例系统** | **医疗事件模板**(原型疫情 / 急召出诊 / 药材短缺) | P0 |
| **34 公卫与疫情** | 疫情事件 | P1a |
| **35 时代事件** | **篇章窗口**(有脚本,非随机) | P1a |
| **36 NPC 与村落** | 求诊 · 交涉 · 流民 | P1a |
| **45 网络层** | 联机协作事件 | P1b |

**池条目 schema(52 持有的全部形状 — 不含任何内容字段):**

| 字段 | 类型 | 含义 |
| ---- | ---- | ---- |
| `key` | int | 条目主键。**整数**,非字符串(抽取结果的确定性依赖整数比较) |
| `W_base` | **int** | 基础权重(最小单位个数)。**不是 `Fix`** —— 同 D-21-17 的口径 |
| `档` | enum | 威胁 / 机会 / 反应 / 灾难(**属且只属**一档,规则三) |
| `anchor` | enum | 事件发生地(医馆门前 / 出诊路径上 / 采集点)—— 见规则十一后注 |
| `触发方式` | enum | **`随机` / `脚本`** —— **默认「随机」**;`脚本` 条目**不进抽池**,由注入方定义触发条件、52 只决定何时进入窗口(见规则十一的三案链裁决) |
| `cause_flag` | string? | 规则九的因果标记(如 `JH_ESCAPEE_SHELTERED`)。非空 = 该条目受历史乘子影响 |

> **52 不拥有任何内容字段** —— 上表没有「病种」「症状」「敌人类型」。
> **内容由注入方填,52 只持有 key、权重与调度元数据。** 这是规则二的可执行化。

> **边界铁律**:`35 时代事件不是随机的` —— 它是**有脚本的篇章**,
> 52 只决定它**何时进入窗口**,不决定它发生什么。这是 52 不吞并 35 的理由。
>
> #### ✅ 52 ↔ 37 的归属裁定(2026-09-14)
>
> **问题**:规则二说「导演**不生产内容**,只调度」,但 P0 池现在必须有医疗内容
> (见 §规则十一 的断口注)—— 那么**医疗事件的内容归谁**?
>
> **裁定**:**52 拥有「池条目与调度」,37 拥有「医疗内容」。**
>
> | | **52 随机事件导演** | **37 病例系统** |
> |---|---|---|
> | **拥有** | 池**条目** · 时机 / 强度 / 密度四轴 · 预告形态 | 病例的**内容与语义**(病种、体征、三案链) |
> | **它做什么** | 决定「**要不要来、来多少、来多强**」 | 决定「**来了是谁、是什么病**」 |
> | **它不做什么** | **不定义任何病例内容**(不写病种、不写症状) | **不管何时触发**(那是 52 的轴) |
>
> **这不动摇规则二** —— 52 依然是零内容的生产者:`W_base` 注册表里那条医疗事件的
> **内容字段由 37 填**,52 只持有**一个 key 与权重**。
> 与 27 的关系**同构**:52 决定遭遇来不来,27 决定遭遇来了怎么动。
> **37 决定医疗事件来了是什么病,52 决定它何时降临。**
>
> ⚠️ **P0 的这层接口现在只有 52 这一侧** —— 37 的 GDD 尚未撰写(`systems-index.md` §2 #37:
> 「未开始」)。**37 落笔时必须兑现本表的三条边界**,否则 P0 的医疗事件源会退化成空条目。

**规则三:四个情绪档。** 每条事件**属且只属**一档 —— 这保证内容池不会一边倒。

| 档 | 情绪 | 玩家的选择 |
| ---- | ---- | ---- |
| **威胁** | 来打你的 | 打 / 逃 / 绕 |
| **机会** | 来给你的 | 接 / 不接 |
| **反应** | 世界对你有意见 | 应对 / 无视 |
| **灾难** | 来了就改变地貌与人口 | 尽力 / 撤退 |

**规则四:强度轴随「玩家状态」,不随空间。**
《英灵神殿》用「离世界中心越远越强」;本作是**固定世界**(`concept-benchmark.md:60`),
空间纵深轴失效。改为:
- **P0**:随**玩家当前状态**(带药量 · 时段 · 相关技能 · 是否独行)
- **P1a**:随**声誉 + 生态区**

**⚠️ 上限帽(2026-09-14 复核补,用户裁定)**:强度**不得**单纯随「你看起来值得抢」上涨 ——
否则「刚采完药 + 技能 0 + 独行」恰好落最高档;失败后再掉药、掉 5% 熟练度 →**同档更难**,
且玩家**读不出为什么**,这正是否定反幻想(「有系统在刷怪」)的体感来源。故:

```
StrengthTier = clamp( Σ(归一化后的 PlayerState × 权重) , MIN_tier , MAX_tier )
StrengthCap  = min( StrengthTier , f(最高格斗线, 急救) )        ← 上限帽
```

`f(...)` 是玩家**硬实力**的代理:带药多只会让**事件更多**(你看起来值得抢),
不会让它**强过你打不过的量级**。**掉药掉熟练度后,帽子随之下降** —— 正反馈闭环成立。

> **归一化是硬要求**:`CarryLoad`(整数计数,可达 10²–10³)若与 `RelevantSkill`(1–60)、
> `SoloFlag`(0/1)同量级入加权和,会以 1–2 个数量级**淹没**后两者。
> 各项必须先除归一常数到 `[0,1]`(见 F1)。
> **P1b 注意**:`SoloFlag` 在联机时改为**队伍规模**(1–4),**语义漂移** ——
> 该旋钮须在 P1b 重新标定,不得沿用 P0 权重。

> **深水线归属(2026-09-14 复核补)**:`concept-benchmark.md` 断裂② 要求 P0 给一条
> **看得见的**难度曲线(采集衰减 + 季节)。**52 不接这条轴的坐标分量** ——
> 它的强度输入是**玩家侧**的,深水线是**空间/内容侧**的,归 **5 时间天气 · 17 采集**
> (见 §Dependencies,已登记归属)。

**规则五:进度锁 = 行医进度。**
《英灵神殿》的 key = 杀 Boss;本作的 key = **声誉 / 救人数 / 生态区解锁**。
**这是对《英灵神殿》最重要的一处改写** —— 你越有名,来找你的人越多,
而**大多数不是来打你的,是来求你的**;同时盯上你的人也变多变强(**有名是把双刃**)。
接 #31 声誉与支柱四(制度性权限)。

**规则六:密度预算。** 每 `TICKS_PER_DAY` 有事件数上限,超预算不刷 —— 防刷。
(原文写「每日 / **每次出诊**」两个分母,**已择一**:分母 = 日。见 F2。)
**脚本条目(三案链)不参与预算。**

**规则六之二:时机轴认识上下文(ContextGate)。** *(2026-09-14 复核补 —— 原文时机轴只有「随机间隔 + 昼夜」)*

核心循环要的是「**出诊途中**可能遇敌/遇险」,而纯随机间隔会让事件在**医馆里**刷出 ——
那会打断本作核心的「静」段(读脉案 = 判断层的前提),且刚打完一场立刻刷第二场。

```
ContextMult:  在出诊路径上  × CONTEXT_TRAVEL      (> 1)
              在医馆地块内  × CONTEXT_CLINIC      (威胁档 ≈ 0;机会/反应档保留)
              战斗结算后    × CONTEXT_COOLDOWN    (短冷却窗内 ≈ 0)
```

**医馆不是绝对安全区** —— 机会与反应档照常来(「门外有人」正是锚点①);
只有**威胁档**在医馆内被压制。**这是有意的**:让家成为「判断」的场所。

**规则七:医馆可被损毁,但知识不可被夺走。** *(**P1a** — 2026-09-14 复核修正)*

> ⚠️ **裁决 2026-09-13(用户选 B:可摧毁)**。
>
> ⚠️ **分档修正(2026-09-14 复核)**:本条整段为 **P1a**。
> 理由:P0 威胁档 = 兵痞劫道 + 野兽(规则十一),**均不打建筑**;围困医馆是 **P1a 条目**。
> 且损毁类事件的护栏①要求「玩家有时间回防」,而 **P0 无地图(#43 属 P1a)** ——
> 玩家凭什么知道回防?**P0 落不了这条承诺。**
> **P0 版本只保留一句**:「**医馆在 P0 不可被损毁**(围困 / 灾难档 P1a 才解锁)」。
> **F3 与相关的 AC 一并标注 P1a。**

事件**可以**损毁医馆。这给建造以真实的 stakes,也合乱世史实
(战乱中诊所被烧被抢是那个时代真实发生的事)。但**必须**配齐下面四条护栏,
否则它会反噬第一审美(Expression):玩家花数小时的建造被骰子抹掉 → 玩家干脆少建
→ Expression 反而崩掉。

1. **预告制** —— 任何威胁医馆的事件**必须先有预告**(规则八),玩家有时间回防
2. **分级损毁** —— 不是二值的毁 / 不毁(见下表)
3. **可修复** —— 损毁可在原地修补,材料可回收
4. **知识永不失** —— **脉案、已学的动作、已解锁的知识、技能等级永不被随机事件夺走**

**第 4 条不是新规矩** —— 它是概念 L270 原话的兑现:
「**不掉:已解锁的动作、已学的知识、已写的脉案**」。
**医馆可以烧,但你是带着知识走出去的。** 这保住了 Expression 的连续性:
房子是身外物,医术不是。

| 损毁等级 | 触发 | 后果 | 恢复 |
| ---- | ---- | ---- | ---- |
| **受损** | 围困战败 / 火灾蔓延 | 家具与墙受损,功能下降 | 就地修补,材料部分回收 |
| **摧毁** | 特定灾难(溃兵焚烧 / 地动) | 建筑体需重建 | **图纸保留、地基保留**;重建更快 |
| **焚毁** | 灾难档最高级,稀有 | 医馆全失 | **脉案与技能全数保留**;从零重建 |

**规则八:预告制**(借自《英灵神殿》的 raid 公告语)。
任何事件**先有名有预告**,绝不突袭。预告必须走**拟物通道**(支撑原则二)——
狗吠、马蹄、锣声、远处一片哭喊、药铺里突然安静下来。**不报数字。**

**规则九:你自招的升级**(借自《我的世界》的不祥之兆 → 劫掠)。
部分事件不是骰子来的,是**你做过的事**招来的。让随机事件有**因果**:

| 你做过的事 | 招来什么 |
| ---- | ---- |
| 救过一个逃犯 / 窝藏伤员 | 官兵上门盘查 |
| 在某村公开用西药治好重病 | 保守医家联名告你 / 医闹 |
| 拒绝过某军阀的征召 | 被针对的遭遇强度上升 |
| 在疫区出入而不做防护 | **你自己染病**(接 9) |

**⚠️ 机制落地(2026-09-14 复核补 —— 原文有表无机制,是本轮 blocking 之一)**:

因果**不是**「另开一条触发路径」(那会被规则六的「预算耗尽直接丢弃」静默吞掉,
且「你救过人,今天预算用完」是最刺眼的反幻想)。它落在 **F1 的乘子**里:

```
HistoryMult = Π( 1 + HIST_W[flag] )   over flags in 玩家历史标记集
```

- **`HIST_W[flag]`** 是每个历史标记的权重增量(P0 标量,`Fix`,可 0)。
  救过逃犯 → `HIST_W[JH_ESCAPEE_SHELTERED] > 0` → **官兵盘查条目的权重被抬起**。
- **它抬的是「哪一条被抽到」,不是「来不来」** —— 因果仍然是概率性的,
  但**可观测**:预告线索必须能读出「这是冲你来的」(见下)。
- **乘子是对应的、非全局的** —— 只作用于该 `cause_flag` 关联的池条目,
  **不作用于全池**(否则会像旧 F1 的全局乘子一样被归一化约去,见 F1 注)。

**因果可见性是硬要求。** 预告线索里必须带一条 `cause_clue_key`,由 **42 拟物 UI**
渲染成可读的世界细节(官兵的口音/腰牌、对方指名道姓)。
**「读得出这是冲你来的」= 因果成立**;读不出就是随机,规则九当场失效。

**规则十:疏忽触发**(借自《我的世界》的幻翼 —— 三天不睡)。
长期不管某事,世界来提醒你:
- 医馆连续多日无人 → 药材被窃 / 药圃荒芜 / 病人转投别家
- 长期不出诊 → 声誉下降,有人上门抱怨

> **✅ 两处补裁(2026-09-14 复核)**
>
> **① 规则十 受规则八约束吗?—— 受。** 「药材被窃 / 药圃荒芜」是**损毁类后果**,
> 而规则七护栏①要求损毁类**必须先有预告**。**故规则十的每一条都走预告通道**
> (门外的动静、药圃里被踩过的痕迹)。原文未裁定,与护栏①显式冲突,已修。
>
> **② 「无人」怎么判?** 检测源 = **医馆地块占用**,由 **23 建造 / 24 医馆** 提供
> (非 52 自算);窃取目标 = **20 库存**;荒芜载体 = **23 建造件**。
> **「无人」的定义**:玩家角色不在医馆地块内 —— **在门口也算有人**,睡着了不算。
> 该判据进 AC(见 §Acceptance Criteria)。
>
> **③ 时间量纲**:「多日」= `N × TICKS_PER_DAY`,接 `ITickProvider`(见 §确定性契约)。

**规则十一:跨档内容池(逐档扩张,不替换)。**

| 档 | 威胁 | 机会 | 反应 | 灾难 |
| ---- | ---- | ---- | ---- | ---- |
| **P0** | 兵痞劫道 · 蛇 / 熊 / 狼 / 野狗 | **行脚货郎** · **半路拦你求救的村民** · **急召出诊**(37) | **药材短缺**(37) | ~~**原型疫情**(37)~~ → **改为脚本条目,不进抽池**(见下) |
| **P1a** | + 山匪 · 溃兵 · **围困医馆** · 追杀(你救过的人被追) | + 药商队 · 游方郎中(换方子 / 换消息)· 古方与稀有药材线索 · 被引荐给权贵 | 官府盘查(无照行医)· 同行相轻 · 医闹 · **拉你入伙**(军阀 / 土匪 / 商会)· 流民涌入 | 疫病爆发 · 药材产地绝收 · 水旱 · **时代篇章窗口** |
| **P1b** | + 生态区级别的持续威胁 | + 联机协作机会 | + 多方势力同时施压 | + 生态区灾变 · 篇章高潮 |

> #### ✅ 三处裁定(2026-09-14 复核,用户裁定)
>
> **① 「事件是调味」与「四档齐全」不矛盾 —— 矛盾在把「频率」与「内容」塞进了同一句话。**
>
> | | **何时来**(频率) | **来的是什么**(内容) |
> |---|---|---|
> | **性质** | **调味** —— 可以缺席 | **核心玩法的原料** —— 不可缺席 |
> | **P0 要求** | 密度预算封上限;**一次没遇到也合法** | **四档齐全,不能有整档为「—」** |
>
> 原文「事件是调味不是驱动」说的是**频率**,却被读成了**内容**。已切分。
> 「P0 必须可通关」的意思是**主线不依赖随机事件**,不是「Boss 可以是彩票」。
>
> **② 三案链(原型疫情)移出抽池,归 37 确定触发。**
>
> 原设计把 P0 的「拼图样 Boss」挂在**灾难档随机**上,同时又说「一次没遇到也合法」
> —— **两者不能同时成立**(支柱四的承重墙不能靠彩票)。
>
> **裁定**:三案链作为**脚本条目**挂在 52 的注册表里(`触发方式 = 脚本`),
> 由 **37 病例系统**定义触发条件(如「连续三日同症候」),**52 只决定它何时进入窗口**。
> **这与 #35 时代事件同构** —— 52 只开窗口,不决定内容。
> **⚠️ 这不动摇 52↔37 的既有裁定**:池条目与调度仍归 52,医疗内容仍归 37;
> 变的只是**该条目的触发从骰子变成脚本**。
>
> **③ `anchor` 是 P0 必需,不是 P1a 装饰。**
> 池条目的 `anchor`(医馆门前 / 出诊路径上 / 采集点)决定**事件在哪生成**。
> 「兵痞**劫道**」「**半路**拦你求救的村民」语义上全是**路上**事件 ——
> 没有 anchor,它们会在医馆院内刷出,空间可信度一次崩掉。

> #### ✅ 「事件必须可拒绝或绕开」—— 从建议升为机制(2026-09-14 复核)
>
> 原文自认:「既然一半压力是战斗,这条就不能是软性建议」—— **但它既无机制也无 AC**。
> P0 无潜行、无替代路线,**回避动词根本不存在**,威胁档只有「打」或「弃」。
>
> **裁定:两条独立路径,都必须有 AC。**
> - **可拒绝** —— 预告窗口内不前往 = 事件不降临,记为**避险**(已有状态机支持)
> - **可绕开** —— 抵达现场后可选择**不接战**而完成其他目标(先救人 / 绕行),
>   遭遇体**不锁定玩家**;这是 **27 敌人 AI** 的接口要求,52 只声明它必须存在
>
> **锚点②(先听见后看见)在此处兑现** —— 预告窗口就是「拒绝」的操作窗口。

> #### ⚠️ P0 医疗事件模板(**须由 37 补 · 2026-09-14 对标复核**)
>
> **原 P0 池的问题**:威胁档全是战斗,机会 / 反应 / 灾难三档**要么空要么与行医无关**
> —— 核心动词「判」在 P0 唯一的事件源里**缺席**,而 8 诊断是全案唯一「不知道好不好玩」的
> 承重系统。**承重墙没有承重对象。**(详见 `systems-index.md` §5 #52 注)
>
> **本表已把三类医疗事件模板挂进 P0 各档**(括号内为注入方 37),它们同时是:
> 52 的池内容 · 37 的三案链载体 · 8 的「拼图样」原料 —— **一份内容,三处用**。
>
> ⚠️ **上表的中文条目是「占位 + 归属」,不是最终内容** ——
> 每条的具体形态(病人是谁、症候如何、可拒绝 / 可绕开的路径)由 **37 病例系统**定义,
> 且**须逐条考据后落盘**(支柱五)。
>
> **「四档齐全」的准确口径**(2026-09-14 修正):**P0 的四档必须都有内容** ——
> 但**灾难档由脚本条目承担**(原型疫情,触发方式=脚本),故
> **P0 的随机池里灾难档可以为空,内容不可为空。**
> 构建期校验的是「四档各有至少一条条目(**随机或脚本皆可**)」。

> **P0 的机会档 : 威胁档 = 1 : 1**(用户裁决 2026-09-13)。
> **P1a 起非战斗(机会 + 反应) : 战斗 = 1 : 1** —— 保留《英灵神殿》的生存建造骨架手感,
> 医学层是差异化增量而非替换。
>
> **⚠️ 机制(2026-09-14 复核补 —— 原文只有裁决,无公式、无 AC)**:
> **池先验占比 ≠ 运行期配额。** 一个 8 事件的夜晚可以全是威胁,1:1 当场失效。
> 故改为**预算窗口内按档无放回配比抽取**:先按 `档占比[]` 决定本窗口各档各抽几条,
> 再在每档内部按 `W_base × 时机权重` 加权抽取。
> **配额对玩家不可见**(不破反幻想:永远不显示「今日事件 3/5」)。
>
> **代价(明说)**:既然一半压力是战斗,**「事件必须可拒绝或绕开」就不能是软性建议** ——
> 否则一半的随机压力会变成不可回避的战斗,直接违反支柱一(判断为骨)。
> **该要求已有机制与 AC**(见上文裁定)。

### States and Transitions

事件实例的状态机(导演侧的推演,非玩家可见):

| 状态 | 含义 | 转到 |
| ---- | ---- | ---- |
| **待触发** | 时机轴到点,等待抽池 | **预告**(或 **取消**,若密度预算耗尽) |
| **预告** | 已注入预告线索,玩家可反应 | **降临** / **避险** |
| **降临** | 事件实体生成 | **结算** |
| **避险** | 玩家在预告窗口内离开 / 化解 | **结束**(视事件可记入声誉) |
| **结算** | 玩家处置完毕 | **结束** |
| **结束** | 释放密度预算 | — |

> **「避险」是一等公民**,不是失败 —— 它是支柱一(判断为骨)在事件层的落地:
> **你总能选择「先救人」而不接这一战。**

### Interactions with Other Systems

| 系统 | 方向 | 接口与数据流 |
| ---- | ---- | ---- |
| **5 时间天气** | 读 | 昼夜 / 季节 / 天气 → 时机轴权重与强度轴系数 |
| **6 世界与生态区** | 读 | 生态区、POI、聚落位置 → 抽池的位置过滤 |
| **27 敌人 AI** | 读 | 遭遇原型(**52 决定来不来,27 决定来了怎么动**) |
| **31 声誉** *(P1a)* | 读 | **进度锁**(规则五) |
| **32 生态区推进门槛** *(P1a)* | 读 | key 的组成之一 |
| **37 病例系统** | **被注入** | **P0**:往池里推医疗事件模板(规则二);**内容归 37,调度归 52** |
| **34 / 35 / 36** *(P1a)* | **被注入** | 各自往池里推内容(规则二) |
| **45 网络层** *(P1b)* | **被注入** | 联机协作事件与共享预算 |
| **9 疾病与伤情模拟** | 写(契约) | 疫情 / 伤病事件经 **9** 落地,52 **不直接操作病人状态** |
| **23 建造** | 写(契约) | 损毁等级(规则七) |
| **42 拟物 UI** | 读(契约) | 预告线索走拟物通道(**无数字**) |
| **30 技能与熟练度** | — | 事件**本身不给经验**;经验来自处置行为本身 |

> ⚠️ **52 绝不直接改病人状态** —— 疫情事件把参数交给 **9**,由 9 按自己的 tick 模型落地。
> 这是 `systems-index.md` §9 **C4** 的延伸:52 是**调度者**,不是**模拟者**。

## Determinism Contract(确定性契约)

> **本节是 2026-09-14 复核新增。** 原文全文对随机源、定点域、tick 源**零提及** ——
> 而 52 是全案**唯一既是随机数消费者、又是事件生产者**的 P0 系统。
> 链条是 `21a(数据) → 52(掷骰) → 9(模拟)`:21a 堵了自己的出口,9 堵了自己的门,
> **52 就是中间那段敞开的管子**。以下三条由 ADR-005 / ADR-006 直接推出,**不是新裁决**。

### DC-1 随机源(唯一)

```
WorldSeed            ← 存档创建时生成一次,持久化
EventRollSeed(t, k)  = SplitMix64( WorldSeed, t, k )      // t = tick, k = 池条目 key
```

- **唯一 RNG = SplitMix64(整数哈希),无状态** —— 与 9 的 `patient_seed` 同源同法。
- **禁止 `UnityEngine.Random`**(全局单例状态,27/13 任何一处调用都会推进同一全局流
  ⇒ 抽取结果取决于**调用次序**)、禁止 `System.Random`。
- **逐次抽取不持 PRNG 状态** —— 每次抽取由 `(t, k)` 重新派生,可重放、可中途加入。
- **P0 即预留 `IEventAuthority`**(主机掷骰、客户端只播)—— 与 ADR-005
  「主机唯一执行 Step / CatchUp」同构。**P1b 只是接上,不是新写。**

> **失败模式是静默的**:若用 `UnityEngine.Random`,两台机器抽出**不同事件** →
> 生成**不同病人** → `IIdAuthority` 分配序列错位 → **`patient_seed` 全变** →
> **病史事件流从第一条起永久分叉**。不崩溃、可能仍自洽,但 ADR-005 的核心裁决在这一环失效。

### DC-2 定点域(全量)

- **F1 / F2 / F3 的全部数学在整数定点域**(int64 / Q16.16),标 `Fix`。
- **`W_base` 是 `int`,不是 `Fix`** —— 它是**最小单位个数**(同 D-21-17)。
- **乘子不得写成浮点字面量**:`TODMult` 等一律写**分子/分母**或整数倍率
  (旋钮表 `>1` / `(0,1)` 的写法**正面违反 ADR-006**,已改)。
- **clamp 走整数 Min/Max**;**舍入模式统一 `ROUND_HALF_AWAY_FROM_ZERO`**
  (禁 `Math.Round` 的默认 ties-to-even)。
- **`Fix` 不可经 Unity 内置序列化器承载** —— 内容池**只能**落 `assets/data/*.json`
  并经 `FixParse` 读入。**禁止把 `Fix` 写进 ScriptableObject / `.asset`**
  (序列化器会静默跳过 readonly struct 的私有 `long` ⇒ 全池权重归零,不报错)。

> **为什么乘性链比加法链更危险**:`item-database.md:628` 已警告「float 累加顺序会翻转
> `≤ 上限` 判定」。52 的链是**乘法 + 加权和 + 一次 clamp 边界比较**,
> 一次翻转就改「遭遇强度」→ 改「战斗结果」→ 改 `INJ_COMA` 是否入流 → **事件流分叉**。

### DC-3 时间量纲(接 `ITickProvider`)

- **全案 tick 唯一来源 = `ITickProvider`**;`ITickProvider.CurrentTick`。
- **禁止 `Time.time` / `Time.deltaTime` / 协程 / `InvokeRepeating` / 墙钟 / 帧数。**
- **「日」= `TICKS_PER_DAY` 常量**,与 5 时间天气的日边界**同值**。
- **`预告窗口时长` 的单位是 tick**(原文未定单位,是最贵的一个悬空旋钮)。
- **F2 的「每日预算」按 tick 边界重置**,不按墙钟。

### DC-4 事件流归属

- **52 的「事件降临」是一条 `SimEvent`**(新 `EventKind`),走主机 `IEventSink.Append`。
- **52 只发事件,不改任何状态** —— 病人的创建与 `patient_id` 分配**唯一归主机 9**
  (防 ADR-006 Amendment B 的 id 复用漏洞)。
- **损毁等级由事件流派生,不另存** —— 否则违 ADR-005 规则六(存档存事件流)。

## Formulas

### F1 事件抽取(**P0 · 已重写**)

```
EventRoll:
  ① 预算窗口开启时,按 档占比[] 决定本窗口各档配额(无放回)
  ② 对每个配额,在该档内按权重抽取:
       W_i = W_base_i × TODMult_i × HistoryMult_i
       W_i = 0  若 KeyGate_i 未通过
       抽中概率  p_i = W_i / Σ W_j        (Σ W_j = 0 时见 Edge Cases)
  ③ 抽中后定档:StrengthTier(见下),并被 StrengthCap 钳位
  ④ 过密度预算 → 生成
```

**⚠️ 乘子必须是「对单条」的,不能是「对全池」的标量。**
若 `TODMult` 等是全池同一个标量 `k`,则 `p_i = w_i·k / Σ(w_j·k) = w_i/Σw_j` ——
**`k` 恒被约去,对分布零作用**。原文的「有名是把双刃」因此**数学上不可能实现**。
故 `ReputationMult` 改为**按档非对称**(下)。

**内容权重构成:**

| 变量 | 类型 | 含义 | 来源 |
| ---- | ---- | ---- | ---- |
| `W_base[event]` | **int** | 事件的基础权重(最小单位个数) | 内容池(21a 形状) |
| `TODMult_i` | `Fix` | **逐条**昼夜修正(夜 → **威胁档**条目 ↑;其他档恒 1) | 5 时间天气 |
| `HistoryMult_i` | `Fix` | **逐条**历史修正(规则九);只作用于 `cause_flag` 匹配的条目 | 52 自身的历史标记集 |
| `KeyGate_i` | bool | 进度锁过滤(未达 key 的条目**权重 = 0**) | 声誉 / 救人数 / 生态区(P1a) |
| `档占比[档]` | **int[4]** | 四档的配额占比(见规则十一之裁定) | 内容池配置 |

**`ReputationMult` 的修正(P1a · 本轮 blocking 之一)** —— 原为**全池标量**,已废:

```
ReputationMult[档] = { 机会: R_opp, 反应: R_react, 威胁: 1, 灾难: 1 }
```

**「一个数,两个方向」仍成立,但实现方式变了**:`R_opp` 与 `R_react` 是**两个独立参数**,
由 31 的声音值共同驱动(两者同升)。**对称的全局乘子做不到这件事** ——
这是原文的核心机制缺陷,已修。

**强度轴**由 `PlayerState` 而非距离决定(各项**先归一化到 `[0,1]`**):

| 变量 | 类型 | 归一化 | 含义 |
| ---- | ---- | ---- | ---- |
| `CarryLoad` | int | `CarryLoad / CARRY_CAP` | 当前携带的药量(药多 → 事件**更多**,但**不强过你的量级** —— 见 `StrengthCap`) |
| `RelevantSkill` | int | `RelevantSkill / SKILL_CAP` | 相关技能等级(格斗线 vs 遭遇;诊断 vs 病情)。**权重为正** |
| `SoloFlag` | bool→int | 原样(0/1) | 是否独行。**P1b 改队伍规模(1–4),须重新标定** |
| `EcoTier` | enum | **查表**,非序数 | 生态区(P1a)。**P0 权重恒为 0** |

```
StrengthRaw  = Σ( 归一化(PlayerState 各变量) × W_strength[变量] )     // Fix
StrengthTier = clamp( StrengthRaw , MIN_tier , MAX_tier )              // 整数 Min/Max
StrengthCap  = min( StrengthTier , f(最高格斗线, 急救) )              // 上限帽(规则四)
```

**⚠️ `MIN_tier` 是全局常数,不是「地板砖」**:若 `MIN_tier` 高于新玩家算出的 `StrengthRaw`,
clamp 会把它**向上**拉 —— 规则四当场失效。故 **`MIN_tier` 随进度缩放**
(见 Tuning Knobs),且**构建期校验 `MIN_tier ≤ MAX_tier`**。

**Output Range:** `MIN_tier` 到 `MAX_tier`(整数档位)。
**P0 的 `EcoTier` 权重恒为 0** —— 固定世界无空间纵深(`concept-benchmark.md:60` 的验证点)。

**Worked example(算例 —— 原文缺失,本项 `design-docs.md` 要求):**

```
设定:CARRY_CAP = 500, SKILL_CAP = 60
W_strength[CarryLoad] = 2, W_strength[RelevantSkill] = 3, W_strength[SoloFlag] = 1
MIN_tier = 1, MAX_tier = 5

玩家甲:CarryLoad=10, 短兵=2, 独行
  归一化 = 10/500=0.02 | 2/60=0.033 | 1
  StrengthRaw = 0.02×2 + 0.033×3 + 1×1 = 0.04 + 0.10 + 1.00 = 1.14
  → clamp → StrengthTier = 1  (0.02 与 0.033 的贡献被 SoloFlag 淹没 —— 归一化仍不够,
   故 W_strength 必须按量级标定;这正是「无上限帽会出事」的数值证据)

玩家乙:CarryLoad=480, 短兵=40, 不带独行标记
  归一化 = 480/500=0.96 | 40/60=0.667 | 0
  StrengthRaw = 0.96×2 + 0.667×3 + 0 = 1.92 + 2.00 = 3.92
  → clamp → StrengthTier = 4
  → StrengthCap = min(4, f(短兵 40, 急救 10))

玩家丙:玩家乙刚被打倒 —— 掉药(CarryLoad→150)、短兵 →38
  归一化 = 0.30 | 0.633 | 0
  StrengthRaw = 0.60 + 1.90 = 2.50 → StrengthTier = 3   ← 帽子随实力下降,闭环成立
```

### F2 密度预算(**P0**)

```
EventBudgetPerDay = clamp( BASE + ΣMod , 0 , BUDGET_MAX )
```

| 变量 | 类型 | 含义 | 默认 |
| ---- | ---- | ---- | ---- |
| `BASE` | int | 每日基础事件数 | *待裁* |
| `Mod_Eco` | int | 生态区修正 | *待裁* |
| `Mod_Rep` | int | 声誉修正(P1a) | *待裁* |
| `BUDGET_MAX` | int | 硬上限(防「N 人重新打开刷怪洪流」) | *待裁* |

> **⚠️ 三处修正(2026-09-14 复核)**:
> ① **下钳 0** —— 原文 `BASE=0, ΣMod=−1` 会得到**负预算**,低于声明范围却无钳制。
> ② **「联机人数」项已删** —— 它与「事件是世界级的、按**队伍共享**预算」冲突:
> 若该项为正,N 人反而重新打开被共享预算封堵的洪流。**联机不抬高预算。**
> ③ **分母 = `TICKS_PER_DAY`**,不是「日 / 每次出诊」二选一(规则六已修)。
>
> **超预算的事件直接丢弃,不排队** —— 排队会导致「雪崩式袭击」,那不是设计意图。
> **⚠️ 例外**:`触发方式=脚本` 的条目(三案链)**不参与密度预算**(见规则十一裁定)。

### F3 损毁与重建成本(**P1a** — 2026-09-14 复核改为 P1a,见规则七)

```
RepairCost  = Σ( 受损件.base_cost × DamageRatio ) − SalvageValue
SalvageValue = Σ( 受损件.base_cost × DamageRatio × SALVAGE_RATE )

RebuildCost_mark  = FullCost_mark × ( 1 − SALVAGE_RATE )     // 摧毁档
RebuildCost_razed = FullCost_mark × ( 1 − SALVAGE_RATE_RAZED )  // 焚毁档(回收率更低)
```

| 变量 | 类型 | 含义 | 安全范围 |
| ---- | ---- | ---- | ---- |
| `DamageRatio` | `Fix` | 单件损毁比例 | **[DAMAGE_MIN, 1)**,`DAMAGE_MIN > 0` |
| `SALVAGE_RATE` | `Fix` | 摧毁档材料回收率 | **(0, 1)** —— **不含 1** |
| `SALVAGE_RATE_RAZED` | `Fix` | 焚毁档回收率 | **(0, SALVAGE_RATE)** |
| `FullCost_mark` | int | 该建筑件的完整建造成本 | ≥ 0 |
| `base_cost` | int | 部件基础成本 | ≥ 0 |

> **⚠️ 四处边界退化(2026-09-14 复核 + 用户裁定)**:
> ① **`SALVAGE_RATE` 范围由 `(0,1]` 改为 `(0,1)`** —— 含 1 时
> `RebuildCost = FullCost × 0 = 0`,**重建免费**,焚毁档失去全部 stakes,
> 且玩家可反复故意焚毁刷布局。
> ② **`DamageRatio = 1` 曾使 `RepairCost = FullCost > RebuildCost`** ——
> **「修不如拆」,维修被严格支配**,与「就地修补」承诺相悖。**已补 `SalvageValue` 回收项**;
> 且 `DAMAGE_MIN > 0` 保证 `DamageRatio = 0` 时不会「零成本复原」。
> ③ **焚毁档原无成本式** —— 已补 `RebuildCost_razed`。
> ④ **材料是整数计数** —— 乘 `SALVAGE_RATE` 的舍入走 **`ROUND_HALF_AWAY_FROM_ZERO`**(ADR-006)。
>
> **⚠️ 构建期拒绝**:`SALVAGE_RATE = 0`(「可修复」承诺破产)、`SALVAGE_RATE = 1`(重建免费)、
> `SALVAGE_RATE_RAZED ≥ SALVAGE_RATE`、`MIN_tier > MAX_tier`、`预告窗口时长 ≤ 0`、
> `DAMAGE_MIN ≤ 0`。

**Variables 表** —— 全部数值待用户裁定(见 Tuning Knobs)。

## Edge Cases

| 情况 | 处理 | 理由 |
| ---- | ---- | ---- |
| **玩家在预告窗口内离开事发区域** | 记为**避险**,事件结束,**不惩罚** | 支柱一:你总能选「先救人」 |
| **事件抽中但密度预算耗尽** | **直接丢弃**,不排队、不延后 | 防雪崩式袭击 |
| **医馆无人时被围困** | 事件照常发生;按规则七**分级损毁**;**脉案与技能不受影响** | 知识永不失 |
| **玩家在医馆内被围困** | 进入防守 / 救治的取舍(同在场的病人要不要先撤) | 判断为骨 |
| **疫情事件与 #35 篇章窗口重叠** | **篇章优先**;随机事件让位 | 篇章是脚本,不可被骰子挤掉 |
| **玩家反复在同一生态区刷事件** | 密度预算封堵;且事件**不给经验** | 双重封堵 |
| **「你自招的升级」被玩家抱怨「凭什么」** | **不是 bug** —— 因果必须**可见**(预告线索里要能读出「这是冲你来的」) | 规则九 | 
| **一个事件同时命中两档** | **不允许** —— 每条事件属且只属一档 | 规则三 |
| **联机时多人各自触发** | 事件是**世界级**的,不是玩家级的;按队伍共享预算(P1b) | 一致性 |
| **玩家故意用灾难档事件刷材料** | 灾难档**不产出可刷取的战利品** —— 产出是叙事与**权限** | 支柱四 |
| **P0 期间玩家一次都没遇到事件** | **合法**。P0 的事件是**调味**不是**驱动**,不能因没遇到而卡住主线 | P0 必须可通关 |
| **`SALVAGE_RATE = 0` / `预告窗口时长 = 0` / `MIN_tier > MAX_tier`** | **构建期报错** | 三者分别违反「可修复」、支柱一、F1 的 clamp 良定义 |
| **ΣW_j = 0(档内全部条目权重归零)** | **该档跳过本轮配额,不掷骰**;不报错 | 除零的显式退化路径(S5)。`KeyGate` 全封或全档 `TODMult=0` 都会触发 |
| **脚本条目(三案链)与随机条目同时到期** | **脚本优先**;随机条目让位(与「篇章优先」同构) | 脚本是不可被骰子挤掉的内容 |
| **医疗事件(37)的处理经验** | **完全归 30 管辖,52 不承诺封堵** —— 原「事件不给经验」的防刷论证来自**战斗**事件,不适用于医疗事件(处置病人**必然**驱动 30) | 防刷只剩密度预算一层;重复病种的冷却归 30 的新颖度加权 |
| **玩家在医馆内且威胁档到期** | `ContextGate` 压制(见规则六之二),**事件延后到下一次出诊**,不丢弃预算 | 家 = 判断场所;但预算不浪费 |
| **玩家连续多日在同一区域停留** | 密度预算封顶;**且抽取输入的 `t` 不含 dwell time** —— 「停留」在数学上进不了权重 | 原 AC「频率收敛而非上升」不可测,已改为这条**结构性**判据 |
| **预告窗口内玩家正在战斗** | 预告**照常发出**(可感知),但窗口计时**暂停**?→ **不暂停**;战斗是玩家自选的代价 | 规则八无例外;否则「先清场再回防」变成一个可刷的漏洞 |

## Dependencies

| 系统 | 方向 | 性质 | 接口 |
| ---- | ---- | ---- | ---- |
| **5 时间天气** | 5 → 52 | 数据 | 昼夜 / 季节 / 天气 → 时机轴与强度轴 |
| **6 世界与生态区** | 6 → 52 | 数据 | 生态区与 POI → 位置过滤 |
| **27 敌人 AI** | 27 → 52 | 数据 | 遭遇原型;52 触发,27 行为 |
| **31 声誉** *(P1a)* | 31 → 52 | 数据 | 进度锁 |
| **32 生态区推进门槛** *(P1a)* | 32 → 52 | 数据 | key 的组成 |
| **37 病例系统** | 37 → 52 | **注入** | **P0** 医疗事件模板;**内容归 37,池条目与调度归 52**(见规则二裁定) |
| **34 / 35 / 36** *(P1a)* | → 52 | **注入** | 内容池扩张 |
| **45 网络层** *(P1b)* | → 52 | **注入** | 联机事件与共享预算 |
| **9 疾病与伤情模拟** | 52 → 9 | 契约 | 疫情事件交 9 落地(**C4**) |
| **23 建造** | 52 → 23 | 契约 | 损毁等级 |
| **42 拟物 UI** | 42 → 52 | 契约 | 预告线索的呈现通道 |
| **21a 物品库** | 21a → 52 | 数据 | **`weight → InstanceWeight → CarryLoad`**(F1 的强度轴输入;2026-09-14 补登 —— 原表漏,`item-database.md` 已登记此边) |
| **20 库存与物品** | 20 → 52 | 数据 | `CarryLoad` 的实际取数(21a 只给形状) |
| **44 音频** | 44 ← 52 | 契约 | 预告线索的**音频侧**(狗吠 / 马蹄 / 锣声;原表漏) |
| **`ITickProvider`**(ADR-005) | → 52 | 契约 | **全案 tick 唯一来源**(DC-3);禁墙钟 |
| **`IEventSink`**(ADR-005) | 52 → | 契约 | 事件降临写为 `SimEvent`(DC-4);**主机唯一** |
| **`IEventAuthority`**(ADR-005 派生) | 52 内部 | 契约 | **P0 预留**:主机掷骰、客户端只播(DC-1) |
| **深水线信号**(5 时间天气 · 17 采集) | 5/17 → 世界 | 数据 | **P0 交付「看得见的难度曲线」**(采集衰减 + 季节);**52 不接其坐标分量**,仅在本表登记归属(见规则四注 + `concept-benchmark.md` 断裂②) |

> ⚠️ **52 → 9 与 52 → 23 是出向契约,不构成分层环**:
> 52 只传**参数与等级**,不认识任何病程或建造语义。
>
> ⚠️ **依赖双向性缺口(2026-09-14 复核)**:本表声明的 13 个系统中,
> **仅 9 有 GDD 文件**;5 / 6 / 27 / 31 / 32 / 37 / 34 / 35 / 36 / 45 / 23 / 42 / 44 / 20 全部**尚无文件**。
> **其中 37 是 P0 医疗内容的唯一所有者且状态「未开始」** —— 52 的修订**必须先于 37 落笔**,
> 否则 37 会拿到一个「随机 vs 脚本」未定的接口。

## Tuning Knobs

> **全部数值待用户裁定** —— 本 GDD 只交付公式与旋钮,不替用户拍定值。
> **全部数值集中在数据文件**,不得硬编码(见 `coding-standards.md`)。

| 旋钮 | 位置 | 类型 | 默认 | 安全范围 | 作用 / 极端后果 |
| ---- | ---- | ---- | ---- | ---- | ---- |
| `BASE` | F2 | int | *待定* | ≥ 0 | 每日基础事件数。↑ 则世界更「闹」,但会压过判断层 |
| `Mod_Eco` / `Mod_Rep` | F2 | int | *待定* | — | 生态区 / 声誉修正(P1a) |
| `BUDGET_MAX` | F2 | int | *待定* | ≥ `BASE` | 硬上限。**联机不抬高它** |
| `TODMult`(夜) | F1 | `Fix`(分子/分母) | *待定* | **> 1**(仅威胁档) | 夜间威胁权重。**必须有上界**,否则可垄断池 |
| `HistoryMult` 各 `HIST_W` | F1 | `Fix` | *待定* | ≥ 0 | 规则九因果强度。**0 = 该因果不生效** |
| `ReputationMult[机会]` / `[反应]` | F1 | `Fix` | *待定* | > 1 | **两个独立参数**(对称全局乘子不可实现) |
| `W_strength[变量]` | F1 | `Fix` | *待定* | — | 强度轴权重。**须按归一化后的量级标定**(见 F1 算例) |
| `CARRY_CAP` / `SKILL_CAP` | F1 | int | *待定* | > 0 | 归一化常数;`SKILL_CAP` **与 30 的技能上限 60 同值** |
| `StrengthCap` 的 `f(...)` | 规则四 | 表格 | *待定* | 单调不减 | 上限帽。**去掉它 = 「带药多就变强」的怪反馈回归** |
| `MIN_tier` / `MAX_tier` | F1 | int | *待定* | `MIN ≤ MAX` | 档位上下限。**`MIN_tier` 须随进度缩放**,否则把新玩家向上拉 |
| `预告窗口时长` | 规则八 | **tick 数** | *待定* | **> 0**(构建期拒绝 0 与负值) | **0 = 取消预告制 = 违反支柱一**;1 tick 通过检查但功能上等于无预告(故须给经验下界) |
| `CONTEXT_TRAVEL` / `CONTEXT_CLINIC` / `CONTEXT_COOLDOWN` | 规则六之二 | `Fix` | *待定* | `TRAVEL > 1`,`CLINIC(威胁档) ≈ 0` | 时机轴上下文系数 |
| `档占比[威胁/机会/反应/灾难]` | 规则十一 | int[4] | *待定* | 各 ≥ 0,和 > 0 | **四档各一个旋钮** —— P0 须满足 `机会 : 威胁 = 1 : 1` |
| `SALVAGE_RATE` | F3(**P1a**) | `Fix` | *待定* | **(0, 1)** —— 不含 1 | 摧毁档回收率。**= 0 则完全重来;= 1 则重建免费** |
| `SALVAGE_RATE_RAZED` | F3(**P1a**) | `Fix` | *待定* | **(0, SALVAGE_RATE)** | 焚毁档回收率 |
| `DAMAGE_MIN` | F3(**P1a**) | `Fix` | *待定* | **> 0** | 损毁比例下限。**= 0 则「零成本复原」** |

> ⚠️ **全部数值待用户裁定** —— 本 GDD 只交付公式与旋钮,不替用户拍定值。
> **全部数值集中在 `assets/data/*.json`**,不得硬编码(见 `coding-standards.md`);
> **不得落 ScriptableObject**(DC-2:`Fix` 会被序列化器静默吞掉)。
>
> ⚠️ **联动**:`BASE` ↑ 与 `机会` 档占比 ↓ **一起调**,
> 会把本作拉回《英灵神殿》的「打怪驱动」手感 —— 那正是本轮最想避开的东西。

## Visual/Audio Requirements

**本系统不渲染任何东西** —— 它是调度器。但它**规定了预告线索的形态**,
而预告全走**拟物通道**:

| 事件档 | 预告线索(拟物) | 音频 |
| ---- | ---- | ---- |
| **威胁** | 狗吠、马蹄、锣声、鸟群惊飞 | 由 44 音频系统实现 |
| **机会** | 门外的铃声、货郎的吆喝 | 同上 |
| **反应** | 门外有人清嗓子、一双手递帖子 | 同上 |
| **灾难** | 远处一片哭喊、钟声 | 同上 |

> **原则**:预告线索必须**可被主角感知,但不能被读出数字** —— 视觉锚点支撑原则二。

## UI Requirements

**本系统无 UI。** 唯一例外是一条**否定声明**:

**本系统不产生任何事件列表、事件计数或事件进度条。**
事件一旦可被计算和刷取,它就不再是世界的一部分。

| 信息 | 显示位置 | 更新频率 | 条件 |
| ---- | ---- | ---- | ---- |
| *(无 —— 事件清单不可见)* | — | — | — |

## Acceptance Criteria

> **2026-09-14 复核对 AC 体系的整改**:原文 17 条**无编号、无分级、无 Story Type 标注**,
> 且大量是「检索源码 → 零命中」的 **grep 式否定断言**。
> 该缺陷 `item-database-review-log.md` 二轮**已裁定不合格**,本文件**复发未继承**,已修。
> - **分级**:`[B]` = BLOCKING(不通过不得进实现)· `[A]` = ADVISORY
> - **层**:`[E]` = EditMode 单元测 · `[I]` = PlayMode 集成测 · `[M]` = 人工 / 美术签核
> - **grep 式静态守门已升级为类型引用级断言(Roslyn)** —— 见 `AC-52-03`

### 骨架(AC-52-01 … 05)

- [ ] **AC-52-01** `[B][E]` **GIVEN** 全量池条目(含脚本条目),**WHEN** 校验档位属性,**THEN** 每条**属且只属**四档之一,且**四档各有 ≥1 条**(规则十一裁定后的准确口径)
- [ ] **AC-52-02** `[B][E]` **GIVEN** P0 池数据文件,**WHEN** 构建期校验,**THEN** **威胁档与机会档各有 ≥1 条随机条目**且条数比 = **1 : 1**(规则十一用户裁决的可执行化)
- [ ] **AC-52-03** `[B][E]` **GIVEN** 52 程序集,**WHEN** 做**类型引用级**扫描(Roslyn,非 grep),**THEN** 该程序集**不引用任何病种 / 症状 / 体征类型**;`W_base` 注册表**只含 key 与权重**,不含内容字段。**附白名单清单**。*[医疗内容归 37 的守门]*
- [ ] **AC-52-04** `[B][E]` **GIVEN** 5 时间天气数据缺失,**WHEN** 抽事件,**THEN** **由权威侧判定**降级为均匀抽取、**不崩溃**,且降级判定**进入事件流**(不各机自判)
- [ ] **AC-52-05** `[A][E]` **GIVEN** 状态机定义,**WHEN** 穷举转移,**THEN** `待触发/预告/降临/避险/结算/结束` 六态**全部可达**,且无孤立态

### 确定性(AC-52-06 … 11 —— 本轮新增,ADR-005/006 的直接守门)

- [ ] **AC-52-06** `[B][E]` **GIVEN** 同一 `WorldSeed` 与同一 `(tick, key)` 序列,**WHEN** 跑两次事件抽取,**THEN** **逐位相同**;换一台机器同样逐位相同
- [ ] **AC-52-07** `[B][E]` **GIVEN** 52 程序集,**WHEN** 做类型引用级扫描,**THEN** **零** `UnityEngine.Random` / `System.Random` 引用。*[DC-1]*
- [ ] **AC-52-08** `[B][E]` **GIVEN** F1/F2/F3 的全部中间量与输出,**WHEN** 检查类型,**THEN** 全部为 `Fix` 或 `int`,**零 `float`**;且**无浮点字面量**经 `FixParse` 之外的路径进入。*[DC-2]*
- [ ] **AC-52-09** `[B][E]` **GIVEN** 池数据以 ScriptableObject / `.asset` 承载,**WHEN** 构建,**THEN** **报错**(`Fix` 会被序列化器静默吞掉 —— 只允许 `assets/data/*.json`)。*[DC-2]*
- [ ] **AC-52-10** `[B][E]` **GIVEN** 52 的时间相关量(日边界 / 预告窗口 / 冷却窗),**WHEN** 检查取数来源,**THEN** 全部来自 `ITickProvider`,**零** `Time.time` / `Time.deltaTime` / 协程 / `InvokeRepeating`。*[DC-3]*
- [ ] **AC-52-11** `[B][E]` **GIVEN** 一次事件降临,**WHEN** 追踪其写入,**THEN** 它是**一条 `SimEvent`** 且**只**经主机 `IEventSink.Append` 写出;52 自身**零** `patient_id` 分配调用。*[DC-4]*

### 预告制(AC-52-12 … 14)

- [ ] **AC-52-12** `[B][I]` **GIVEN** 任一威胁类事件,**WHEN** 生成,**THEN** **必先经过预告状态**,不存在「直接降临」的路径(穷举所有生成入口)
- [ ] **AC-52-13** `[B][I]` **GIVEN** 预告窗口,**WHEN** 玩家离开事发区域,**THEN** 终态 = **避险**,遭遇体生成计数 **= 0**,玩家状态(生命 / 物资 / 声誉)**逐字段与进入前相等**
- [ ] **AC-52-14** `[B][E]` **GIVEN** `预告窗口时长 = 0` **或为负**,**WHEN** 构建,**THEN** **报错**;且窗口时长 **≥ 1 tick** 有独立断言(负值不得绕过 `=0` 检查)

### 可拒绝 / 可绕开(AC-52-15 … 16 —— 原文声称但零 AC 的硬要求)

- [ ] **AC-52-15** `[B][I]` **GIVEN** **全威胁池逐条**(不允许白名单例外),**WHEN** 玩家在预告窗口内不前往,**THEN** 终态 = **避险**,不进入战斗,不惩罚
- [ ] **AC-52-16** `[B][I]` **GIVEN** 玩家已抵达威胁类事件现场,**WHEN** 玩家选择不接战而完成其他目标,**THEN** 遭遇体**不锁定玩家**、可脱离;该路径**必须可达**(27 敌人 AI 的接口要求)

### 因果(AC-52-17 … 19 —— 规则九的守门)

- [ ] **AC-52-17** `[B][E]` **GIVEN** 玩家历史标记集含 `JH_ESCAPEE_SHELTERED`,**WHEN** 抽池,**THEN** 关联 `cause_flag` 的条目权重**被抬起**;无关条目权重**不变**(乘子是**对应的,非全局的**)
- [ ] **AC-52-18** `[B][E]` **GIVEN** 任一因果触发的预告,**WHEN** 检查其载荷,**THEN** `cause_clue_key` **非空且可解析**
- [ ] **AC-52-19** `[B][E]` **GIVEN** 预告载荷结构体,**WHEN** 静态检查字段类型,**THEN** **无数值通道**(兑现「不能被读出数字」)
- [ ] *(人工 · ADVISORY)* **AC-52-M1** `[A][M]` **GIVEN** 因果事件的预告线索,**WHEN** playtest 玩家描述感受,**THEN** 能读出「这是冲我来的」而非「随机来的」;由 **42 拟物 UI** 签核

### 进度锁与强度轴(AC-52-20 … 25)

- [ ] **AC-52-20** `[B][E]` **GIVEN** P0 构建配置,**WHEN** 检查 31 声誉系统在依赖表中的分档,**THEN** 标记为 **(P1a)**;且 F1 的 `ReputationMult[]` 在 P0 **无输入源**(等价恒为 1)。*[原文的「ReputationMult=1」是同义反复死断言,已改为依赖表 + 公式路径双重守门]*
- [ ] **AC-52-21** `[B][E]` **GIVEN** 注入 P1a 桩,**WHEN** 未达 key 的条目参与抽取,**THEN** 其权重 **= 0**(`KeyGate` 生效)。*[替代原死断言]*
- [ ] **AC-52-22** `[B][E]` **GIVEN** 强度轴在 P0(无声誉、无生态区),**WHEN** 以**任意坐标**跑两次,**THEN** `StrengthRaw` **逐位相同** —— 坐标不是输入。**且**:构建期常量断言 `W_strength[EcoTier] = 0`。*[改写:原文把坐标依赖验证成「必须为零」会锁死未来接入;现为「P0 是设计意图 + P1a 由 5/17 交付深水线」(见 DC 与 Dependencies)]*
- [ ] **AC-52-23** `[B][E]` **GIVEN** 输入超限(`StrengthRaw > MAX_tier` 或 `< MIN_tier`),**WHEN** 计算,**THEN** 被**钳位**而非溢出 / 回绕
- [ ] **AC-52-24** `[B][E]` **GIVEN** 归一化后的各项,**WHEN** 分别移除 `CarryLoad` / `RelevantSkill` / `SoloFlag` 之一,**THEN** `StrengthRaw` **可观测地变化**(无任何一项被数量级淹没 —— 原 F1 的 `CarryLoad` 淹没问题有回归测试)
- [ ] **AC-52-25** `[B][I]` **GIVEN** 玩家带药多但**技能低**,**WHEN** 结算强度,**THEN** `StrengthCap` 生效,强度**不超过** `f(最高格斗线, 急救)`;玩家被打倒掉药掉熟练度后,**强度随之下降**

### 密度与防刷(AC-52-26 … 29)

- [ ] **AC-52-26** `[B][E]` **GIVEN** 本日事件数已达 `EventBudgetPerDay`,**WHEN** 再次抽中,**THEN** 事件**被丢弃且不排队**;但 `触发方式=脚本` 的条目**不受此限**
- [ ] **AC-52-27** `[B][E]` **GIVEN** `BASE = 0` 且 `ΣMod < 0`,**WHEN** 计算预算,**THEN** 结果**下钳 0**,不为负
- [ ] **AC-52-28** `[B][E]` **GIVEN** 抽取函数的输入签名,**WHEN** 静态检查,**THEN** **不含 dwell time / 停留时长** —— 「停留」在数学上进不了权重。*[替代原不可测的「频率收敛」]*
- [ ] **AC-52-29** `[B][I]` **GIVEN** 玩家在医馆地块内,**WHEN** 威胁档条目到期,**THEN** 被 `ContextGate` 压制并**延后**,预算**不丢弃**;机会 / 反应档**照常**

### 医疗与三案链(AC-52-30 … 32)

- [ ] **AC-52-30** `[B][E]` **GIVEN** 三案链条目,**WHEN** 检查其 `触发方式`,**THEN** = **`脚本`**;**不参与抽池、不参与密度预算**
- [ ] **AC-52-31** `[B][E]` **GIVEN** 脚本条目与随机条目**同时到期**,**WHEN** 结算,**THEN** **脚本优先**(与「篇章优先」同构)
- [ ] **AC-52-32** `[B][I]` **GIVEN** 一次 P0 全程,**WHEN** 校验主线推进,**THEN** 主线**不因「未遇到随机事件」而卡住**;且**三案链条目在满足 37 定义的触发条件时必然进入窗口**

### 损毁(AC-52-33 … 35 —— **P1a**)

- [ ] **AC-52-33** `[A][I]` **GIVEN** 一次围困导致医馆损毁,**WHEN** 读取存档,**THEN** **脉案、技能等级、已解锁知识完全不变**(**P1a**)
- [ ] **AC-52-34** `[A][E]` **GIVEN** `DamageRatio = 1`,**WHEN** 比较 `RepairCost` 与 `RebuildCost_mark`,**THEN** **`RepairCost < RebuildCost_mark`**(维修不被重建支配)。**P1a**
- [ ] **AC-52-35** `[A][E]` **GIVEN** 构建期,**WHEN** 传入 `SALVAGE_RATE ∈ {0, 1}` 或 `SALVAGE_RATE_RAZED ≥ SALVAGE_RATE` 或 `MIN_tier > MAX_tier` 或 `DAMAGE_MIN ≤ 0`,**THEN** **报错**。**P1a**

### 边界(AC-52-36 … 38)

- [ ] **AC-52-36** `[B][E]` **GIVEN** 疫情事件与 #35 篇章窗口重叠,**WHEN** 结算,**THEN** **篇章优先**
- [ ] **AC-52-37** `[A][I]` **GIVEN** 灾难档事件,**WHEN** 结算,**THEN** 其产出**不含可刷取的战利品**,只有叙事与权限
- [ ] **AC-52-38** `[B][E]` **GIVEN** 档内全部条目权重归零(`ΣW_j = 0`),**WHEN** 抽该档,**THEN** **跳过该档配额、不掷骰、不报错**

### 工程(AC-52-39 … 41)

- [ ] **AC-52-39** `[B][I]` **GIVEN** IL2CPP Release 构建 + 标定硬件,**WHEN** 跑事件抽取,**THEN** **GC.Alloc = 0 字节**且 **P99 < 0.1 ms**。*[原文「< 1 ms」无鉴别力、不可复现]*
- [ ] **AC-52-40** `[B][E]` **GIVEN** 抽取路径,**WHEN** 检查触发方式,**THEN** **由 tick 触发求值**,**无每帧轮询**
- [ ] **AC-52-41** `[B][E]` **GIVEN** 52 源码,**WHEN** 扫描硬编码,**THEN** 全部数值来自数据文件(**附白名单**:0/1 与结构常量);与 ADR-006 交界处**无 float 字面量**

## Open Questions

| Question | Owner | Deadline | Resolution |
| ---- | ---- | ---- | ---- |
| **P0 机会档的具体内容**(货郎卖什么 / 求救者的来源与去向) | 用户 | 写 27 / 13 前 | 未定 |
| **P0 三类医疗事件模板的内容**(原型疫情 / 急召出诊 / 药材短缺:病人是谁、症候、可拒绝路径) | **37 病例系统** | 写 37 时 | **未定** —— 52 只占池条目,内容归 37(见规则二裁定);须逐条考据(支柱五)。**⚠️ 原型疫情的触发条件也归 37**(三案链条目,`触发方式=脚本`) |
| **P1a 医疗 / 社会事件清单** | 用户 + 专项考据 | P1a 设计前 | **未列** —— 须逐条考据(支柱五) |
| **P1b 生态区灾变 / 联机事件** | — | P1b | 未定 |
| ~~损毁三档的触发条件细则~~ | 用户 | ~~写 23 建造前~~ | **转为 P1a 门控项**(规则七已标 P1a);写 23 前必须关闭 |
| **声誉如何驱动 `ReputationMult[机会]` 与 `[反应]`**(两个独立参数的具体函数) | 用户 | 写 31 前 | **未定** —— **公式形状已定**(非对称双参数,见 F1),只剩函数与数值 |
| **「你自招的升级」的因果如何在预告里被读出** | 42 拟物 UI | 42 设计时 | **机制已定**(`cause_clue_key`,见规则九);**剩余 = 42 的具体呈现**,由 AC-52-M1 签核 |
| **P1a 事件的历史依据** | 用户 + 考据 | P1a 落笔前 | **未核实** —— 疫情 / 官府盘查 / 鸦片相关事件的具体形态须按支柱五复核 |
| **`MIN_tier` 如何随进度缩放**(斜率与锚点) | 用户 | 写 25 前 | **未定** —— 形状已定(规则四 + F1),数值归用户 |
| **`StrengthCap` 的 `f(最高格斗线, 急救)` 形状** | 用户 | 写 25 前 | **未定** —— 须单调不减;可与 25 格斗线同批标定 |

---

> **本文件 = #52 随机事件导演。** 它**不生产内容,只调度** ——
> 内容归 **27 / 37** / 34 / 35 / 36 / 45 各自拥有。
> **P0 部分为定稿候选;P1a / P1b 的清单为提案,须逐条考据后落盘(支柱五)。**
