# === IDE and Editor ===
.vs/
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# === Claude Code Local ===
.claude/settings.local.json
CLAUDE.local.md
# Session audit trail — hook-generated. `**/` covers nested copies left behind
# when a hook ran from a subdirectory (see .claude/settings.json path anchoring).
**/production/session-logs/
# Ephemeral session checkpoint — see .claude/docs/directory-structure.md.
production/session-state/active.md
# Local agent-runner configs carry a plaintext ANTHROPIC_API_KEY. Never commit.
.claude/opencode*.json

# === Internal Planning (not for public) ===

expansions/

# === Marrow (eval tooling — never commit) ===
marrow/

# === Runtime Artifacts (auto-generated, start empty on fresh clone) ===
# Created on first use by /consistency-check and /architecture-review
docs/consistency-failures.md

# === Build Output ===
build/
builds/
bin/
obj/
out/
dist/
*.exe
*.dll
*.so
*.dylib
*.app

# === Engine: Godot ===
.godot/
*.translation
export_presets.cfg
# Uncomment if using Godot:
# *.import

# === Engine: Unity ===
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
*.csproj
*.unityproj
*.sln
*.slnx
# Unity 生成的 IDE 工作区产物(按需重建,不入库)。
# 注意:上方 `.vscode/settings.json` 带斜杠 ⇒ 根锚定,匹配不到 unity/.vscode/。
unity/.vscode/
# U1 spike 批临时资产(Setup 菜单生成,Teardown 清理;永不提交,见 production/u1-spike-checklist.md §4)。
# 同时匹配 .meta。
unity/Assets/Scenes/Spike*
# Addressables 编辑器生成物(Setup 菜单 / 首次初始化自动产出;归 ADR-014 正式批,永不提交 —— 见 u1-spike-checklist.md §4.0 第 5 步)。
# 含 addressables_content_state.bin 等机器相关产物;目录级忽略连同 .meta(含同级目录 meta)。
unity/Assets/AddressableAssetsData/
unity/Assets/AddressableAssetsData.meta
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
crashlytics-buildid.txt
sysinfo.txt
# Uncomment if using Unity:
# [Aa]ssets/[Aa]sset[Ss]tore[Tt]ools/

# === Engine: Unreal ===
Binaries/
DerivedDataCache/
Intermediate/
Saved/
*.VC.opendb
*.opensdf
*.sdf
*.cachefile
*.VC.db
# Uncomment if using Unreal:
# *.uasset
# *.umap

# === Compiled Shaders ===
*.spv
*.cso
*.dxbc

# === Temporary and Cache ===
*.tmp
*.temp
*.cache
*.log
__pycache__/
*.pyc
node_modules/
.npm/

# === Archives ===
*.zip
*.tar.gz
*.rar
*.7z

# === Secrets and Environment ===
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.json

# === Large Binary Assets (use Git LFS instead) ===
# Uncomment and configure with Git LFS for your project:
# *.png
# *.jpg
# *.jpeg
# *.wav
# *.mp3
# *.ogg
# *.fbx
# *.blend
# *.psd
# *.tga
# *.exr
# *.hdr

# === OS Generated ===
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk
