# Smoke — 2026-09-24

> **Story 008** · AC-21a-47(性能冒烟)· **ADVISORY**(非确定性计时,不入 BLOCKING 套件)

## AC-21a-47:物品表全量加载 + 单次配方求解 < 1 ms

| 项 | 值 |
|---|---|
| **阈值** | < 1 ms(GDD 原文) |
| **方法** | 【桌面】Unity Editor:先跑菜单「大医精诚/数据管线/烘焙 item-database」+「确保 data-core Addressables 组」;PlayMode 下 `DataCorePreloader.Preload()` 后计时 ① `provider.Load<ItemDataSet>()` 缓存命中与首次装载两态 ② 一次 `RecipeSettlementSolver` 求解调用;建议 `Stopwatch` 取 1000 次中位数 |
| **状态** | **NOT-RUN(【超算】无 Unity)** |
| **记录值** | 待桌面回填:首次装载 = ___ μs;缓存命中 = ___ μs;单次求解 = ___ μs |

- Edge:冷加载 vs 缓存命中分开记;E-13 失败路径另测硬失败,不与计时混测。
- 计时超阈 = 调优信号(非测试失败);结果回填本表后本条目闭环。
