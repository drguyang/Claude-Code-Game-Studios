# 战斗系统 Combat System —— 辨证施治

> **Status**: Draft (rev g 2026-09-12 · 旁观遭遇 · **M 二维表（轴关系 × 揭示）** · **主靶轴 / 次靶轴（R1）** · 阴阳两读 · 牌池涌现判定 · 资源按队员独立核算 · 切诊"不返回"口径 · §4.8 牌名「补液·支持」· §4.3a 泛例去「脉象」· **D6 / D9 / D10 已裁决并落地**)
> **Author**: game-designer（本轮草案）
> **Last Updated**: 2026-09-12
> **Last Verified**: 2026-09-12（据独立 Opus 复核修订 B1 / M1 / M2 / m1 / m2；rev e 仅改 §4.8 牌名；rev f 仅改 §4.3a 泛例措辞；**rev g 落地 D6 / D9 / D10 裁决与 R1–R4 新规则——本轮**改了数值**〔§4.8 三分支重算为六子支〕，与前两轮"数值冻结"不同，且已获用户对完整改动集的批准**）
> **Implements Pillar**: 医者仁心 · 四镜顿悟 · 外行即内行
> **Layer**: Core · **Priority**: MVP（卷 1–3 竖直切片必需） · **Key deps**: 成长系统（医道图谱）、状态/证候系统、关卡系统

---

## 1. 概述 Overview

战斗系统是本作对"治病"这一行为的下游机制化：玩家面对的不是敌人，而是**一场病**。场上有两类实体——**病机 Pathomechanism**（携带「病邪 Qi-Evil」条）与**病人 Patient**（携带「正气 Vital Qi」条）。玩家指挥由队员组成的医疗队，在**行动条（CTB）**时间轴上依次出手，用取自各自**方剂牌组**的牌去削减病邪。**病邪清零 = 治愈（胜）；正气归零 = 病人死亡（败）**——玩家可能因为"用药过猛"而输掉一场已经"指标正常"的战斗。系统同时承载两套医学认识论（还原论与整体论），并以"只靠一套必败"的结构迫使玩家两者并用。

> **快速索引** — 双资源：剂量 Dosage（还原）/ 气机 Qiji（整体）· 双条：病邪 / 正气 · 核心张力：治得快 vs 治得住。

---

## 2. 玩家幻想 Player Fantasy

**"我同时拿着显微镜和脉枕。"**

玩家要体验的，是一个医者在病床前的**双重专注**：一边用化验与检查锁定病原的身份（"是哪种菌、坏在哪个器官"），一边用望闻问切读出这个人此刻整个的状态（"他此刻是寒是热、是虚是实"）。成功的战斗有一种**侦探破案式的豁然**——证候对上的那一刻，牌力骤增，"啪"地一声，病邪退去。

同时要有**如履薄冰的责任感**：你手里的牌是药，也是刀。打错一张，病人的正气就掉一截；为了速战而滥用峻剂，可能赢下指标、输掉病人。玩家在胜负之间体验到的不是"打赢了"，而是**"我把他救回来了"**或**"我把他治死了"**。

**情绪目标**：从容辨析（侦探式）+ 责任重量（不可轻率）+ 顿悟快感（对证时牌力爆发）。

---

## 3. 详细规则 Detailed Rules

### 3.1 战场实体与胜负条件

| 实体 | 携带条 | 归零后果 | 备注 |
|---|---|---|---|
| 病机 Pathomechanism | 病邪 Qi-Evil（0–100 基准值） | 治愈（胜） | 一"敌"可含多个病机（多病邪条） |
| 病人 Patient | 正气 Vital Qi（0–100 基准值） | 死亡（败，立即判定） | 一"友"方病人，可为多人 |
| 医者 Healer | 无血条；有资源 | 无 | 牌组使用者 |

**胜负判定顺序（不可颠倒）**：
1. 若任一病人**正气 ≤ 0** → **立即判负**（即使同一结算中病邪也已归零）。
2. 否则，若所有病机**病邪 ≤ 0** → **判胜**。
3. 否则继续。

> 判定顺序即本作的核心道德命题：**"指标正常了，人没了"依然是失败。**

### 3.2 双资源（两套认识论）

| 资源 | 归属认识论 | 生成 | 过用后果 |
|---|---|---|---|
| **剂量 Dosage** | 还原论（西医） | 每回合基础回复 + 部分牌生成 | **耐药 Resistance**↑ → 同族药效衰减 |
| **气机 Qiji** | 整体论（中医/心身） | 每回合基础回复 + 部分牌生成 | **气机紊乱 Disruption**↑ → 回复效率衰减 |

- 每张牌标明其**费用类型**：还原牌耗剂量，整体牌耗气机。玩家**不能**用一种资源支付另一种的牌——这是"两套语言"的硬边界。
- **资源按队员各自独立核算**：每位在场医者持有**自己的**剂量池与气机池，出牌由**发牌者本人**支付，**不共享、不跨人支付**（见 `party-system.md` §3.2 / §3.6）。
- 资源上限与初始值由队员属性与图谱决定（见第 4、7 节）。

### 3.3 行动条 CTB（出手顺序）

- 每个实体（医者、病机）在行动条上拥有「行动值 ActionGauge」。
- 每时间刻 Δt：`ActionGauge += 速度 Speed × Δt`。
- 当 `ActionGauge ≥ 阈值 Threshold`（默认 1000）→ 该实体行动；行动后 `ActionGauge -= Threshold`。
- 同一刻多方同时达标 → 按 **速度降序** 结算；速度相同 → 按 **队伍位次**（医者优先于病机，遵守"救人先手"的主题）。
- 影响行动条的牌：**先手**（自身行动值 +Δ，提早上场）、**迟滞**（目标行动值 −Δ，推迟病机发作）、**回气**（提前一轮行动）等。

### 3.4 证候向量（辨证）

- 每个病机拥有一组**隐藏的「证候向量」**，取自八纲的四个轴，每个轴为**双极**：

| 轴 | 两极 | 说明 |
|---|---|---|
| 表里 | 表 / 里 | 病位浅深 |
| 寒热 | 寒 / 热 | 病性 |
| 虚实 | 虚 / 实 | 正邪消长 |
| 阴阳 | 阴 / 阳 | **总纲**——唯一有"两层"的轴：表层极（望，仅线索）/ 深层极（切，真正的证），见 3.5 |

- 每个轴带**强度** ∈ {1, 2, 3}（弱 / 中 / 强）。
- 证候对玩家**默认完全隐藏**；玩家必须用四诊逐轴揭示。

### 3.5 四诊（揭示手段）

| 诊法 | 耗费 | 揭示的轴 | 特性 |
|---|---|---|---|
| **望** Observe | 1 行动 | 表里、**阴阳表层极** | 稳定；对"表证/神色"最灵 |
| **闻** Listen/Smell | 1 行动 | 寒热 | 稳定；对气息、声音类病机必用 |
| **问** Ask | 1 行动 + 1 资源 | 寒热、虚实 | **90% 准确**——病人可能答错（见 5.8） |
| **切** Palpate | 1 行动 → **0 行动（被动）** | 虚实、**阴阳深层极** | 需医者具「气感」属性；无气感者**不可执行**（不返回深层阴阳，**非假值**） |

- **阴阳两层（本作核心机制之一）**：阴阳是唯一分"表层 / 深层"的轴（详见 `status-syndrome.md` §3.2）。
  - **望**给的是**表层极**——一条**线索，不是证**。**在切诊之前，阴阳轴按"未证"结算（M = 0.7）**，望得的表层极**不触发** M=1.5、**不触发**悖证。
  - **切**给的是**深层极**（由完整阴阳值决定，含病机固有倾向）——此后的阴阳轴才进入「已知」态，才参与 M 与悖证判定。
  - 普通病机两层一致（无破绽）；**心身 / 志怪类**病机两层可分叉（表层"阳"、深层"阴"），是单镜型玩法的侦探钩子。**纯还原阵容没有气感 → 切诊不可用 → 深层阴阳读不到 → 心身病机对它永久不可见**（此"缺眼"效果无需特判，由规则涌现，见 4.10.4）。
- **切诊被动（角色职能）**：队伍中若有**苏晚棠或陆九针**在场，切诊**不耗费行动**——辨证是中医角色的核心职能，应由他们承担操作负担。望 / 闻 / 问 仍耗 1 行动。此被动使"带对的人"在辨证节奏上获得实质收益，强化"聚散皆成长"。**注意**：切诊被动是"看谁在队"，与"谁有气感"是两回事——**能免费切 ≠ 人人能切**；无气感者即便免费也读不到深层阴阳。
- 每揭示一个轴，该轴进入「已知」态，牌结算时按已知真实值判定。
- **未揭示轴**：玩家出牌时该方向视为"未证 Unverified"，匹配系数 **M = 0.7**，**不伤正气**。惩罚只针对"无视已揭示信息的鲁莽"（见下），不针对"信息不足时的合理猜测"。
- **悖证是可预判的确定性惩罚，不含随机**：若某轴**已揭示**，而玩家打出与该轴真实极**相悖**方向的牌 → 悖证（M = 0.0，伤正气，见 4.3）。玩家永远能事先知道一张牌会不会伤到病人——**失误是选择的结果，不是运气的结果**。**对阴阳而言**："已揭示"仅指**深层极**已由切诊揭示；只有表层极时不算已揭示，**永不触发悖证**（见上）。

### 3.6 治法牌（八法）与匹配

- 治法牌对应中医**八法**，每张牌标注其**法**与**方向**（作用于哪个轴的哪一极）。

| 法 | 主作用 | 典型方向 |
|---|---|---|
| 汗 | 解表 | 表 |
| 吐 | 涌吐上焦实邪 | 里·上焦·实 |
| 下 | 攻下里实 | 里·实 |
| 和 | 调和半表半里 / 阴阳 | 阴阳·失和 |
| 温 | 温里散寒 | 寒 |
| 清 | 清热 | 热 |
| 消 | 消积散结 | 里·实（缓） |
| 补 | 扶正补虚 | 虚 |

- 牌与证候的匹配关系决定效力与是否伤正（见 4.3）。

**★ R1（本轮新增）· 每张牌必须声明「主靶轴」与「次靶轴」**：

| 字段 | 作用 | 是否影响 `M` |
|---|---|---|
| **`mainAxis` 主靶轴**（单值，形如 `虚实:虚`） | **M 的唯一决定轴**（见 §4.3） | **是**——`M = M(mainAxis)` |
| **`subAxes` 次靶轴**（0–n 个） | **只贡献悖证**（该轴已揭示且相悖 → 触发 §4.3b 伤正） | **否**——**不加成 `M`** |

- **为什么必须显式声明**：`M` 的判定**只能有一个输入轴**（§4.2 的公式里 `M` 是标量）。多轴牌若不定主次，`M` 就无从取值——这正是 **D9** 的根因（详见 §4.3 D6 / D9 裁决）。
- **守卫**：`status-syndrome.md` §5 已要求"多轴牌的 `M` 只取主靶轴"；本条把该要求的**数据前提**（牌必须有主靶轴）钉进战斗侧。
- **★ R3 · 卷一教学牌「补液·支持」的轴声明**（原「强心剂」，2026-09-12 更名；**更名与本次轴声明均不改 `P` / 资源类型**）：

  | 字段 | 值 |
  |---|---|
  | 资源 | 剂量（还原） |
  | `P` | 35 |
  | **`mainAxis`** | **`虚实:虚`** |
  | `subAxes` | `[表里:里]` |
  | `no_resistance` | `true`（**本轮新增，见 §4.5**） |

  - **`mainAxis = 虚实:虚` 是 D9 的裁决结果**：卷一的 `虚实` **可由章 B 的探索节点 `ex_v1_m_4` 揭示**（见 `design/levels/vol-01.md` §3.4），故打出「补液·支持」**可能**进入 `M = 1.5` 对证档——这是**卷一唯一**能让玩家亲身体会"对证 → 牌力骤增"的通道（详见 §4.3 D9 裁决）。若改取 `mainAxis = 表里:里`，则卷一永不揭示 `表里`、`M` 恒为 `0.7`，**卷一将不存在任何 `M = 1.5` 时刻**——玩家体会不到对证的收益。

### 3.7 峻剂（风险回报）

- 部分牌为**峻剂 Drastic**（猛药）。打出时**主动**以病人正气换取更高病邪削减。
- 峻剂对病人正气造成固定损耗，换取约 **3:1** 的病邪收益（见 4.4）。
- 峻剂是**玩家主动选择**的风险，与"错治惩罚"（被动）并列为两条伤害正气的路径。

### 3.8 合方 / 合击（汇通连线技）

- 战斗中的最强牌来自成长系统的**《杏林汇通录》跨树连线**（见 `design/gdd/progression-system.md`）。
- 一条"通" = 一张**合方牌**：同时具备还原与整体两种方向，可一次性处理"病邪削减 + 正气维护"。
- **费用"双付"**：合方牌由**两端队员各付其对应侧资源的一半**（见 `party-system.md` §3.6）；若一端为"遗志"（殉职者）或不在场，则费用全由在场发起方承担。合方牌要求**两端队员均在本场出战**。
- 合方牌享受**汇通连线加成 S_link**（见 4.6），且**不受**单侧耐药/紊乱影响（因为它换用了新的认识论）。

### 3.9 回合流程

1. **诊段**：战斗开始，证候全隐藏；玩家可先手投入四诊行动。
2. **辨段**：玩家依据已揭示轴，决定以哪套认识论为主、出哪一法的牌。
3. **治段**：CTB 推进，交替出牌→病机行动（病机行动通常**加剧病邪**或**损耗病人正气**，视病机类型）。
4. **收束**：判定胜负；结算战斗评级（治愈/代价/是否留后遗症）。

**状态机（病机）**：

| 状态 | 进入条件 | 退出条件 | 行为 |
|---|---|---|---|
| 潜伏 Latent | 战斗开始、未揭示 | 任一轴被揭示 | 不行动或弱行动 |
| 发难 Active | 被揭示 / 回合数达标 | 病邪清零或传变 | 正常行动 |
| 传变 Shifting | 回合数达到传变点 | 传变完成（1–2 回合） | 重组证候向量（见 5.4） |
| 势穷 Spent | 病邪 ≤ 20% | — | 行动减弱（濒死反扑可选） |
| 已瘥 Resolved | 病邪 ≤ 0 | — | 移出战场 |

### 3.10 旁观遭遇 Spectator Encounter

一种**非交互的战斗演出**：完整的"诊 → 辨 → 治"由**固定脚本**驱动，**玩家零输入**。用于教学铺垫（卷一 A-2：旁观老郎中治病，见 `design/levels/vol-01.md` §2.4）与剧情演出（如卷二"回看"陆九针救母）。

- **不实例化战斗循环**：旁观遭遇**不进入 CTB**、不抽牌、不结算 `ΔE`、不触发耐药 / 紊乱 / 传变。它的三段 = `§3.9` 的 **诊段 / 辨段 / 治段**——故其"段"是**叙事节拍**，**不是** `§4.1` 的行动回合；**术语不得混用**（§4.1 的"回合"专指 CTB 行动序）。
- **不产出评级与 IP**：不结算 `Rating`（`§4.7`）、不发放顿悟点（`§4.5` 的 `IP_gain` 不发放）；不计入成就、战斗统计、连胜 / 连携数据。
- **UI 为叙事模式**：**不显示**病邪条、正气条、行动条、证候向量、`M` 值、`ΔE`、牌名、资源消耗。只呈现"动作 + 病人状态变化 + 角色独白"（理由：教学期"不给机制解释、不弹数值"，见 `design/levels/vol-01.md` §1.3）。
- **恒不可失败**：`failMode = none`，脚本保证演出必然走到"治愈"。切片"正气下限钳到 1"（`level-system.md` §3.6）在此**不适用**——病机**根本不进入行动循环**，没有任何一方能掉血。
- **脚本中的病机数据是描述性的**：`syndrome` / `revealed` / `behavior` 等字段仅用于**渲染与叙事一致性**，**不参与任何结算**；但其取值仍须**合法**——`behavior` 必须是 `status-syndrome.md` §3.3 的合法枚举（用 `ampPerTurn = 0` / `drainPerTurn = 0` 表达"不出手"，**不得发明新枚举值**），`syndrome.yinyang` 必须是 `§3.2` 的**两层结构** `{ surface, deep, innate }`。**数据合法性不因"不结算"而豁免。**
- **可跳过**：`skipAllowed = true` 时玩家可整段跳过；跳过**不改变**用于**推进链**的 flag（链完整性优先），但**另产出一个"是否真看过"的 flag** 供下游叙事分支（`level-system.md` §3.2 `Encounter.yields`）。

**示例**（卷一 A-2 `spect_v1_old_doctor`）：老郎中 · 镇上妇人；病机「风寒束表」；三节拍 = 诊（望 / 问 / 切）→ 辨（独白）→ 治（桂枝汤，**汗法**解表），病邪 60 → 0。**本行只写叙事事实，不写 `M` 与 `ΔE`**——旁观遭遇不结算 `ΔE`（本 §），且此刻 `revealed = ∅`，按 §4.3 二维表取不到 `M = 1.5`。（同口径见 `design/levels/vol-01.md` §2.4 ②。）

> **口径（勿踩）**：`§3.5` 规定**望**只给阴阳**表层极**（线索，**不入**「已知」）；**阴阳深层极须由切诊揭示**。故脚本若宣称"四轴已揭示"，**必须**包含**切诊**。本例脚本**含切诊**（"深层极由切揭示"在叙事上成立），**但旁观遭遇不结算 `M`**——`M` 只在真实战斗里算（§4.3）。**不得**把"望得表层"说成"阴阳已证"，**也**不得在旁观遭遇里写 `M` / `ΔE` 数值。

---

## 4. 公式 Formulas

### 4.1 行动序（CTB）

```
下一行动时刻 T_next = (Threshold − ActionGauge) / Speed
```

| 变量 | 类型 | 范围 | 来源 | 说明 |
|---|---|---|---|---|
| Threshold | int | 800–1200（默认 1000） | 全局配置 | 行动条阈值 |
| ActionGauge | int | 0 – Threshold | 运行时 | 当前行动值 |
| Speed | int | 40–150 | 单位属性 | 医者 80–140；急性病机 100–150；慢性病机 40–80 |

**示例**：病机 A 速度 120，医者 B 速度 100，ActionGauge 均为 0。
- T_A = 1000/120 ≈ **8.33 刻**；T_B = 1000/100 = **10 刻**。→ 病机先手。
- 若医者先出一张「先手」牌使自身 ActionGauge += 300，则 T_B = 700/100 = **7 刻** → 医者反先。

**预期输出范围**：单次行动间隔 6.7–25 刻。

### 4.2 病邪削减 Qi-Evil Reduction

```
ΔE = P × M × (1 + Σb) × T_drug × S_link
```

| 变量 | 类型 | 范围 | 来源 | 说明 |
|---|---|---|---|---|
| P | float | 10–60 | 牌面数据 | 基础效力 |
| M | float | 0.0–1.5 | 见 4.3 | 匹配系数 |
| Σb | float | 0.0–1.5 | 加成和（图谱、士气、连携） | 各项加成之和 |
| T_drug | float | 0.25–1.0 | 见 4.5 | 耐药/紊乱衰减 |
| S_link | float | 1.0–1.6 | 见 4.6 | 汇通连线加成（仅合方牌 ≠1） |

**示例**：P=40 的「清」法牌，对证（寒热轴=热，M=1.5），无加成（Σb=0），首用（T_drug=1.0），非合方（S_link=1.0）：
`ΔE = 40 × 1.5 × 1 × 1 × 1 = 60`。一次打掉 100 病邪条的 60%。

**预期输出范围**：单牌 ΔE ∈ [0, 约 200]（含合方与满加成上限）。

**★ R4（本轮新增）· `ΔE` 逐牌取整**：`ΔE` 的乘积**逐张牌四舍五入到整数**后，再从病邪条上扣除。理由：病邪条是**整数**资源，余量永远以整数结算；不取整会让"是否打得完"依赖浮点末位，玩家无法预判。

- **取整时点**：**每一张牌各自取整**（不是把整回合的多张牌求和后一次性取整）。
- **卷一即用到**：`35 × 0.7 = 24.5 → 25`；`35 × 1.5 = 52.5 → 53`（见 §4.8）。
- **★ 边界必须钉死**：`§4.8` 乙 a 恰好坐在临界上——`4 × 25 = 100`（第 5 回合清零、正气 58、`Rating 0.481`）对 `4 × 24.5 = 98`（第 6 回合清零、正气 48、`Rating 0.436`）。**取整方向不同，结论会跳到另一支**。本条锁定为**四舍五入**。

### 4.3 匹配系数 MatchCoefficient

**唯一真源声明**：下面的**二维表**是 M 判定的**唯一真源**，并由 **R1–R4**（§3.6 / §4.2 / 本条）补全机械判据。`status-syndrome.md` §4.3 的小标题写着"（引用 `combat-system.md` §4.3）"，但其现存 5 行旧式表述与本表**冲突**（它把"相关 1.0"写成**依赖揭示**）——**rev g 已把该文件纳入改动集并同步修订**（见 `status-syndrome.md` §3.5 / §4.3）。

| 牌轴与致病轴的**客观关系** | 该轴**未揭示** | 该轴**已揭示** |
|---|---|---|
| **同轴 · 方向相符** | **0.7**（未证，不伤正） | **1.5**（对证，不伤正） |
| **同轴 · 方向相反** | **0.7**（未证，不伤正） | **0.0**（悖证，**伤正**，见 §4.3b） |
| **相关轴**（轴不同，但逻辑相邻、成立） | **1.0** | **1.0** |
| **无关轴** | **0.5** | **0.5** |

- **两个维度**：**轴关系**（同轴 / 相关轴 / 无关轴——牌轴与病机致病轴的**客观**关系，是病机自身的**事实**，玩家未读也知道自己在打哪根轴）**× 揭示状态**（该轴可否由四诊读出）。
- **只有「同轴」这一列受揭示影响**：方向未明时（未揭示）一律 **0.7**；方向已明时（已揭示）才分 1.5 / 0.0。
- **相关轴 1.0 与无关轴 0.5 不依赖揭示**：它们是客观轴关系，不是"已知信息"的判定对象。
- **推论（核心承诺）**：**未揭示的牌永远落在 0.7 / 1.0 / 0.5 三档，绝不可能触发悖证 0.0**——0.0 的前置就是"已揭示"。故玩家**永远能事先知道牌会不会伤人**。

> **相关轴的真源**：哪些轴算"某致病轴的相关轴"，由 `status-syndrome.md` §3.5「八法 × 证候轴 对应表」的 **`相关轴（M=1.0）`** 列给出（如 汗→阴阳:阳；温→阴阳:阴 / 虚实:虚；清→阴阳:阳 / 虚实:实；消→表里:里 / 寒热:寒）。该表以**法**为键，故**法牌**（整体树）可直接查得；**还原牌**的轴关系须按牌轴与致病轴的客观邻接判断。

> **关键差异（第二轮修订）**：悖证**只在轴已揭示时**才可能发生——即玩家**明知**证候是"寒"却打出"清热"牌。未揭示轴出牌永远只是效力打折（M=0.7），**绝不随机伤人**。这一改动消除了"凭合理猜测却被概率惩罚"的挫败，把惩罚严格绑定到"无视已知信息的鲁莽"这一**可预判、可归因**的行为上。

> **阴阳特例（本轮修订）**：阴阳是唯一分层的轴。**"已揭示"仅指深层极已由切诊揭示**；只望得表层极不算"已揭示"，其出牌按 M=0.7 结算、**永不触发悖证**。原理见 §3.5 与 `status-syndrome.md` §3.2.2——守护"玩家永远能事先知道牌会不会伤人"的承诺。

> **★ R2（本轮新增，承本表表头）· 「相关轴」以「轴不同」为必要条件**：本表的表头把相关轴定义为"**轴不同**，但逻辑相邻、成立"。该条**机械化**后即为 R2：若一张牌的**全部轴**（`mainAxis` ∪ `subAxes`）**都落在病机的致病轴集合内**，则该牌与致病轴**同轴**，**不得**判为相关轴。**推论**：「补液·支持」（`虚实:虚` / `表里:里`）对病机「寒饮伏肺」（致病轴 `表里:里` + `虚实:虚`）**不构成相关轴**——它的两支都在致病轴集合里。

> **★ 已裁决 D6（「补液·支持」的 `M` 归属；2026-09-12 登记，07 同日裁决为 **(甲)**；原名「强心剂」，2026-09-12 更名）**
>
> **原争议**：`§4.8` 回合 2 曾给「补液·支持」在所有轴未证下 `M = 1.0`，解释为"相关"。但该牌轴 ∈ 该关致病轴集合 → 按本表应落 **「同轴 · 未揭示」= 0.7**。
> **裁决 = (甲)**：**承认 `M = 0.7`**（未揭示时）**/ `M = 1.5`**（`虚实` 已揭示且为虚时），`1.0` 读法**作废**。
> **否决 (乙) 的理由**：(乙) 要求"致病轴 = 最强轴、其余轴作相关轴"的新判据，**与 R2 直接冲突**（R2 规定轴相同即为同轴），且该判据会**永久剥夺** `补虚` 对虚证应得的对证 `1.5` 档，与 §2 玩家幻想（"辨证 → 牌力兑现"）相悖。
> **落地**：`§4.8` 回合 2 起全表重算（三分支 × `虚实` 两态 = **六子支**）；「补液·支持」的轴声明见 **§3.6 R3**。

> **★ 已裁决 D9（多轴牌的 `mainAxis` 归属 —— D6 的真根因；2026-09-12 裁决为 (甲)）**
>
> **原争议**：牌的数据模型此前**未声明主靶轴**（§3.6 的 R1 是本次补的）。「补液·支持」有两支（`虚实:虚` / `表里:里`），`M` 取哪一支从未写死。
> **裁决 = (甲)**：`mainAxis = 虚实:虚`（**理由见 §3.6 R3 末段**：这是卷一唯一能出现 `M = 1.5` 的通道）。
> **被否决的另一解**：`mainAxis = 表里:里` —— 会让 D9 整体消失（卷一永不揭示 `表里` → `M` 恒 `0.7`、结论表回到 3 行），但**代价是卷一没有任何 `M = 1.5` 时刻**，玩家在整卷里看不到"对证"的回报。**这是本裁决最需要用户复核的设计取舍**（用户已于 2026-09-12 复核并选择 `虚实:虚`）。

**4.3c 伤正（ΔV_wrong）的触发条件（本轮补全 · 原二维表无法表达）**

原二维表只给出 `M` 的取值，**未定义 `ΔV_wrong` 何时触发**。补全如下——**两者是独立的判定**，`M = 0` 不必然带伤正，伤正也不必然要求 `M = 0`：

```
触发 ΔV_wrong  ⟺  (同轴 · 已揭示 · 方向相反)          // 主靶轴相悖 → M = 0.0 且伤正
                ∨  (任一次靶轴 已揭示 且 相悖)          // 2026-09-12 新增：次靶轴相悖 → M 不受影响，但伤正
```

- **新增第二条的理由**：`status-syndrome.md` §3.5 规定"次靶轴只在**已揭示且相悖**时触发悖证伤害，**不加成 M**"——但原二维表**只按主靶轴的轴关系定 `M` 与伤正**，表达的永远是"伤正 ⟺ `M = 0`"，与 §3.5 冲突。本条把 `status-syndrome.md` §3.5 已定的规则**落进战斗侧的真源**。
- **卷一不触发**：「补液·支持」的次靶轴是 `表里:里`，与该关致病轴的 `表里:里` **方向相符**，不构成相悖。
- **卷二起会触发**：和 / 下 / 消 / 吐等牌的次靶轴在特定病机下可能相悖。


**4.3a 悖证反馈文本（让玩家知道为何被罚）**

悖证触发时，不只给红色数值，还给一句**角色口吻的即时反馈**，让玩家理解因果、而非感觉遭遇随机厄运：

> （沈砚之）："……清热？**可我知道她是寒。**这张牌，我打反了。"

> **★ rev f 修订（2026-09-12）**：本句原写"可**脉象**明明是寒"——但**卷一沈砚之不可执行切诊**（`design/levels/vol-01.md` §1.1：无 `sense.qi`），他**拿不到脉象**。已改为"可我知道她是寒"：此处"寒"的**来源是探索节点的物证**（`ex_v1_m_1` / `ex_v1_m_3`），不是切诊。`design/levels/vol-01.md` §3.5 的悖证反馈以此为准。

> （陆九针）："证在里、在寒，你却用清法——这不是治病，是添病咯。"

反馈文本按"发牌者是谁"动态选择；玩家可据此在下回合修正（而无需重开）。**教学期优先给最直白的一句因果，不堆术语。**

**4.3b 错治伤正**：

```
ΔV_wrong = C_base × (1 + Overdose) × (1 / 1 + B)
```

| 变量 | 类型 | 范围 | 说明 |
|---|---|---|---|
| C_base | float | 10–15（默认 12） | 基础错治损耗 |
| Overdose | float | 0 或 0.5 | 是否猛药；猛药错治更重 |
| B | float | 0–10 | 病人虚衰度（正气已低时更脆弱） |

**示例**：C_base=12，普通牌误投（Overdose=0），病人虚衰 B=0 → `ΔV_wrong = 12`。若为猛药（Overdose=0.5）→ 18。

### 4.4 峻剂换血（主动交易）

```
ΔE_drastic = P × M × K_drastic
ΔV_drastic = P × C_drastic

换血比 ExchangeRatio = ΔE_drastic / ΔV_drastic = (M × K_drastic) / C_drastic
```

| 变量 | 范围 | 默认 | 说明 |
|---|---|---|---|
| K_drastic | 2.0–3.0 | 2.5 | 峻剂病邪增益 |
| C_drastic | 0.6–1.0 | 0.8 | 峻剂正气损耗系数 |

**示例（对证峻剂）**：P=40，M=1.5 → ΔE = 40×1.5×2.5 = **150**；ΔV = 40×0.8 = **32**。换血比 ≈ **4.7:1**（对证时更划算）。
**示例（未证峻剂）**：M=0.7 → ΔE=70，ΔV=32 → 2.2:1。**教玩家：先辨证，再下猛药。**

### 4.5 耐药与紊乱（过用衰减）

```
还原牌： T_drug = 1 / (1 + R × k_R)
整体牌： T_drug = 1 / (1 + D × k_D)
```

| 变量 | 范围 | 默认 | 说明 |
|---|---|---|---|
| R 耐药 Resistance | 0–12 | — | 同一药族本场使用次数累加 +1 |
| D 紊乱 Disruption | 0–12 | — | 同一"法"在**非对证**下使用 +1 |
| k_R | 0.15–0.35 | 0.25 | 耐药斜率 |
| k_D | 0.10–0.30 | 0.20 | 紊乱斜率 |

**示例**：同族还原药连用 4 次 → T = 1/(1+4×0.25) = **0.5**；连用 8 次 → **0.33**。

**★ 豁免：`no_resistance: true`（本轮新增，承 D11）**

- 牌可声明 **`no_resistance: true`**，则该牌**永不累加 `R` / `D`**，恒取 `T_drug = 1.0`。
- **「补液·支持」即此类**：**生理盐水不产生药理耐受**——同一药族连用不会"越用越不灵"。这是**医学身份**判断（39 行的专家受众首战不能出戏），不是数值调参。
- **D11 的由来（已裁决）**：`§4.5` 规定同族还原药连用 4 次 → `T_drug = 0.5`，但此前 **`T_drug` 从未进入任何一次演算**——`§4.8` 与 `design/levels/vol-01.md` §3.5 都按 `T_drug = 1.0` 打出 4–5 张同类牌。若照 §4.5 严格执行，五张牌的实际总量只有 24.5+19.6+16.3+14.0+12.3 = **88.5 < 100**，丙 a / 乙 a **根本打不完**（需 8 回合、正气落到 40、`Rating ≈ 0.43`），"3–5 回合"的**教学节奏整体崩塌**。
- **裁决（2026-09-12，用户选）**：给「补液·支持」加 `no_resistance: true`——**六子支数值照 §4.8 原样成立**，教学节奏不动；其余还原药（洋地黄类、解热镇痛类等**确实会产生耐受**的药）**仍按 §4.5 累加**。
- **附注**：`§4.5` 的"连用 4 次 → 0.5"隐含 `R` 的**初值为 0**（首用 `T_drug = 1.0`，与 §4.2 示例一致）——此处**明写**，消除"连用 4 次是第 4 次还是第 5 次"的歧义。

### 4.6 汇通连线加成（来自成长系统）

```
S_link = 1 + (LinkCount × 0.15) + AffinityBonus
```

| 变量 | 范围 | 说明 |
|---|---|---|
| LinkCount | 0–4 | 本场合方牌生效的连线数 |
| AffinityBonus | 0–0.20 | 亲缘系数超限部分（见 progression GDD 4.3） |

**预期输出范围**：S_link ∈ [1.0, 1.6]。

### 4.7 战斗评级（结算）

```
Rating = w1 × (1 − 病邪残余率) + w2 × (正气保有率) + w3 × (回合效率) − 罚项
```

| 权重 | 默认 | 罚项示例 |
|---|---|---|
| w1 | 0.35 | 有"后遗症"（正气 < 30% 收场）→ −0.15 |
| w2 | 0.45 | 使用峻剂次数 × 0.02 |
| w3 | 0.20 | 错治次数 × 0.03 |
| — | — | **单核取胜**（本场未动用两侧资源出牌，走 §4.10 速攻型 / 持守型）→ **−0.20**（"缺一只眼睛"的代价，见 4.10.6） |

评级影响顿悟点奖励（见 progression GDD 4.5）。

### 4.8 完整战斗演算示例（教学关：卷一"母亲的病"）

**进入本关的前置引导（教学铺垫）**：

1. **序章·旁观遭遇（`§3.10`，无机制解释）**：在卷一战斗之前，安排一次**旁观遭遇**（类型定义见 `§3.10`，章层编排见 `level-system.md` §3.2.1 `Encounter`）——玩家**独自**在镇上看见老郎中给一位镇上妇人治病，全程**零输入**。老郎中不问"你得了什么病"，只问："怕冷还是怕热？""夜里咳得凶，还是天明咳得凶？"**此处不给任何机制解释、不弹任何数值、不显示任何条**，只让玩家**看见**"中医是这样问问题的"。玩家离场时心里带着一个模糊的参照系，为后面"我连她冷不冷都没问"的顿悟埋下引信。完整谱面见 `design/levels/vol-01.md` §2.4。〔实施须与 `story-bible.md` 序章 / 卷一同源对齐。〕

2. **悖证失败后的教学反馈（角色独白，非数值弹窗）**：**分支乙**（明知寒证却用清法）触发错治伤正时，不弹"−12 正气"的冷冰冰数字，而是插入沈砚之的内心独白：

   > "退热……可她不热。她冷。我连她冷不冷都没问。"

   **用角色的声音教玩家犯错在哪，而非用系统的声音宣布他扣了血。**

**设定**：病人（周慧兰）正气 100；病机「寒饮伏肺」病邪 100，证候向量 = {表里: 里(2)，寒热: 寒(3)，虚实: 虚(1)，阴阳: {表层 阴(2)，深层 阴(2)，固有 0}}。玩家此刻**只有还原牌**（教学期），**队伍仅沈砚之**（卷一无可执行切诊）。

**出手节奏（全篇统一）**：病机**第 1–2 回合不出手**（潜伏），**自第 3 回合起每回合损耗正气 −10**；胜负判定在**每回合收束**（§3.1）。

**本关的两个"揭示开关"**（决定 §4.3 二维表落哪一格；均来自 `design/levels/vol-01.md` §3.4 的探索节点）：

| 开关 | 揭示手段 | 影响哪张牌 |
|---|---|---|
| **`寒热` 轴** | 章 B 探索节点的**物证**（`ex_v1_m_1` / `ex_v1_m_3`） | 「退热剂」（`mainAxis = 寒热`）——决定它是否**悖证** |
| **`虚实` 轴** | 章 B 探索节点 **`ex_v1_m_4`** | 「补液·支持」（`mainAxis = 虚实:虚`，§3.6 R3）——决定它落 `0.7` 还是 `1.5` |

> 故下文三支（甲 / 乙 / 丙，按**对 `寒热` 的态度**分）**各再分两态**：后缀 **`a` = `虚实` 未揭示**、**`b` = `虚实` 已揭示（= 虚）**。**共六个子支。**

**回合 1（诊段）**：玩家无四诊牌（卷一尚未习得整体树），只能凭"症状"猜。此处有**两条互斥分支**，取决于玩家是否在章 B 的探索节点里问过冷热（`design/levels/vol-01.md` §3.4）——**关键**：**悖证只在寒热轴已揭示时才可能发生**（§4.3 二维表「同轴 · 已揭示 · 方向相反」）。

- **分支甲 · 寒热轴未揭**（没问过冷热）：出「退热剂」（还原，P=30，方向=热）。寒热轴**未揭示**，方向无从判定 → 命中 §4.3 二维表「同轴 · 未揭示」→ **未证 `M = 0.7`**，**不伤正气**：`ΔE = 30 × 0.7 × 1 × 1 × 1 = 21`。
  - 病邪 100 → **79**；正气不变（**100**）。
  - **教学信号**：牌打出去了，却**"打不动"**——玩家第一次意识到**自己缺的不是药，是信息**。
- **分支乙 · 寒热轴已揭**（已在探索节点问得"怕冷"）：仍出「退热剂」（方向=热）即**明知证寒而用清法** → 命中 §4.3 二维表「同轴 · 已揭示 · 方向相反」→ **悖证 `M = 0.0`**，并触发错治：`ΔV_wrong = 12 × 1 × (1/1) = 12`。
  - 病邪不变（**100**）；病人正气 100 → **88**。
  - **教学信号**：玩家第一次领教"方向错了会伤人"——而**这一次是他自己选择无视已知信息的**。失误是选择的结果，不是运气的结果（§4.3）。
- **分支丙 · 寒热轴已揭且用对了牌**：**回合 1 起即出「补液·支持」**（不与寒热轴相悖），故不触发悖证；其病邪轨迹与下述 `a` / `b` 两态一致（`a`: 100 → 75；`b`: 100 → 47）。丙 是 `design/levels/vol-01.md` §3.5 的三分支持有支。

**回合 2 起（治段）**：玩家**改出 / 续出「补液·支持」**（还原，P=35，**`mainAxis = 虚实:虚`**）。**D6 / D9 已裁决**（§4.3）：该牌与本关致病轴**同轴**（R2），`M` **只由 `虚实` 轴的揭示状态决定**：

| `虚实` 轴状态 | 命中二维表 | `M` | `ΔE = 35 × M`（**R4 逐牌四舍五入**） |
|---|---|---|---|
| **未揭示**（未探 `ex_v1_m_4`）→ 后缀 `a` | 同轴 · 未揭示 | **0.7** | `35 × 0.7 = 24.5 → **25**` |
| **已揭示 = 虚**（已探 `ex_v1_m_4`）→ 后缀 `b` | 同轴 · 已揭示 · 方向相符 | **1.5** | `35 × 1.5 = 52.5 → **53**` |

**六子支全程轨迹**（病机第 1–2 回合潜伏，**自第 3 回合起每回合 −10 正气**；清零回合的病机**仍先出手**，胜负在回合收束判定——`§3.1`）：

| 支 | 出牌序列 | 病邪轨迹 | 清零回合 | 病机出手 | 收束正气 |
|---|---|---|---|---|---|
| **甲a** | 退热 ×1 → 补液 ×4 | 100 → 79 → 54 → 29 → 4 → **0** | **5** | 3 次（回合 3/4/5） | **70** |
| **甲b** | 退热 ×1 → 补液 ×2 | 100 → 79 → 26 → **0** | **3** | 1 次（回合 3） | **90** |
| **乙a** | 退热 ×1（**悖证**）→ 补液 ×4 | 100 → 75 → 50 → 25 → **0** | **5** | 3 次 | **58** |
| **乙b** | 退热 ×1（**悖证**）→ 补液 ×2 | 100 → 47 → **0** | **3** | 1 次 | **78** |
| **丙a** | 补液 ×4 | 100 → 75 → 50 → 25 → **0** | **4** | 2 次（回合 3/4） | **80** |
| **丙b** | 补液 ×2 | 100 → 47 → **0** | **2** | 0 次 | **100** |

> **★ 数值巧合警告**：新的 **丙a = 0.610** 恰等于**旧表**里甲的数、新的 **甲b = 0.655** 恰等于**旧表**里丙的数。**不要**把重算前后的同名数字读成"没变"——本轮的六个数**全部是新算的**（旧表是三分支 + `M = 1.0`，新表是六子支 + `M = 0.7 / 1.5`）。

**结论对照**：同一场病，玩家对"已知信息"的态度，代价差得很清楚——

| 支 | 态度（`寒热` / `虚实`） | 清零回合 | 收束正气 | **`Rating`** | 一句话 |
|---|---|---|---|---|---|
| **丙b** | 已探 / 已探 | 2 | **100** | **0.700** | 两把钥匙都拿到，**最快、不掉一点正气** |
| **甲b** | 未探 / 已探 | 3 | **90** | **0.655** | 只拿到 `虚实` 这把钥匙，也很快 |
| **丙a** | 已探 / 未探 | 4 | **80** | **0.610** | 拿到 `寒热` 钥匙，**避开了悖证** |
| **乙b** | 已探仍打反 / 已探 | 3 | **78** | **0.571** | 回合 1 白掉 12 正气 |
| **甲a** | 未探 / 未探 | 5 | **70** | **0.565** | 两把钥匙都没有，**进度最慢**（但不伤正） |
| **乙a** | 已探仍打反 / 未探 | 5 | **58** | **0.481** | 最差：把已知信息用反，还打不快 |

这正是系统要逼玩家体会的核心命题：**同样的药，差别在有没有先看。**

`Rating` 的构成（§4.7；全部六支的 病邪残余率 = 0、**回合效率 = 0.5**、**单核罚项 −0.20**〔纯还原牌池，§4.10.2〕）：

```
Rating = 0.35 × 1 + 0.45 × (正气 / 100) + 0.20 × 0.5 − 0.20 − 0.03 × (错治次数)
乙支额外 −0.03（回合 1 一次错治）
```

> **★ D12（本轮登记）· 「回合效率」在 §4.7 只有权重、没有公式**：上表六支的 `Rating` **全部建立在"回合效率 = 0.5"这一假定**之上（六支同取 0.5，故差异只来自正气保有率）。这不是巧合，是 §4.7 的**缺陷**——它给了 `w3 = 0.20`，却从未定义「回合效率」怎么算。**在本条被裁决前，本节六个 `Rating` 只能在"回合效率 = 0.5"的口径下成立**（对**横向比较**无害：六支同口径；对**绝对值**有影响）。

- **丙 > 甲 > 乙 在同一 `虚实` 态内始终成立**：`丙b 0.700 > 甲b 0.655 > 乙b 0.571`；`丙a 0.610 > 甲a 0.565 > 乙a 0.481`。**"先看再打"的收益是真的。**
- **"明知故犯"的恒定代价 = 0.084**（= `0.45 × 12/100` 的悖证伤正 `0.054` + 错治罚 `0.03`）：`甲a − 乙a = 0.565 − 0.481 = 0.084`，`甲b − 乙b = 0.655 − 0.571 = 0.084`。**该值与 `M` 取 `0.7` 还是 `1.5` 无关**——这是本表最硬的一条性质：**惩罚绑定在"用错方向"这一行为上，不随牌力波动。**
- **`虚实` 的探索价值 = +0.09**（`(100−70) − (90−80)` 折成评级：`0.45 × 0.20 = 0.090`），**是 `寒热` 探索价值（+0.045）的两倍**——因为 `虚实` 直接把 `M` 从 `0.7` 抬到 `1.5`，而 `寒热` 只避免一次悖证。

> **★ D13（本轮登记，未解决 · 原临时编号 N6）· 排序不再是单维的**：上表产出 **甲b（0.655）> 丙a（0.610）**——即"探到 `虚实` 但没探 `寒热`"**优于**"探到 `寒热` 但没探 `虚实`"。这与 `design/levels/vol-01.md` §1.3 的**教学弧**（`寒热` 才是一级教学的主角）以及 §3.6 的**转归文本只按 `寒热` 分叉**相冲突（甲b 的玩家手里有 `虚实` 物证，却拿到甲的"我说不清为什么"）。**本轮照裁决落地，冲突登记为 D13**（见 `vol-01.md` §6）。

> **★ N4（本轮已关闭）· `ex_v1_m_4` 的可达窗口未定义**：`ex_v1_m_4` 的前置是 `history_told`（章 B Q1 的产出），而 `design/levels/vol-01.md` §1.2 把本卷写成**单向链**、§2.5 又说探索节点**自由顺序**。若不在 Q1 与 T1 之间定义一段**自由探索窗口**，`ex_v1_m_4` 就**不可达**——`b` 列（3 个 `1.5` 子支）在实机上永不出现，D9 的分叉**只是纸面的**。**已解决**：`design/gdd/level-system.md` §3.4 新增该窗口（配置期强校验），§4.2 补其补全路径校验；本文件 §6 亦已把关卡系统行改为**双向**依赖。

**评级示例**（§4.7 公式的泛例，非本表某一支）：设病邪残余率 0、正气保有率 0.9、回合效率 0.7、无罚项 → `Rating = 0.35×1 + 0.45×0.9 + 0.20×0.7 = 0.35+0.405+0.14 = 0.895`。

### 4.9 特殊战斗设计：霍崇光终局战（卷九）

> 本场是全篇的**终极 Boss 战**，也是主题"两套认识论"的最后清算。它不是一场"打怪"，是一场**认识论的辩论被做成了战斗**。

**人设绑定**：霍崇光 = 还原论极端（"纯医学"、数据至上），见 `story-bible.md` 6.7。他不携带"病邪条"式的血肉，而是**将自己作为"一个绝对正确的体系"来运转**——玩家攻击的不是他的命，是他**信念的完整性**。

**"数据节奏"机制（他的行动）**：
- 他的行动模式**完全固定、可预测**：每回合重复同一套"标准化治疗"（固定顺序、固定数值），像一台从不失误的机器。
- 但他每回合会**免疫玩家上一回合使用过的牌类型**（"学习"）：若玩家连续两回合用同类型的牌（同"法"或同"认识论"），第二回合该类型对他**效力归零**。
- **设计意图**：逼迫玩家**每回合换策略**，不能用一套连招碾压——正如现实中"标准化方案"会迅速被耐药/演化瓦解。玩家必须不断换眼睛，才能不被他算死。

**破法（唯一绕过"学习免疫"的路）**：
- **只有合方牌（汇通连线技）能绕过他的"学习免疫"**。因为合方牌包含"人的因素"——其效果**随机浮动、不可被预测**。他的系统能学会"清热""攻下"这类**可命名、可归档**的牌，却学不会"沈砚之把陆九针的方和苏晚棠的针拼在一起"这种**没有标准名字的、个体化的**东西。
- **机制化的主题**：数据能学习"可量化"的一切；个体化、不可量化的医道，是它的盲区。

**胜负条件（不是打死他，是让他看见）**：

```
证伪触发： Σ Damage_convergence ≥ 0.6 × E_max
其中 Damage_convergence = 本场所有合方牌对霍崇光造成的伤害之和
```

- **E_max** = 霍崇光"信念完整性"条（基准 100，与病邪同尺度）。
- 当**累计合方伤害 ≥ 60% × E_max** → 霍崇光进入**「证伪 Falsified」状态**，战斗结束。
- 结束方式**不是打赢/打死他**，而是他**当众问出那句全篇最深的疑问**（见 `story-bible.md` 卷九）："我算过每一个变量。……我算过每一个人。可人，怎么可以不算进去？"——**他看见了自己的错，战斗即终结。** 这是一场以"敌人的自我怀疑"为胜利条件的战斗，也是本作对"科学主义"最克制、最不流血的批判。
- **失败条件**：病人（若场上有危重病人）正气归零，或**回合数超过上限仍未达证伪阈值**（表现上：玩家的"个体化"始终没能击中他，他被"证明"是对的）。

**示例计算**：E_max=100，证伪阈值 = 60。玩家本场合方牌累计造成 62 点伤害（如三条「通」各命中一次，S_link 加成后合计 62）→ `62 ≥ 60` → 触发证伪 → 战斗结束。

**与其它系统的关系**：本战的胜利路径**强依赖成长系统的汇通图**——若玩家到卷九仍未建起足够多的跨树连线，就**没有足够的合方牌**去击穿他的免疫，这正是"只练一棵树"在主线上的终局代价。

### 4.10 特殊战斗设计：单认识论遭遇（单核 / 双核由牌池涌现）

> 前面所有战斗都默认"两套眼睛并用"。但玩家完全可能**只带一套眼睛**上场——尤其是剧情早期（卷一只有沈砚之），或队伍因聚散而缺员时。本节定义：**当牌池只有一侧资源时，这场仗怎么打。**
>
> **核心原则——不做强制失败**：单核牌池永远留一条**"惨胜"之路**（病邪清零且病人存活），但它赢得极窄：容错极低、评级极低、常常损正气。系统用**明确的敌人行为与反馈**提示"你缺一只眼睛"，而**不用硬性锁死**拒绝玩家。全篇唯一的例外是 §4.9 霍崇光终局战（见本节末）。

#### 4.10.1 单核 / 双核 由牌池涌现判定（不给角色贴标签）

> **本轮修订（砍掉阵容分类器）**：早期版本给每名队员贴 R/H/B"主树归属"标签、再由在场阵容推导战斗型。**该分类器已删除**——理由是它**覆盖不了实际用例**：全篇（依 `story-bible.md` §8.1 聚散总图）**陆九针自卷二起全程在队**，故"只有整体"的持守型**实际零次发生**，而"只有还原"的速攻型**仅卷一一次（且是教学脚本）**。一个三态分类器覆盖两个实例、其中一个是脚本，属**过度设计**；更糟的是它会诱发"为凑战斗型而给角色硬派归属"的动机（见下）。

**改为按牌池实际内容涌现判定**——不引用任何角色标签：

```
本场可用牌池 P = 在场队员的单方牌 ∪ 已生成「通」的合方牌

has_R_pool = P 中存在消耗「剂量」资源的牌（还原侧）
has_H_pool = P 中存在消耗「气机」资源的牌（整体侧）

has_R_pool 且 has_H_pool → 双核型（常规，走 §3–§4 全部规则）
仅 has_R_pool            → 速攻型 Race
仅 has_H_pool            → 持守型 Sustain
两者皆无（空池）          → 非法，配置校验拒绝
```

- **好处**：①**纯涌现**——无需维护角色归属表，角色换流派/后期转树也不会失配；②顺带消掉"为让战斗型成立而硬凑归属"的设计动机；③归属描述**只留在叙事层**（`progression-system.md` §3.5 人物弧），不进机制。
- **叙事落点不变**：同伴来去仍**直接改变能打的仗**——苏晚棠（天生是桥，牌池含两侧）在队时队伍天然握有两套眼睛；她离队（卷六→七）后合方牌灰化，若其余在场者牌池只剩一侧，战斗型随之落回单核。这是"聚散皆成长"在战斗层的直接体现。

**注**：单镜型（下 4.10.4）**不是**由此判定出来的，而是**病机自身的属性**——它决定"哪些牌能处理这个病机"，与上面的单/双核判定正交。

#### 4.10.2 速攻型 Race —— 纯还原牌池（只有西医）

- **可胜路径**：还原牌（耗剂量）直接削减病邪；病邪清零且病人存活即"惨胜"。
- **代价**：队伍**无"托住正气"的手段**——没有整体牌去扶正、调衡。剂量毒性、峻剂、耐药持续侵蚀病人正气，且**没有任何牌能把正气抬回来**。
- **玩法本质**：**赛跑**——在附带损伤杀死病人之前清空病邪。节奏紧、容错低、几乎不容一回合浪费。
- **主题**：还原论**快、准、狠**，靠速度赢、不靠平衡。它是一把锋利的刀，刀没有问题，问题是刀不负责"养人"。
- 与 §4.8（卷一教学关：只有还原；病人正气从 100 掉到 **58–100**，六子支区间——最差乙a **58**、最好丙b **100**，见 §4.8 结论表）同源——那就是速攻型的极端样本。

#### 4.10.3 持守型 Sustain —— 纯整体牌池（只有中医 / 心身）

> **本轮修订（降级为关卡层）**：持守型**不进战斗规则**，而是**关卡胜利条件的一个变体**——由关卡系统接管（见 §6 关卡系统依赖）。理由是"撑到某回合"是**换目标**，不是新机制；把它塞进战斗结算层只会徒增状态机复杂度。

- **可胜路径**：整体牌（耗气机）护住病人（扶正、调衡、固本），并**缓慢"化"解**病邪。
- **代价（若临时出现）**：整体论缺"斩杀键"——对**暴发性 / 急性**病机，病邪削减速率追不上增幅 → 只能拖。**注意**：能修得暴发急症的情形在本作剧本中**极罕见**（陆九针全程在队），故此型主要为**框架完备性**与**单镜型遭遇**而保留。
- **主题**：机制化历史上那句真实而沉重的批评——"中医救不了急症"。不是中医无能，是**整体论要的是时间，而急症不给时间**。
- **胜法（关卡层实现，复用既有 stages，不加新机制）**：把该关的胜利条件从"削减病邪"改写为**"存活到第 N 回合"**：
  1. 末段 stage 设为**"自限"形态**（复用 `status-syndrome.md` §4.4/§4.4.1 的"病势燎原"同款机制）：`ampPerTurn ≤ 0`、病机停手——避免"病邪条还很高却判胜"的观感。
  2. 关卡校验 `N ≤ 0.8 × 正气上限 / 每轮净损耗`，保证 N 回合内病人不会先死（有余量、非侥幸）。
  3. **中段插入 1–2 次"诊断性转折"**（本轮修订，取代早期的"援军"）：病机**传变**，迫使玩家**重新四诊、重排治法**——让防守的空转段变成**谜题节拍**，且贴整体论气质（"援军"是还原式救援，不搭）。
  4. **转折必须带正收益**（否则纯成本、玩家觉得被课税）：传变后的新 stage **必须附带一个可化解的状态**（如 `st_toxin` → 对证牌化解后该 stage 的 `ampPerTurn` 再降 25%）。传变次数**限 1–2 次**，防疲劳。

#### 4.10.4 单镜型 Single-lens —— 只有一种认识论有效

有些病机**本身**只有一只眼睛看得见。此时**错误的牌池面对的是一个"查不出、打不动"的敌人**：

| 病机类型 | 只有谁能处理 | 其他牌池遭遇时 | 
|---|---|---|
| **无症状带菌者** | 只有「**检验**」（还原）能识别与处理 | 纯整体牌池**无法通过四诊发现病机**——望闻问切一律返回"无显著证候"；病邪条可见但对整体牌近乎免疫 |
| **心因性疾病**（情志、癔症等） | 只有「**心身**」（整体）能触到 | 纯还原牌池的检验 / 靶向**对病邪条无效或甚微**——查不出病灶，也打不动"病邪"。**且纯还原阵容无「气感」→ 切诊不可用 → 深层阴阳读不到**（见 3.5），此"缺眼"由规则涌现、无需特判 |

- **设计意图**：**反向巩固"两套都要"**——有些问题**确实**只需要一只眼睛；但反过来，**没有任何一种眼睛能包打全场**。单镜型遭遇让玩家体会到：不是"双用永远更强"，而是"带错眼睛，连病都看不见"。
- **提示方式（用行为说不，不用锁死不让你打）**：敌人行为 / 系统反馈明确示意"你缺一只眼睛"——例如对带菌者反复只给"无显著证候"的回报；或病邪对某类牌给出**"免疫"的视觉反馈**（"你的药，打在了空气里"）。玩家可**立刻转诊**（见 §5）换阵容重试，而非被判负。

#### 4.10.5 失败模式保留（主题不可丢）

两种"单镜之败"必须在数值上**真实存在**、可被玩家亲身踩到，它们是全篇主题的反面教材：

| 失败模式 | 表现 | 对应主题 |
|---|---|---|
| **霍崇光式失败**（纯还原） | 病邪清零了，病人却因正气耗尽而死 | "指标全正常，人没救回来"——卷九霍崇光体系的证伪 |
| **救不了急症式失败**（纯整体） | 病人被养得越来越舒服，暴发性病邪在平衡回来之前先杀死了他 | 历史上"中医救不了急症"的批评 |

二者都呼应 §5 边界中"病邪清零但正气也归零 → 判负"的用例——**判定顺序本身就在为这两种失败兜底**。

#### 4.10.6 "惨胜"的判定

单核牌池的胜利**不是**被禁止的，而是**代价可见**：

- **满足**常规胜利条件（病邪 ≤ 0 且所有病人正气 > 0）即判胜，**不额外锁死**。
- **但评级被显著压低**：`Rating` 计入**单核罚项**（见 4.7 与 §7 旋钮）——单核取胜的 `Rating` 显著低于双核解，因此顿悟点奖励更少（见 `progression-system.md` 4.5）。**赢是赢得起，但赢得不漂亮。**
  - **罚项的判定改为按资源使用结算**（本轮修订，取代"按阵容"）：`若本场未动用任一侧资源（剂量或气机）出牌 → 触发罚项`。语义不变（"你只用了半套眼睛"），定义更硬（以实际行为为准，不依赖阵容归属标签）。
- **常见次生代价**：病人以低正气收场（< 30%）→ 触发**后遗症 debuff** 带入后续关卡（沿用 §5 既有规则）。
- **设计承诺**：玩家**永远**有一条路走通每一场常规遭遇——哪怕只有一套眼睛。系统只负责把"你走得多艰难"如实算给他看。

#### 4.10.7 与 §4.9 的关系（全篇唯一的强制双核例外）

- **常规遭遇**（§4.10.2–4.10.6）：**不做强制失败**——单镜型永远留"惨胜"之路。
- **§4.9 霍崇光终局战**：**全篇唯一**允许"单核无法通关"的战斗。他的"学习免疫"使任何单一流派必被算死，**只有合方牌能击穿**——即**强制双核**。这是主题的终局清算，且经卷四→卷六的**渐进软墙**（见 `progression-system.md` 3.7）长期铺垫，玩家到卷九早已具备双核能力。
- 二者的关系是**程度之别、非矛盾**：常规遭遇用"惨胜的代价"逼你学会双用；终局战用"单核必败"逼你**兑现**双用。**主题从"建议"升格为"考核"。**

---

## 5. 边界情况 Edge Cases

| 场景 | 会发生什么 | 理由 |
|---|---|---|
| 同一结算中病邪归零、正气也归零 | **判负**（正气优先判定） | 核心命题："指标正常，人没了"= 失败 |
| 病邪清零但病人正气 < 30% | 判胜，但附加**后遗症 debuff** 带入后续关卡 | 治好了，但伤本 |
| 多病人关卡中一名病人死亡 | 若为**主要病人**（目标）→ 立即判负；若为**次要病人** → 判负但允许"转诊"重试 | 区分目标与背景患者 |
| 战斗中证候**传变**（轴重组） | 已揭示的轴**重置为未证**，病机进入「传变」态 1–2 回合后重组 | 辨证是动态的，不是一次性谜题 |
| 手牌全部不匹配 | 玩家只能出**无关牌（M=0.5）**或使用「换气/弃牌」行动重抽；不出悖证不会伤正 | 保证玩家永远有"劣解"，不会死锁 |
| 病人身上**同时多个病机** | 多病邪条并列；玩家须**选择优先削减**哪个；不对某病机的牌可能加剧另一病机（可选高阶规则） | 复现真实"复合病" |
| 前 3 回合就使用峻剂 | 允许；但对虚衰度 B 高的病人风险放大（ΔV_wrong/ΔV_drastic 乘 (1+B)） | 惩罚鲁莽，不禁止选择 |
| 猛药 + 错治叠加 | 同时触发 ΔV_wrong(Overdose) 与 ΔV_drastic，**两者相加** | 最坏情形显式定义 |
| 无症状带菌者（证候完全不可诊） | 四诊均返回"无显著证候"；只能靠**检验**（还原）识别，且检验需特定队员 | 还原论不可替代的场景 |
| 玩家中途退出/逃跑 | 视为**转诊**：判"未愈"，病人保留生命但关卡记为未完成，可重试 | 医疗语境的合理逃逸，非死亡惩罚 |
| 战斗中存档 | 快照仅存于**回合边界**；回合结算中途不可存（防作弊/状态不一致） | 实现可行性 |
| 队员在战斗中阵亡 | 队员**不会死亡**（无血条），但可被"击倒/离场"，其牌暂时不可用 | 保护叙事角色（苏晚棠之死由剧情决定，不由战斗随机决定） |
| 苏晚棠离队期（卷六→七） | 依赖她的**合方牌与连线**全部灰化不可用，队伍被迫用残缺图谱作战 | 剧情与机制绑定的"系统性留白" |
| 阈值为 0 或负数 | 配置校验拒绝启动；速度 ≤ 0 视为非法数据 | 防除零/负时间 |
| 单张牌 ΔE 超过病邪剩余 | 病邪截断至 0，**溢出伤害不转嫁**正气或他病机 | 避免"过量治疗"被误当收益 |
| **旁观遭遇**（§3.10）被跳过 | 演出整段跳过；**不结算任何评级 / IP / 耐药 / 紊乱**（本就为 0）；推进链 flag 由关卡层保证点亮 | 跳过改变的是"理解厚度"，不是"事实" |
| **旁观遭遇的配置数据非法** | 配置校验**拒绝**启动：`behavior` 须为 `status-syndrome.md` §3.3 合法枚举、`syndrome.yinyang` 须为**两层结构**——**不因"不结算"而豁免数据合法性** | 数据合法性（§3.10） |
| **旁观遭遇中玩家尝试操作** | 输入**被忽略**（`playerControl = none`）；不弹牌面、不弹行动条、不进入 CTB | 演出态而非战斗态（§3.10） |
| **旁观遭遇脚本宣称"阴阳已证"但脚本内无切诊** | 配置校验**拒绝**：望只给表层极，**阴阳深层极须由切诊揭示**——脚本缺切诊即不得置阴阳轴为「已揭示」 | 守护"玩家永远能事先知道牌会不会伤人"（§3.5 / §4.3） |

---

## 6. 依赖 Dependencies

| 系统 | 方向 | 依赖性质 |
|---|---|---|
| 成长系统（医道图谱 / 《杏林汇通录》） | **本系统依赖它** | 提供牌库、单方/合方牌、连线加成 S_link、顿悟点 |
| 成长系统（医道图谱） | **它依赖本系统** | 战斗评级 → 顿悟点奖励；战斗使用记录 → 耐药/紊乱数据 |
| 状态 / 证候系统 `status-syndrome.md` | 双向 | 本系统使用证候向量、四诊揭示、匹配系数与状态词条；证候系统是本系统的数据结构宿主 |
| 关卡系统 `level-system.md` | **双向** | **它依赖本系统**：关卡编排战斗遭遇、胜利条件、病人配置；**章层旁观遭遇**（`level-system.md` §3.2 `Encounter`）同样由关卡层编排，但**不实例化本系统循环**（§3.10）。**★ 本系统依赖它（rev g 新增）**：本系统 §4.3 的 `M = 1.5`（对证）与 §4.3c 的悖证判定**都以 `revealed` 轴为前提**——若关卡不给玩家可探的证候，`revealed` 恒空，对证档与悖证**在整卷内不可达**。关卡系统以 **§3.4 自由探索窗口**（`问 Q`→`治 T` 间隙，配置期强校验）**保证给本系统一个非空的 `revealed` 输入**；分工为"**本系统定义规则、关卡保证规则可被玩到**" |
| 对话系统 | 弱依赖 | 战前问诊、战后结算对白由对话系统托管 |
| 论战系统（卷八） | 并列 | 论战复用"证据/逻辑/案例/人心"资源模型，与本系统**共享说服/匹配判定框架**（可选复用） |
| 存档系统 | **本系统依赖它** | 回合边界快照；战斗内状态序列化 |
| 故事圣经 `story-bible.md` | **本系统依赖它** | 队员、卷次解锁、反派机制一致性 |
| UI / HUD | **它依赖本系统** | 需显示：病邪条、正气条、行动条、已揭示证候轴、双资源、耐药/紊乱；**旁观遭遇为叙事模式 UI**——以上全部**不显示**（§3.10） |

**循环依赖**：无。成长系统提供牌 → 战斗产出顿悟点 → 成长系统消费顿悟点，为**单向数据流**（非循环）。

---

## 7. 调参旋钮 Tuning Knobs

| 旋钮 | 当前值 | 安全范围 | 增大影响 | 减小影响 |
|---|---|---|---|---|
| 行动条阈值 Threshold | 1000 | 800–1200 | 战斗变慢、策略时间变长 | 节奏加快、容错降低 |
| 病机速度区间 | 40–150 | 30–180 | 病机压迫感↑、治疗窗口↓ | 慢性病主题、教学友好 |
| 病邪基准值 | 100 | 60–200 | 战斗更长、更依赖增效 | 战斗短促、顿悟感强 |
| 正气基准值 | 100 | 60–200 | 容错高、峻剂更敢用 | 高风险、贴近"HARD"体验 |
| 匹配系数（对证） | 1.5 | 1.3–1.8 | 辨证收益拉大、鼓励四诊 | 辨证收益变小、战斗趋平 |
| 悖证伤正 C_base | 12 | 8–20 | 失误代价重、惩罚性 | 教学友好 |
| 峻剂 K_drastic | 2.5 | 2.0–3.0 | 峻剂更诱人、风险回报更陡 | 峻剂趋鸡肋 |
| 峻剂 C_drastic | 0.8 | 0.6–1.0 | 峻剂更危险 | 峻剂更安全 |
| 耐药斜率 k_R | 0.25 | 0.15–0.35 | 逼玩家换套牌、反单一流派 | 允许一路平推 |
| **耐药豁免 `no_resistance`**（§4.5） | 开（仅「补液·支持」） | 开 / 关 | （关）生理盐水也累积耐药、卷一教学关须重算为 8 回合 | （开）医学身份正确（盐水不产生耐受），且 §4.8 六子支数值成立 |
| 紊乱斜率 k_D | 0.20 | 0.10–0.30 | 逼整体牌对证使用 | 整体牌更宽容 |
| 四诊行动耗费 | 1 | 1–2 | 辨证更贵、更需取舍 | 辨证更廉、谜题感降低 |
| 切诊被动（苏晚棠 / 陆九针在场） | 开（耗 0 行动） | 开 / 关 | 操作负担↓、辨证节奏快 | 辨证仍需全额投入、更硬核 |
| 霍崇光"学习免疫"强度 | 连续 2 回合同类型免疫 | 1–3 回合 | 更宽容、可连招 | 逼每回合换牌、更硬核 |
| 霍崇光证伪触发阈值 | 0.6 × E_max | 0.4–0.8 | 更易触发、终局更短 | 需更多合方命中、终局更长 |
| 汇通连线系数 | 0.15/线 | 0.10–0.20 | 合方牌支配性↑ | 合方牌趋普通 |
| 手牌上限 | 8 | 6–10 | 选择更多、决策更重 | 更受抽牌随机性影响 |
| 每回合抽牌 | 3 | 2–5 | 出牌频率↑ | 资源更紧 |
| **单核取胜罚项**（§4.10，**按资源使用结算**） | −0.20 | −0.10 – −0.30 | 单核代价更重、更逼玩家双用 | 单核几乎无痛、双用动机弱 |
| **单镜型遭遇"缺眼"反馈强度**（§4.10） | 强（敌人行为 + 角色文案） | 弱–强 | 玩家更快学会"带对眼睛" | 更含蓄、易错过（学习更慢） |
| **单核胜法**（常规遭遇，§4.10.6） | 放行"惨胜" | 放行 / 禁止 | （禁止）无惨胜路径、必须凑齐双核 | （放行）兑现"永远有路走通"的设计承诺，**不可违反**（见 §4.10.7） |

**必须数据驱动**：以上全部值应位于外部配置文件，不得硬编码（见项目 coding-standards）。

---

## 8. 验收标准 Acceptance Criteria

- [ ] 战斗可在"病邪归零"时判胜、"正气归零"时判负；**同刻双归零判负**——测试用例可验证。
- [ ] CTB 行动序符合 `T_next = (Threshold − ActionGauge)/Speed`；给定速度可预测出手次序（误差 0）。
- [ ] 还原牌与整体牌分别只接受剂量 / 气机支付；跨资源支付被拒绝。
- [ ] 打出对证牌（M=1.5）时 ΔE 与公式一致（示例：P=40→ΔE=60，误差 < 1%）。
- [ ] 打出悖证牌触发 ΔV_wrong；数值 = 公式值（示例 C_base=12→−12 正气）。
- [ ] 峻剂牌 ΔE/ΔV 比值落入 2:1–5:1 区间，且对证峻剂比未证峻剂明显更优。
- [ ] 同族还原药第 4 次使用的 T_drug = 0.5（±0.01）；**声明 `no_resistance: true` 的牌（如「补液·支持」）连用任意次均恒为 T_drug = 1.0**（守卫检查，§4.5 / D11）。
- [ ] **`ΔE` 逐牌取整**（§4.2 R4）：单牌 `ΔE` 四舍五入到整数后才从病邪条扣除；`35 × 0.7 → 25`、`35 × 1.5 → 53`（边界值测试）。
- [ ] 未揭示轴出牌按 M=0.7 结算且**不伤正气**；已揭示轴打出相悖方向牌时**确定性地**触发悖证（M=0.0 + ΔV_wrong），可复现、无随机（连测 100 次结果一致）。
- [ ] 悖证触发时显示对应的角色口吻反馈文本（4.3a），且文本随发牌者不同而不同。
- [ ] 队伍含苏晚棠或陆九针时，切诊行动耗费为 0；不含时耗费为 1（守卫检查）。
- [ ] 霍崇光终局战：连续两回合出同类型牌时，第二回合该类型效力归零（学习免疫）；合方牌**不**触发免疫。
- [ ] 霍崇光终局战：累计合方伤害 ≥ 0.6 × E_max 时进入「证伪」状态并结束战斗（示例 62 ≥ 60 触发）。
- [ ] 霍崇光终局战：回合数超上限仍未达证伪阈值时判负。
- [ ] 手牌全不匹配时，玩家仍有"换气/弃牌"行动可用，不会死锁。
- [ ] 证候传变后，原已揭示轴重置为未证。
- [ ] 苏晚棠离队期间，依赖她的连线与合方牌不可用（灰化）。
- [ ] 单张牌溢出伤害不转嫁（病邪截断至 0，正气不受影响）。
- [ ] **阴阳两层**（§3.5 / `status-syndrome.md` §3.2）：仅望得表层极时，阴阳轴按 M=0.7 结算且**不触发悖证**；切诊揭示深层极后，才参与 M=1.5 与悖证判定——三种状态（未读 / 仅望 / 已切）各有确定行为。
- [ ] 普通病机两层阴阳一致（无破绽）；心身病机在 `\|D_derived\| < 0.667×\|D_innate\|` 时两层极相悖，且此路径**无需特判**即可复现。
- [ ] **单核/双核由牌池涌现判定**（§4.10.1）：仅含剂量牌 → 速攻型；仅含气机牌 → 持守型；两侧俱在 → 双核；空池被配置校验拒绝——**不引用任何角色归属标签**（守卫检查：无 R/H/B 标签亦可判定）。
- [ ] **速攻型**（纯还原牌池）**无任何抬升正气的手段**：作用于正气回复的牌均为整体牌 / 合方牌，纯还原牌池不可用（守卫检查）。
- [ ] **持守型**（§4.10.3，关卡层）：末段"自限"stage 的 `ampPerTurn ≤ 0`；关卡校验 `N ≤ 0.8 × 正气上限 / 每轮净损耗`；中段 1–2 次传变各带一个可化解状态（化解后 amp 再降 25%）——可逐关验证。
- [ ] **单镜型**（§4.10.4）：无症状带菌者 / 心因性疾病在**错误牌池**下，四诊返回"无显著证候"或病邪对相应牌给出免疫反馈（效力 ≈ 0）；**正确牌池**可正常削减——两侧均可测试。
- [ ] 单核取胜时 `Rating` 计入 **−0.20 罚项**（**按"是否动用两侧资源出牌"结算**，非按阵容），其 `IP_gain` 显著低于同场双核解（数值可核算）。
- [ ] **常规遭遇中，单核牌池始终存在**一条满足"病邪 ≤ 0 且所有病人正气 > 0"的胜利路径（不允许无解关卡）——逐关回归测试。
- [ ] §4.9 霍崇光战中，单核牌池**无法**达成证伪阈值（合方牌不可用即必然失败）——与常规遭遇的"不做强制失败"形成对照（§4.10.7）。
- [ ] 战斗评级按 4.7 公式计算，权重可由配置修改。
- [ ] 所有数值（阈值、系数、基准值）来自外部配置，实现中无硬编码。
- [ ] 性能：单场战斗（≤6 实体）回合结算在 **16ms** 内完成（60fps 帧预算内）。
- [ ] UI 能同时显示病邪条、正气条、行动条、已揭示证候轴、双资源与耐药/紊乱数值。
- [ ] **M 二维表可复现**（§4.3）：M 由「轴关系（同轴 / 相关轴 / 无关轴）× 揭示状态」唯一确定——**同轴·相符** = 已揭示 `1.5` / 未揭示 `0.7`；**同轴·相反** = 已揭示 `0.0`（伤正）/ 未揭示 `0.7`；**相关轴** = `1.0`（不随揭示变）；**无关轴** = `0.5`（不随揭示变）。**未揭示的牌永不落在 `0.0`**（连测 100 次结果一致，无随机）。
- [ ] **相关 / 无关不依赖揭示**（§4.3）：所有轴未揭示时，作用于**相关轴**的牌取 `M = 1.0`、**无关轴**取 `M = 0.5`（不随揭示变）。**★ 已裁决（D6 = 甲 / D9 = 甲）**：`§4.8` 回合 2 的「补液·支持」**不再取 `1.0`**——其 `mainAxis`（`虚实:虚`）与病机致病轴**同轴**（§4.3 R2），故取 **`0.7`（`虚实` 未揭示）/ `1.5`（已揭示且为虚）**；`1.0` 旧读法**作废**。
- [ ] **多轴牌必须有 `mainAxis` 且 `M` 只取它**（§3.6 R1）：未声明 `mainAxis` 的多轴牌被配置校验**拒绝**；`subAxes` 不参与 `M` 计算（守卫检查）。
- [ ] **相关轴以「轴不同」为必要条件**（§4.3 R2）：牌的全部轴均落在致病轴集合内时判为**同轴**，非相关轴；「补液·支持」对「寒饮伏肺」即此例（用例）。
- [ ] **伤正触发条件两条**（§4.3c）：① 主靶轴「同轴 · 已揭示 · 方向相反」→ `M = 0.0` 且伤正；② **任一次靶轴已揭示且相悖** → `M` 不变但**触发 `ΔV_wrong`**。卷一不触发第二条（次靶轴 `表里:里` 方向相符）。
- [ ] **六子支全程可复现**（§4.8）：甲a 5 回合 / 正气 70 / `Rating 0.565`；甲b 3 / 90 / `0.655`；乙a 5 / 58 / `0.481`；乙b 3 / 78 / `0.571`；丙a 4 / 80 / `0.610`；丙b 2 / 100 / `0.700`——六条各自可核算（误差 < 1%，口径：病邪残余率 0、回合效率 0.5、单核罚项 −0.20、乙支另 −0.03）。
- [ ] **「明知故犯」代价与 `M` 无关**（§4.8）：`甲a − 乙a = 甲b − 乙b = 0.084`（用例）。
- [ ] **§4.8 回合 1 两分支均可复现**：分支甲（寒热未揭 → 退热剂 `M = 0.7`、`ΔE = 21`、病邪 100 → 79、**正气不变 100**）；分支乙（寒热已揭 → 悖证 `M = 0.0`、`ΔV_wrong = 12`、病邪不变、正气 100 → 88）——两条路径数值各自可核算（误差 < 1%）。
- [ ] **`ex_v1_m_4` 在学生手上可达**（§4.8 / `level-system.md` §3.4 自由探索窗口）：章 B 存在一段 Q1 与 T1 之间的自由探索窗口，玩家在其中可探得 `ex_v1_m_4` 并点亮 `vol1.mother.knew_deficient`；`b` 列（3 个 `1.5` 子支）在实机可复现（用例）。
- [ ] **旁观遭遇不结算**（§3.10）：不进入 CTB、不结算 `Rating` 与 `IP_gain`、不累积耐药 / 紊乱、不触发传变；不计入成就与战斗统计（守卫检查）。
- [ ] **旁观遭遇 UI 为叙事模式**（§3.10）：全程不显示病邪条 / 正气条 / 行动条 / 证候向量 / `M` / `ΔE` / 牌名 / 资源数值（UI 守卫检查）。
- [ ] **旁观遭遇零输入**（§3.10）：脚本运行期间不产生任何玩家操作输入；玩家输入被忽略。
- [ ] **旁观遭遇数据合法**（§3.10）：`behavior` 为 `status-syndrome.md` §3.3 合法枚举（不得出现"无行动"等自定义值）；`syndrome.yinyang` 为两层结构；非法配置被校验**拒绝**。
- [ ] **旁观遭遇可跳过且不破坏推进链**（§3.10）：跳过后章层的推进链 flag 照常点亮，并额外产出"是否真看过"的 flag；不跳过时该 flag 为真——两侧均可测。

---

> **rev c 修订要点（2026-09-12，承卷一 A-2 旁观遭遇改动）**
> 1. **新增 §3.10 旁观遭遇 Spectator Encounter**：一种**非交互的战斗演出**（玩家零输入、不进入 CTB、不抽牌、不结算 `ΔE` / `Rating` / `IP_gain`、不累积耐药 / 紊乱、不触发传变、UI 为叙事模式、恒不可失败、可跳过）。其"诊 / 辨 / 治"是**叙事节拍**，**不得**与 §4.1 的行动**回合**混称。用途：教学铺垫（卷一 A-2）与剧情演出（如卷二"回看"陆九针救母）。
> 2. **§4.3 补明 M 的判定顺序**（一句话口径升级为**唯一 5 步顺序**）：悖证 0.0 → 对证 1.5 → 未证 0.7 → 相关 1.0 → 无关 0.5，取**首个命中**项。关键性质：**未揭示的牌永不落在 0.0**。〔**rev d 已把此 5 步顺序重构为二维表**，见下。〕
> 3. **§4.3 补明"相关 / 无关不依赖揭示"**：对证与悖证依赖"该轴已揭示"；相关与无关是**牌的轴方向与病机致病轴的客观关系**。"未证 0.7" = 打的就是致病轴、但轴未揭示，方向无从判定。
> 4. **§4.8 回合 1 拆为两条互斥分支**（修 D1 矛盾）：**分支甲**（寒热未揭）→ 退热剂 `M = 0.7`、`ΔE = 21`、病邪 100 → 79、**不伤正**；**分支乙**（寒热已揭）→ **悖证** `M = 0.0` + `ΔV_wrong = 12`、病邪不变、正气 100 → 88。§4.3 规则**一字未改**。
> 5. **§4.8 的 `ΔE` 记法补第 5 因子 `S_link`**（修 D5）：`35 × 1.0 × 1 × 1 × 1 = 35`；数值不变（`S_link = 1.0`），记法与 §4.2 的 5 因子对齐。
> 6. **§4.8 结论对照重写**为 甲 / 乙 / 丙 三支对照表（甲 80 / 乙 68 / 丙 90），并与 `design/levels/vol-01.md` §3.5 的三分支表**互相声明同源**；§4.8 回合 4 明确标注为**反事实对照（卷二用），非卷一可行路径**（卷一无可执行切诊）。
> 7. **§4.8 前置引导 1 改写**：由"随意找母亲周慧兰去镇上抓药、看老郎中问诊"改为"**独自**撞见一次**旁观遭遇**"（§3.10），并指向 `design/levels/vol-01.md` §2.4。
> 8. **§5 补 4 条边界**（跳过、配置数据非法、玩家尝试操作、脚本宣称"阴阳已证"却无切诊）；**§6 依赖表** 关卡系统 / UI-HUD 两行补旁观遭遇口径；**§8 补 9 条 AC**。
>
> **同轮改动的关联文件**：`design/gdd/level-system.md` §3.2.1（章层 `Chapter` / `Encounter`）、`design/levels/vol-01.md` §2.4（A-2 实例配置）。

> **rev d 修订要点（2026-09-12，据独立 Opus 复核）**
> 1. **B1 修**：§3.10 示例**不再写 `ΔE = 60`**（旁观遭遇本不结算 `ΔE`），"温法"改为 **汗法**（`status-syndrome.md` §3.5：桂枝汤为辛温解表之汗法，主靶轴为表里:表，**不是**寒热），并把 `level-system.md §3.2` 的引用改为 **§3.2.1**。
> 2. **M1 / M2 修**：§4.3 的「5 步判定顺序」**重构为二维表**（**轴关系 × 揭示状态**），并**声明本节为 M 的唯一真源**；"相关轴"的真源指向 `status-syndrome.md` §3.5 的 `相关轴（M=1.0）` 列。`status-syndrome.md` §4.3 的旧式 5 行表述（"相关需已揭示"）与本表冲突，该文件**不在本轮改动集内**，**登记为待办**。
> 3. **★ D6 登记（数值下轮定）**：二维表下，「补液·支持」（轴 = 表里:里 / 虚实:虚，**即**该关致病轴）应取 **`0.7`**（同轴·未揭示），故 §4.8 回合 2 与 `vol-01.md` §3.5 的 `1.0` 属**旧读法**。**本轮只修口径、不动数值**——`1.0` 保留并在此二处标注「**待裁决 D6**」。另：该牌 2026-09-12 由「强心剂」**更名**（只换医学身份与叙事定位，机制属性一字不动），**改名不解决本条**（轴未变）。
> 4. **m1 修**：§4.10.2 的"正气从 100 掉到 78"更正为 **80（分支甲）/ 68（分支乙）**（与 §4.8 结论表一致）。
> 5. **m2 修**：文件头 `Status` / `Last Updated` / `Last Verified` 更新至 **rev c / 2026-09-12**。
> 6. **m3 修**：§4.3 内的 `(见 4.3b)` 统一为 **`§4.3b`**，与 `status-syndrome.md` §4.3 的引法一致。

> **rev e 修订要点（2026-09-12，卷一章 B 治段 · 医学身份更正）**
> 1. **§4.8 回合 2 牌名「强心剂」→「补液·支持」**（含 §4.3 D6 条、§4.8 D6 提示、§8 AC 佐证说明共 5 处）。理由：38.1℃ 发热、**无**心衰指征的病人西医不会开强心剂（洋地黄）；**只换医学身份，机制数值一字不动**（还原 / P=35 / 轴 里·虚 / `M` / `ΔE` / 分支数值皆不变）。详见 `design/levels/vol-01.md` §3.3 D1 / §3.5。
> 2. **文件头 `Status` 由 rev c 更正为 rev e**（消除"头部 rev c、尾部 rev d"的残留不一致）。

> **rev f 修订要点（2026-09-12f，据独立 Opus 复核两轮〔机制 / 叙事〕；**机制数值一律不动**）**
> 1. **§4.3a 悖证反馈泛例去「脉象」**：沈砚之那句原写"可**脉象**明明是寒"——但**卷一沈砚之不可执行切诊**（`design/levels/vol-01.md` §1.1：无 `sense.qi`），拿不到脉象。已改为"**可我知道她是寒**"（"寒"来源于探索节点物证 `ex_v1_m_1` / `ex_v1_m_3`），并加注说明来源。
> 2. **§4.8 结论对照表 · 乙分支残余值的冲突已登记**：本表乙 = 病邪 **5 / 没打完**，而 `design/levels/vol-01.md` §3.5 三分支表乙 = 残余 **0 / 第 4 回合胜**（两处对乙回合 4 的建模不同：本表把"卷二才可行的温法反事实"混进了乙的演算）。**本轮不改数值**，登记为 **D10**（见 `vol-01.md` §6 D10），与 D6 / D9 同批裁决。
> 3. **不改机制数值（硬约束）**：`M` 二维表、`ΔE` 五因子式、评级权重、D6 待裁决值（`1.0`）**全部冻结**。

> **rev g 修订要点（2026-09-12，**落地 D6 / D9 / D10 裁决 + 新规则 R1–R4 + D11**；⚠️ **本轮改了数值**，与 rev d / e / f 的"数值冻结"不同——已获用户对完整改动集的批准）**
> 1. **D6 裁决 = (甲)**（§4.3）：「补液·支持」`M = 0.7`（未揭示）/ `1.5`（`虚实` 已揭示），`1.0` 读法作废；否决 (乙) 的理由（与 R2 冲突、永久剥夺补虚的对证档）。
> 2. **D9 裁决 = (甲)**（§4.3）：「补液·支持」`mainAxis = 虚实:虚`、`subAxes = [表里:里]`——这是**卷一唯一**能出现 `M = 1.5` 的通道（另一解 `表里:里` 会使 D9 消失但卷一再无对证回报）。
> 3. **D10 裁决 = (甲)**（§4.8）：删除原"回合 4 温法反事实段"（把卷二才可行的温法混进乙支演算，是乙支残余值冲突的根因）；乙支与其余五支**同口径**重算。
> 4. **§4.8 由三分支重写为六子支**（三分支 × `虚实` 两态），结论表给出六个 `Rating`：**丙b 0.700 > 甲b 0.655 > 丙a 0.610 > 乙b 0.571 > 甲a 0.565 > 乙a 0.481**。
> 5. **新规则**：**R1** 每张牌必须声明 `mainAxis`（M 的唯一决定轴）与 `subAxes`（只贡献悖证）（§3.6）；**R2** 相关轴以"轴不同"为必要条件（§4.3）；**R3** 「补液·支持」轴声明表（§3.6）；**R4** `ΔE` **逐牌四舍五入**（§4.2，卷一即用到 `24.5→25` / `52.5→53`）。
> 6. **§4.3c 新增「伤正触发条件」**：补全原二维表无法表达的**次靶轴相悖**路径（与 `status-syndrome.md` §3.5 对齐）。
> 7. **D11 裁决（§4.5）**：新增 **`no_resistance: true`** 豁免；「补液·支持」适用（生理盐水不产生耐受）。此前 `T_drug` **从未进入任何演算**，照 §4.5 严格执行会使丙a / 乙a 打不完（8 回合、正气 40、`Rating ≈ 0.43`），教学节奏崩塌。
> 8. **§4.10.2 更新**：教学关正气落点由 "80 / 68" 改为 **58–100（六子支区间）**。
> 9. **§7 / §8 同步**：新增 `no_resistance` 旋钮与 `ΔE` 取整 AC；重写 §4.8 相关 AC（六子支可复现、`ex_v1_m_4` 可达、"明知故犯"代价恒 0.084）。
> 10. **本轮新登记的缺陷（未解决）**：**D12** §4.7「回合效率」只有权重没有公式（六支 `Rating` 建立在"取 0.5"的假定上）；**D13** 六子支排序**不再是单维**——甲b（0.655）> 丙a（0.610），与 `vol-01.md` §1.3 教学弧及 §3.6 转归文本（只按寒热分叉）冲突。二者均见 `design/levels/vol-01.md` §6。
>
> 11. **§6 依赖表更新**：关卡系统行由"**它依赖本系统**"改为 **双向**——本系统 §4.3 的对证档 / §4.3c 悖证**以 `revealed` 轴为前提**，须由关卡 §3.4 自由探索窗口保证输入非空（承 N4）。
>
> **同轮改动的关联文件**：`design/levels/vol-01.md`（§1.3 / §3.4 / §3.5 / §3.6 / §4.1 / §6 / §7）· `design/gdd/status-syndrome.md`（§3.5 R1 / §4.3 二维表 / §7 AC）· `design/gdd/level-system.md`（§3.4 自由探索窗口 / §4.2 补全校验 / §6 / §8 AC）· `design/dialogue/vol-01-mother.md`（§5 转归注 / §7 T4 / rev c）。
