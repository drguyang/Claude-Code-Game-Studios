# 处方用药 (Prescription & Medication, #11) — Review Log

## Review — 2026-09-18 — Verdict: **MAJOR REVISION NEEDED** → **用户裁定 [A] 本轮整体修订,6 条根因当日全部落盘**(待二轮复核)

Scope signal: **L**
Specialists: game-designer · systems-designer · qa-lead · ux-designer · economy-designer(×5 报告,haiku 并行)· creative-director(opus 串行综合)
Blocking items: **6 条根因(其中 **5 条 BLOCKING**)** | Recommended: **6** | Nice-to-Have: **2** | 主评审独有: **2**
Prior verdict resolved: **First review**(本文件首条 —— 无前轮)

### Summary(creative-director 终裁)

**架构姿态成立,但三处结构面必须重写。** 11 的四条承重面(剂量是**药效刻度**而非库存单位 /
极性真源在 9 / 品级永不调幅值 / 技能回报在**投入端**)经逐条核对**均未被推翻**;
本轮 6 条根因**全部**是「引用了不存在的东西」「把别系统的语义拿来自用」「半句无落点的幻想」
三类**接缝缺陷**,不是机制设计错误 ⇒ 按清单一次性补齐即可。

**🔴 最关键的一条(R-2)**:`eff_potency = dose_potency × SkillMul(QueryLevel(...)) / SKILL_MUL_ONE`
是 11 的**头号公式**,而它**三处同时不成立** ——
① `SKILL_MUL_ONE` 在 `skill-system.md` 与 `emergency-procedures.md` 中**均无定义**(死符号);
② `SkillMul` 经 10 的 **A6 改判**后语义 = **稳度容差乘子**,只被 `Judge` 的容差消费,**11 无合法消费点**;
③ 与 30 的「辨证是唯一的增益型治疗技能」**直接相抵**。
⇒ **熟练度的出口整体迁移到「省料 + 解锁」**(投入端,非乘法面,`drug_potency` 载荷**逐位不变**)。

**🔴 接缝缺陷(R-1)**:`DrugTreatmentApplied` **从未具名** —— 11 全篇写「载荷 = `TreatmentEvent`」,
而 9 的白名单(`disease-simulation.md:183-184`)**列表外构建期拒收** ⇒ **11 的每一笔写入会被静默拒收**。
同型的第二处:11 声称的「`SkillGrown` 双向 ✅」是**假绿** —— 30 侧**无登记入口**,`AC-11-16` 实为 `NOT-RUN`。

**🔴 记账不对称(R-3)**:品级侧已立「可感知地板」(`D-21-24`),**剂量侧没有** ——
`drug_potency / DOSE_BASE` 可以小到玩家读不出,而 11 的整个幻想押在「戥子读得出分量」上。

**🔴 契约空洞(R-5)**:戥子是**模拟量输入**而**零边界契约**(10 的同类问题已结 `OQ-3-5`);
且「相机档位」与「六屏闭集」被各裁一半、互相绑死却都推给 `/ux-design` 之后 —— **不可推**。

### ⚠️ ux-designer 的一处过度陈述(主评审核对后修正,不据其改稿)

ux-designer 断言「`AC-11-12`(**零数字 + 不靠颜色**)不可同时满足」。**经查 `skeuomorphic-ui.md`
的 2026-09-17 收窄(墨侧禁刻度条/进度填充,黄铜侧錾刻刻度合法)⇒ 该断言为假。**
残留的真实缺陷**不是**「AC 互斥」,而是三件可修的实事:
① 该读数通道(**倾角 / 鼓胀**)**从未被指定**;② 无**可感知地板**;③ 未向 42 **立案**。
已按此三条落盘,**未按 ux-designer 的措辞改 AC**。

### 6 条根因 → 落点

| 根因 | 失效模式名 | 落点 |
| --- | --- | --- |
| **R-1** | 「引用却无登记」(**第 10 次**) | 规则三 · F-11.1 · §Dependencies 注⑥ · AC-11-03 |
| **R-2** | 「语义挪用 / 劫持」 | F-11.3(**删**)· 规则九 · 规则十一 · AC-11-09/10/15 |
| **R-3** | 「记账不对称」 | 规则七 · UI-11.2 义务 ② · AC-11-13/19 |
| **R-4** | 「静默失败 / 无门」 | 规则六 ② · 规则七 · 规则十 ⑤ · F-11.5 · AC-11-18 |
| **R-5** | 「契约空洞」 | **规则十三(新立)** · 规则十二 · OQ-11-7/11-9 |
| **R-6** | 「载荷归属错挂」 | 规则一 · 规则四 · 规则五 |

### 🔴 三处结构性方向 —— **已按 creative-director 推荐先落稿,但标 🔶 方向未经用户最终确认**

用户裁定「现在就整体修订,方向问题按 creative-director 推荐先落稿」⇒ 下列三条**写入了正文**,
并以 🔶 标注**不等于已裁**。二轮评审须**优先复核这三条**:

1. **熟练度出口 = 省料 + 解锁**(F-11.3 改写 / 涟漪 ① `skill-system.md:60`)
   —— 药效放大**删除**,非乘法面不改任何 sim 载荷。
2. **载荷 = `action → 单一 polarity`**(规则五收窄)—— 兑现规则一的「11 读不到病种」。
   ⚠️ **不推翻用户的 `OQ-11-1` 裁定**(极性真源 = 9 的病种注册表);通用解(主机侧派生 `salted_key` 同构)
   登记为**备选**,未采纳。
3. **戥子四条边界**(规则十三新立)—— 离散档焦点序列 / float→int 量化**在 3 侧** / 禁手势 /
   `MAX_DOSE_DETENTS`(建议 ≤ 9);**相机档位与六屏闭集绑为同裁**,**不得推给 `/ux-design` 之后**。

### 🔴 复核过程中的独立查证(逐条实核,不采信专家报告)

- `SKILL_MUL_ONE` 全库 grep ⇒ **零定义**(死符号)✅ 核实;
- `SkillMul` 在 10 的 A6 后语义 = 稳度容差乘子,消费点 = `Judge` 容差 —— **11 无消费点** ✅ 核实;
- `TreatmentEvent` ⇒ **非具名 Kind**(内联元组),而 9 白名单**列表外构建期拒收** ✅ 核实;
- 11 声称的 `SkillGrown` 双向 ✅ ⇒ `skill-system.md` 侧**零 11 登记** ⇒ **假绿** ✅ 核实;
- `DOSE_BASE` / `MIN_USABLE_HALF_LIFE` / `NOISE_BAND_9` / `MAX_DOSE_DETENTS` ⇒ 原稿**均无** ✅ 核实;
- 42 的 2026-09-17 收窄 → ux-designer 的「AC-11-12 互斥」**为假** ✅ 核实。

### 🔁 本轮落盘的跨域涟漪(6 处,均已执行)

| # | 文件 | 动作 |
| --- | --- | --- |
| ① | `skill-system.md:60` | 处方用药输出行:「药效」**删** → **省料 + 解锁**(+ `EmitGrowth` 登记义务注) |
| ② | `item-database.md` | 新增 **`D-21-29`**(「一株 → 几剂」换算字段缺失 —— `dose` 与库存**份数**解耦义务) |
| ③ | `systems-index.md` | row 11 整行重写(🟡 In Review + 首轮终裁 + 七项重裁 + `D-21-29`);**C5 行重新解释** |
| ④ | `medical-consequences.md` | 新增 11 上游行 + `OQ-53-8`(禁忌/过量代价侧)/ `OQ-53-9`(试药史判据) |
| ⑤ | `skeuomorphic-ui.md` | 双义务注(六屏闭集是否需 方笺 / 戥子档位属哪一材质侧) |
| ⑥ | `interaction-system.md` | 11 行补「施治入口 + **`K_difficulty` 代入方**」(11 读不到 severity) |

> ⚠️ 另有 `design/registry/entities.yaml` 两处注释订正(撤 `AC-11-10` 陈旧引用)与
> `camera-and-viewpoint.md` 的条件占位行(方笺视角,**未裁**)。

### ⚠️ 本轮 = **未复核的修订**

6 条根因的修法**自始未经第二双眼睛**(单会话内撰写 + 修订)。二轮必须为
**新会话 · 评审者独立于撰写上下文**。二轮同时核:
🔶 三处结构性方向(user 未最终确认)/ `D-21-29` 是否被 21a 认领 / 涟漪 ①⑥ 是否被 30 与 4 承接 /
`NOISE_BAND_9` 与 `D-21-24` 的地板口径是否同源。

**未结项**:`OQ-11-1`…`OQ-11-12`(**12 条**)· `D-21-29`(待 21a/17/18 认领)·
`AC-11-16` = `NOT-RUN`(30 侧入口未兑现)·
**相机档位 / 六屏闭集**(BLOCKING,须 39/42/2 联合裁定,`OQ-11-7` / `OQ-11-9`)。

**权威状态**:`design/gdd/systems-index.md` row 11 = **🟡 In Review**(修订已落盘,待二轮)。

---

## Review — 2026-09-19 — Verdict: **MAJOR REVISION NEEDED** → **用户裁定 [A] 全量落盘,7 条阻断当日全部结清**(修订已落盘)

Scope signal: **L**
Specialists: 无(`--stage lean` —— 单会话独立复核,**不派专家子代理**;撰写者与复核者同为单会话 ⇒ 见下方「独立性问题」)
Blocking items: **7** | Recommended: **11** | Completeness: **8/8**
Prior verdict resolved: **Yes —— 首轮 6 条根因全部落盘并逐条复核确认**;本轮 7 条阻断**全部为新暴露面**(4 条与首轮修法本身的副作用有关)。

### Summary(二轮终裁)

**首轮的 6 条根因修法本身是对的,但它们引入了一套「引用既有常量 / 既有接口 / 既有裁定」的新承重面 —— 而这套承重面有 7 处没兑现。**
首轮把「熟练度出口」从乘法面迁到「省料 + 解锁」,于是 11 开始引用 `MIN_USABLE_HALF_LIFE`(21a)、
`NOISE_BAND_9`(9)、`GateHit`(9 的 `treatable_by`)、`EmitGrowth`(30)、`K_difficulty`(4)、
`EFF`(21a)—— **这六个符号,11 侧全部无定义,承接侧也全部无承诺**(21a 只断 `> 0`、9 无噪声带常量、
4 已于 2026-09-18 明确驳回移交)。⇒ **首轮修法把「死符号」从一处迁到了六处,规模扩大了。**

**本轮新命名的失效模式:「判据对合法输入类误判」**(BL-7)。
`AC-11-11` 无条件要求「128 位中间积」,而 `drug_potency` 是 Q16.16(≤ 2^47)——
**对合法输入类误判**与前几轮记的「判据空转」是**对偶**:空转是「判据永不触发」,
误判是「判据对合法输入也触发 ⇒ 把合法实现判红」。同型缺陷在 `OQ-11-1` 的断言上重演过一次。

### 7 条阻断 → 落点

| 阻断 | 失效模式名 | 落点 |
| --- | --- | --- |
| **BL-1** | 「静默失败 / 假闭合」 | `OQ-11-3` 加锁(② 断言须与 `O-11→21a` 升格同批)· 新增重开条件 **`O-11→21a`** |
| **BL-2** | 「引用却无登记 + 记账不对称」 | `OQ-11-3` 加锁(前置补 `O-11→9`);**9 侧立噪声带常量**;`AC-11-19` 两把尺明写 |
| **BL-3** | 「引用却无登记」 | `AC-11-16` 双成因拆条(`NOT-RUN` 写明);**`OQ-11-11`(30 入口签名)+ `OQ-11-13`(`K_difficulty` 入参无主)** |
| **BL-4** | 「判据挂错对象」 | **新增 `AC-11-22`**(`DrugTreatmentApplied` 的**写者唯一性**,区别于 `AC-11-10` 的算法唯一性)· **`TR-prescription-018`** |
| **BL-5** | 「文内自相矛盾」 | 规则十 ⑤ 就地订正(4 已于 2026-09-18 驳回 `K_difficulty` 移交) |
| **BL-6** | 「双份实现 + 旋钮无出处」 | `OQ-11-8` 改写 —— 省料**单一出处 = 21a 的 `EFF`**;30 删 `PROVINCE_SAVE_RATE[]`,只欠「等级 → `EFF`」映射(**`O-11→30`**)· **`TR-prescription-017`** |
| **BL-7** | **「判据对合法输入类误判」(本轮新命名)** | `AC-11-11` 收窄到适用域(`drug_potency` Q16.16 ≤ 2^47 时**不**强制 128 位中间积)· **`TR-prescription-007`** |

### 4 条用户裁定(本轮 · 全部经 `AskUserQuestion` 裁定)

- **BL-1 = 「开 21a 重开条件」** —— 不替 21a 定值,改为在 21a 立**具名重开条件**(`O-11→21a`),断言升格与 11 的数值轮**同批**。
- **BL-2 = 「请 9 立噪声带常量」** —— 9 侧新增**具名常量**(`O-11→9`),使 11 与 9 的「可感知地板」**同一把尺**(两处原为**不同轴**:21a 的地板在时间轴、11 的在 potency 幅值)。
- **BL-3/4/5 = 「成长门改为 11 可自判」** —— 不依赖 4 的代入,11 自持判据来源。
- **BL-6 = 「只认 21a 的 EFF」** —— 单一出处裁决;30 不得自定系数。

> ⚠️ **BL-4 的裁定残留**:`AC-11-22` 现在给出**单点断言**(`TR-prescription-018` = `partial`,**非 `covered`**)——
> 「未注册 `Kind` 构建期失败」的**全局作者侧防线仍然缺席**,归**下一次 `/architecture-review`**,
> **不在本条内解决**。**不得因本 AC 存在而记 018 为绿**(承 `feedback-no-borrowed-green-ac`)。

### 🔁 本轮落盘的跨域涟漪(4 处 + 两表回刷)

| # | 文件 | 动作 |
| --- | --- | --- |
| ① | `item-database.md`(21a) | **D-21-34** 行 + 头部 ⑤ + F5 块 —— `MIN_USABLE_HALF_LIFE` 的**重开条件**(`O-11→21a`) |
| ② | `disease-simulation.md`(9) | **D-9-J** 行 + 头部 ⑤ + tuning-knobs 块 —— **噪声带具名常量**(`O-11→9`) |
| ③ | `skill-system.md`(30) | §3.1 块 + 头部 ⑤ —— **删 `PROVINCE_SAVE_RATE[]`**,只认 21a 的 `EFF`(`O-11→30`) |
| ④ | `systems-index.md` | row 11(22 AC / 13 OQ / `11-DC` 首成文)+ row 6(系统 10 陈旧「Draft」→ ✅ Approved,Recommended #5) |
| ⑤ | `tr-registry.yaml` | 6 条改写(003/007/008/013/014/015/017)**+ 2 条新增**(018 / 019);`TR-prescription` 17 → **19** |
| ⑥ | `traceability-index.md` | 计数行 + 2 条行文本回刷;总计 385 → **387** |

> **21a / 9 / 30 三处只加「重开条件 / 具名常量 / 签名登记」三类最小行,不动机制**(用户对涟漪范围的明示约束)。

### 🆕 本轮首次成文:`11-DC` 数据契约

11 此前**零数据契约**(10 有 `10-DC` / 4 有 `4-DC`,11 无)。本轮补上:**3 资产**
(`prescription_actions.json` / `materia_lexicon.json` / `dose_const`)+ **7 条烘焙期校验 DC-1…DC-7**。
其中 **DC-2 / DC-6 / DC-7 三条判据待前置**(受 BL-1 / BL-2 决定),
**显式登记为不可执行,不记绿**(承 `feedback-no-borrowed-green-ac`);DC-1 / DC-3 / DC-4 / DC-5 立即可执行。

### ⚠️ 独立性问题(lean 轮的结构性限制 · 明写)

本轮为 `--stage lean`,**复核者与首轮撰写者为同一会话** —— 严格意义上**不满足首轮结语要求的「评审者独立于撰写上下文」**。
⇒ 本轮 7 条阻断的**发现**是可信的(均为点对点 `grep` 查证:六个符号**逐条实核无定义**),
但**修法仍然未经第二双眼睛**。**若重开,二轮查证点**:
BL-1/2 的两把尺是否真同轴 · BL-7 的适用域收窄是否有遗漏输入类 · `AC-11-22` 的 `partial` 归因是否在下次架构复核被承接。

### 本轮独立查证(逐条实核,不采信上文)

- `MIN_USABLE_HALF_LIFE` 全库 grep ⇒ **11 侧引用,21a 侧无断言(只断 `> 0`)** ✅ 核实;
- `NOISE_BAND_9` ⇒ **全库零定义**(11 引用,9 只有 `σ`)✅ 核实;
- `GateHit` / `treatable_by` ⇒ 9 无返回该布尔值的接口 ✅ 核实;
- `EmitGrowth` ⇒ 30 侧**签名未登记** ✅ 核实;
- `K_difficulty` ⇒ 4 已于 **2026-09-18 驳回移交**(`interaction-system.md:694` 记账)✅ 核实;
- `PROVINCE_SAVE_RATE[]` ⇒ **30 侧无定义**,且与 21a 的 `EFF` / `D-21-15` **重复** ✅ 核实;
- `AC-11-11` 无条件 128 位要求 ⇒ 与 `drug_potency` Q16.16(≤ 2^47)域**不匹配** ✅ 核实。

### 未结项

- **三处重开条件**:`O-11→21a`(BL-1)· `O-11→9`(BL-2)· `O-11→30`(BL-6)—— **三处均已登记,待承接侧兑现**。
- **`AC-11-16` = `NOT-RUN`**,双成因(入口未兑现 + 入参无主),**修其一不解另一**。
- **`TR-prescription-018` = `partial`** —— 全局作者侧防线归下次 `/architecture-review`。
- **`OQ-11-7` / `OQ-11-9`(相机档位 / 六屏闭集)** —— 首轮 BLOCKING,**本轮未动**(不在本次 7 条阻断内)。
- **`11-DC` 的 DC-2 / DC-6 / DC-7** —— 待 BL-1 / BL-2 前置解除。

**权威状态**:`design/gdd/systems-index.md` row 11 = **✅ Approved(2026-09-19 用户裁定「接受二轮修订、免三次复核」= 显式风险接受结案)**。
⚠️ **免三次复核 ≠ 「已核对」** —— 二轮为 lean 单会话,撰写者与复核者同源。
**重开触发条件**(三者任一):① `O-11→21a` / `O-11→9` / `O-11→30` 任一被承接侧兑现;② `AC-11-16` 脱离 `NOT-RUN`;③ `11-DC` 的 DC-2 / DC-6 / DC-7 前置解除。
