# 音频系统 (Audio System) — 44

> **Status**: **✅ Approved**(2026-09-18 用户裁定接受首轮修订、**免二轮** · 2026-09-15 成稿 · 八必备节 + Visual/Audio + UI + Event Table Schema · **32 条 AC**)
> **首轮 `/design-review`(2026-09-18,`full`)· 裁定 MAJOR REVISION NEEDED · 8 位专家 + creative-director 综合**
> **→ 同日修订落盘**(8 项阻断 + 16 项推荐全修;4 项 D 裁决由用户裁定,见 §修订记录)。
> **用户裁定:接受修订、免二轮** —— **免二轮 = 显式风险接受,不是「已核对」**;重开触发条件见 §修订记录。
> 评审史见 `reviews/audio-system-review-log.md`。
> **Author**: dr_guyang + audio-director / sound-designer / unity-specialist
> **Last Updated**: 2026-09-18(**2026-09-20 `/review-all-gdds` 回刷**:7b 事件名枚举落点收口 ·
> `AC-44-15` 字幕呈现落点 42 回刷 —— 承 R-2⑤,机制与数值不变)
> **Implements Pillar**: 支柱一「判断为骨,手感为肉」
> **Authority**: `docs/architecture/adr-018-audio-architecture.md`(✅ Accepted 2026-09-15)

## Overview

**44 音频系统是表现层的一条「语义 cue → 声音」通道,与 42 拟物 UI 同构 ——
只触发 / 只渲染,永不持有游戏状态(ADR-018,✅ Accepted,本 GDD 的权威件)。**

它的技术面:**上游系统在表现层发出语义 cue**(13 的咳嗽呻吟 · 37 的结案纸页 ·
52 的预告线索 · 8 的听诊层 / 9 的呼吸语义),44 只把 cue 变成声音 ——
经由 **`AudioCueDto`**(无 `disease_id`,整数语义,受 `PresentationDtoGuard` 递归扫描)。
音频事件表走 **ADR-014 烘焙管线**(玩家构建零 JSON 解析器);混音拓扑为
`Master / Music / Ambience / Voice / SFX / Stethoscope / UICue` 七条总线 + 行为驱动的快照清单。

它的玩家侧:**你是**先听见后看见**的那个人**。玩家可以在隔壁房间听见病人呼吸变坏、
听诊器里听见细湿啰音(吸气象、断续、撕 velcro 那种)——**而系统永不叮一声告诉你「他转危了」**。
「无提示音」铁律由白名单断言机械化(AC-44-09 BLOCKING),让「判断为骨」在听觉侧成立:
**听见变化是你的本事,不是系统的通知。**

> **职责边界(一句话)**:44 不订阅 sim 真值、不写三流、不进 sim 程序集;
> 它消费的是**表现层语义 cue**,不是**游戏状态**。任何「我需要知道病种才能选素材」的需求,
> 语义解析都在上游完成,44 只收结果 cue id。

## Player Fantasy

**「门后的人还在喘气。」** —— 玩家可以在**看不到病人**的情况下,仅凭声音判断他是否转危。
这是无血条设计(支柱一「判断为骨,手感为肉」)在**听觉侧**的兑现:
`diagnosis-system.md:171` 转引的 9 的幻想「世界不为你暂停」,唯一不依赖 UI 的外显方式就是声音。

**玩家应该是那个「听见了什么」的人,不是那个「收到了通知」的人。**

- **听诊器里的世界**:低技能 ≠ 把啰音静音 —— 低技能 = 更高的接触噪声底、
  更窄的通带、更少的频段细节(摩擦声、衣料声、病人自己的呼吸声混进来)。这正是真实的实习医生体验
  (`diagnosis-system.md:1588-1591`)。**你的耳朵不会说谎,但你的耳朵也提醒你:「你没练过听」。**
- **变体库混合的病人语声**:病人是**人**,不是音效机 —— 男 / 女 × 强 / 中 / 弱 × 数种句尾,
  按 (病种语义, 精度档, 强度) 选变体并交叉淡化。**禁**把已录语声做五项参数调制
  (音高 / 气声 / 语速 / 断句 / 共鸣)—— 那是「变声器」,不是「病人说话」。
- **世界声是线索,不是警报**:狗吠 / 马蹄 / 锣声 / 鸟群惊飞(52 预告线索)是**可以听出来的前兆**,
  不是「红色感叹号的声音版」。玩家学到的不是「叮 = 危险」,而是「今晚的鸟在乱飞」。

**参考锚**:英灵神殿的**风声 / 门吱呀**—— 世界有声音,但从不替你说话;
《双城》式的拟物声(笔尖走路、翻纸)让「翻开脉案」本身有触感。
**要避免**:任何「成就达成」式 stinger、任何「转危报警」式 ducking、任何「玩家做对了就播对音」的素材切换。

## Detailed Design

### Core Rules

**规则一 · 44 与 42 同构 —— 只触发 / 只渲染,永不持有游戏状态(ADR-018 §一)。**
44 不读 sim 真值、不写三流、不进 sim 程序集(门 A 不受影响)。
**数据入口只有 `AudioCueDto`**(2026-09-18 修正:原写「`AudioCueDto` + `VitalsDto`」——
`VitalsDto` 全文无任何消费者 / 公式 / AC 引用,是一处**绕开 AC-44-09 白名单的旁路**
(可直接由体征驱动呼吸声,不经上游 cue 语义)。**已删除**;ADR-018 §一 表同批同步。
44 的**唯一**输入是上游语义 cue,输出只有 `AudioMixer`。)

**规则二 · 音频触发源必须来自「行为反馈白名单」,不得来自「状态播报」(ADR-018 §六)。**

| 类别 | 例 | 归属 |
|------|-----|------|
| 玩家自身动作 | 笔尖走路 / 落笔 / 翻纸 / 器物放置 / 脚步 | 42 / 44 |
| 玩家主动的检查动作 | 听诊器接触声 / 摩擦声 / 衣料声 | 44(听诊层) |
| 玩家可感知的世界声 | 狗吠 / 马蹄 / 锣声 / 鸟群惊飞 / 哭喊 / 钟声 | 52 → 44 |
| 持续生理声(非状态播报) | 呼吸层的**连续**变化(平稳→急促→停顿) | 9 语义 → 13 触发 → 44 |
| **战斗乐层(连续织体)= 第 5 类**(2026-09-17,25 的 V4/V7 回填) | 遭遇期乐层的**缓变**(淡入淡出,**无边界拍**) | **表现层音频观察者订阅遭遇态 → 44**(27 永不点歌);护栏 **G1 禁帧对齐 / G2 去标注盲测 / G3 本地设备侧触发** 全文见 **ADR-018 §六「音乐层例外」条** —— 无护栏则例外不成立,黑名单四禁对本类原文适用 |

黑名单(禁止 —— 状态播报):任何 sting / jingle / 提示音宣告状态变化;
用 ducking 报状态;用素材切换报状态;用音频表达本来该由玩家读数得出的结论。

> **⚠️ 2026-09-18 修订(首轮评审阻断 2;原机械化**不可执行**)** —— 原文 AC-44-09 只断言
> 「每个 CueId ∈ 白名单类别」,但**事件表 schema 里根本没有类别字段**
> (ADR-018 §Key Interfaces 的示例行 = `{cue, assets, tier_params, loop, bus}`),
> 且 schema 本体被推迟到 `OQ-44-4` —— **BLOCKING 门骑在一个 Open Question 上,无载体**。
> 更糟:**只查内容不查时机**。反例:13 恰在病人跨过危险阈值的那个 tick 发一条咳嗽 cue ——
> 咳嗽在合法类别里、断言全绿,但**那条咳嗽就是警报**。52 的档位表
> (威胁=狗吠 / 灾难=锣声)同理 —— 它是「状态枚举 → 声音」的映射穿了件拟物外衣。

**机械化(AC-44-09,BLOCKING)**:音频事件表 schema **必须携带两个字段**,
`OQ-44-4` 由 Open Question **升格为本 GDD 的定型小节**(见 §Event Table Schema):

| 字段 | 类型 | 作用 |
|------|------|------|
| `whitelist_category` | enum(5 类) | 内容侧:cue ∈ 上表五类之一(**含第 5 类战斗乐层**) |
| `trigger_source` | 闭枚举 | **时机侧**:见 §Event Table Schema 的合法 / 禁止集 |

**双重负向夹具(缺一不可)**:
① 注入一条 `whitelist_category: Sting` 的行 → 断言**必须失败**;
② 注入一条**类别合法**(如 `PlayerAction`)但 `trigger_source: VitalsCrossed` 的行
→ 断言**必须同样失败**(这是堵住「阈值时刻的咳嗽」的那一半)。
`OQ-44-4` 未落盘前置 **NOT-RUN 守卫**,不得放行(见 §Event Table Schema)。

> **⚠️ 2026-09-18(25 二轮 E3,用户裁定 [甲],详见 `combat-and-weapon-lines.md` §6.3/§6.4/旋钮表)**:
> 战斗音频的触发 = **事件类别**(onset 到达 / 挥空 / `EncounterEnded`),
> **任何音频参数(音量 / 音色 / 声像)不得读 `magnitude`** —— 与 ADR-018 §六 无提示音铁律同向:
> 听觉侧不允许出现「伤害数字」的第二载体。昏迷后静默的触发源 = `EncounterEnded`,不设「昏迷瞬间」专用 cue。

**规则三 · 快照由行为驱动,绝不用来播报状态(ADR-018 §三)。**
`StethoscopeFocus` 是玩家**自己**拿起听诊器触发的;`Paused` 是暂停。
快照切换**不**用于「病人转危 ⇒ 压低环境以突出心跳」。

> **⚠️ 2026-09-18 修订(首轮评审阻断,`DialogueFocus` 自相矛盾)** —— 原 `DialogueFocus`
> 触发源 = 「病人语声播放」。而病人语声(呻吟 / 咳嗽)由 13 依病情发出 ⇒ **病情恶化 = 呻吟更频
> = 混音器自动压低环境音** —— 这**正是**本条禁止的「转危 ⇒ duck 环境」,只是绕过了 cue 层
> 走快照层,AC-44-09 的白名单抓不到。ADR-018 §三 原文「行为 / 场景状态」措辞把病情
> (一种「场景状态」)也包了进去,使 AC-44-C1 不可证伪。
> **修订**:铁律判据收窄为 **「无 *非拟物* 触发源」** ——
> **拟物**的让位(世界因一个**你能定位的声音**而让位)合法;**非拟物**的 ducking(混音器替你
> 给这一刻打分)非法。据此:`DialogueFocus` 仅限**玩家主动发起**的对话(玩家点选病人交谈),
> **病人自发呻吟 / 咳嗽不触发**该快照(它们走 Voice 总线,不压环境)。
> **AC-44-C1 同步改写**为「无任何快照由**非拟物**的状态变化触发」。

**规则四 · 呼吸分两层渲染,精度档 = 通带与噪声底,不是静音开关(ADR-018 §四 需求①)。**
基础气流层 + 附加音层(细湿啰音等)。**低技能 ≠ 静音,低技能 = 更吵的接触噪声、更窄的通带。**
**AC-44-01(BLOCKING)**:三档下附加音层**始终非零**;断言无「档位 ⇒ 静音」路径。
**AC-44-08**:附加音层**不得**实现为连续水声(细湿啰音 = 吸气末、断续、细密的爆裂音,咳嗽后不消失)。

**规则五 · 语声走「变体库混合」,不是「参数调制」(ADR-018 §四 需求②)。**
男 / 女 × 强 / 中 / 弱 × 数种句尾;按 (病种语义, 精度档, 强度) 选变体并交叉淡化。
**禁**对已录语声做音高 / 气声 / 语速 / 断句 / 共鸣五项独立调制。

**规则六 · 接触噪声 / 信噪比是暴露参数(ADR-018 §四 需求③)。**
`Stethoscope` 总线暴露接触噪声底与信噪比,由精度档驱动。
`READ_FLOOR_MIN > 0` 在音频侧有对应实现:背景噪声永不为零,但也不毁掉信号。

**规则七 · 听诊音精度 = 本机玩家自己的技能档(2026-09-18 用户裁定 D-A,重开 2026-09-14 裁决)。**

> **⚠️ 原裁决已作废**:原写「联机下统一取**主机**技能」,**理由**是「单 `AudioListener` 每设备
> 只有一条混音总线(结构性约束)」—— 该理由**为假**,三个专家独立证伪:
> 钉死的 `audio.md:256-266` 只说「**同一时刻只有一个 `AudioListener` 处于活动状态**」
> (= 一个空间化参考点),**从未说「每设备一条总线」**。而联机是 **1-4 名玩家各自一台机器、
> 各自一个 Unity 实例、各自一套完整 `AudioMixer`**(`game-concept.md:27`)——
> 「四人共用一条总线」只在 **split-screen** 下成立,本作**没有** split-screen。
> ⇒ 每人按自己技能设档 **零同步成本**。
> **设计后果(裁定 D-A 的决定性理据)**:原裁决会让**非主机玩家的听诊保真度冻结在主机档**,
> 听觉侧的技能成长**永不发生** —— 与 `game-concept.md:124`「你认得出」及本 GDD
> §Player Fantasy 的「**你的耳朵提醒你:你没练过听**」直接冲突;
> 还会制造**反向激励**(新手当主机拖低全队保真度;老手当主机则新手可跳过学习)。

**裁决**:`SetTier(TierSource.Local)` —— 每台设备按**本机玩家自己**的技能档渲染听诊音。
技能档(一个只读整数)**由 sim 同步到表现层**;脉案字体量**早已**各自分叉(铁律⑤),
其数据路径**本就存在**(客户端已必须读本地技能值显字)—— 听觉侧复用同一条路径。
**边界**:分叉的是音**与**字,统一的是**事实**(病情由主机唯一执行,ADR-005 不变)。
**若日后支持 split-screen**,则「单总线」约束在该形态下**真实成立**,须**另开 ADR** 登记例外,
不得以本 GDD 修补。
远端实体的空间化复用 ADR-001 §一之二的 **`IPositionalChannel`**(per-`ActorId` latest-value 表,
2026-09-18 自 `IReplayPipe` 拆出),44 **只读取声源锚点、不自建位置通道、不新增 QoS 通道、不做语音通道**。

**规则八 · 44 不拥有音频素材的语义选择权。**
若「需要病种才能选素材」—— 那是上游(8 / 13)的责任,上游解析成 cue id,44 只收结果。
`AudioCueDto` 无 `disease_id`,`PresentationDtoGuard` 递归扫描覆盖。

**规则九 · 关键音频的视觉替代 = 引用 13 已交付之物 + 显式 P0 非目标(无障碍,ADR-018 §八;2026-09-18 裁定 D-C)。**

> **⚠️ 原条文是标语不是规格**:原文「关键音频必须有视觉替代」**未命名任何通道、未指派任何系统**,
> 且本 GDD §UI Requirements 明写 **44 零玩家可见 UI** ⇒ 44 **物理上无法自己渲染**替代物。
> 更根本:幻想的定义恰恰是「**看不到**病人,仅凭声音」—— 拟物视觉信号(呼吸幅度等)**需要视线**、
> **不能穿墙**,因此**没有任何视觉替代能重建那条跨房间通道** —— 那条通道**就是**论点本身。
> 而最自然的替代(面色 / 口唇发绀)是**纯颜色通道**(WCAG 1.4.1),色觉盲玩家读不出
> 「发绀 vs 苍白」—— **替代物自身不可及**。另:病人语声**无字幕 AC**(WCAG 1.2.2)。

**裁决(三条具名项 —— 承 13 的 `AC-13-F1/F2/F3` 先例)**:
1. **视觉替代 = 直接引用 `AC-13-F1`**(咳嗽 / 呻吟 / 呼吸三类 cue 均有**默认可见**对应物:
   姿态 / 面色 / 呼吸幅度;**由 13 拥有,44 不新造组件**)。
2. **显式 P0 非目标**:「**隔墙听觉检测在 P0 无视觉等价物**」—— 聋哑玩家在 P0 失去本设计的主张。
   这是**诚实登记的代价**,不是靠「双通道冗余」一句话糊过去(与 13 的 AC-13-F3 同形)。
3. **44 自己的非视觉义务**:mono 选项 · 各总线独立音量 · **病人语声字幕**(WCAG 1.2.2)。

**证据类型 = ADVISORY**(不可自动化 —— 承 `patient-ai.md:1152`「硬标 BLOCKING 只会被绕过」)。

### States and Transitions

**44 是表现层,状态是「混音 / 播放」状态,不是游戏状态。** 分两层:

**① 总线与快照层(ADR-018 §三):**

| Snapshot | 用途 | 触发(必须由行为驱动) | 回退 |
|----------|------|----------------------|------|
| `Default` | 常规游玩 | 默认 | — |
| `StethoscopeFocus` | 听诊聚焦(压 Ambience / Music,抬 Stethoscope) | **玩家主动**进入听诊动作 | 玩家离开听诊动作 |
| `DialogueFocus` | 语声优先(压 Ambience / Music) | **玩家主动发起**的对话(点选病人交谈)—— **病人自发呻吟 / 咳嗽不触发**(2026-09-18 修订,见规则三) | 对话结束 |
| `Paused` | 全总线衰减 | 暂停 | 恢复 |
| `VRComfort` | VR 模式(见 ADR-018 §七) | **P1b** | — |

> **快照 / 脚本参数归属分离(2026-09-18 增补)** —— Unity 快照会**插值它存储的全部参数**:
> `DialogueFocus → Default` 回退会把 `Stethoscope` 组的档位滤波 / 噪声底参数**一并拉回**
> 快照存储值,**静默覆盖脚本控制**。两个控制面从未分离 —— 这是实现期必踩的坑。
> **纪律**:快照**只拥有** Ambience / Music / Master 的音量;
> `Tier` / 接触噪声底等参数**不入任何快照**,**只由脚本驱动**。

**② 播放句柄层(循环 cue):**

```
BeginLoop(cue) → [Looping] ── EndLoop(handle) → (释放)
```

- 循环 cue(呼吸层 / 环境)由**唯一持有者(上游)**负责收尾;44 不自行决定何时结束。
- 一次性 cue(咳嗽 / 翻页 / 狗吠)即发即弃,无状态。

> **⚠️ 2026-09-18 增补(首轮评审:`EndLoop` 可能永不到达)** —— 「由上游收尾」的**前提是
> 上游收得到收尾依据**。但 ADR-001 的 `ReorderBuffer` **超界即丢弃**(`adr-001:131`),
> 且 25 §6.6 T2 已承认 onset 事件可被丢 —— 若上游**从未收到**收尾依据事件,
> 呼吸层 / 乐层**永不停止**(「忽略过期句柄」覆盖不了「`EndLoop` 根本没来」)。
> **纪律**:循环 cue 必须有**自评兜底** —— 从最新快照 `Progress` 自行求值是否该结束;
> 或声明「重连时拉流重放重建句柄」(须 45 侧登记)。二者择一,不得留空。

**禁**:任何「播放状态」被解读为「游戏状态」—— 44 的状态机只描述**声音正在发生什么**,
不描述**病人正在发生什么**。

### Interactions with Other Systems

**44 的接口(ADR-018 §Key Interfaces)**:`IAudioCueSink`(只入不出)—— `Emit(in AudioCueDto)`
一次性 / `BeginLoop` + `EndLoop` 循环 / `SetTier(TierSource)`(**单机 = 本地;联机 = 本机玩家本地档**,
2026-09-18 裁定 D-A —— `TierSource.Local` 为默认且联机下亦然)。

| 上游系统 | cue 语义 | 44 的动作 |
|---------|----------|----------|
| **13 病人 AI**(`adr-016:232-236`) | 咳嗽 / 呻吟(表现层映射,不经 sim) | 变体库选语声 / 一次性 cue |
| **37 病例系统**(`case-system.md:539-548`) | 结案纸页翻合 / 立案落纸 | 纸面物理声(禁「成就达成」音效) |
| **52 随机事件导演**(`random-events.md:977,1058`) | 狗吠 / 马蹄 / 锣声 / 铃声 / 钟声 / 鸟群惊飞 | 世界声 cue,空间化 |
| **8 诊断**(`diagnosis-system.md:1588-1610`) | 听诊层:呼吸两层 + 接触噪声 + 精度档 | `Tier` + `Intensity` 驱动通带 / 噪声底 |
| **9 疾病模拟**(`disease-simulation.md:1111-1121`) | 呼吸连续变化语义(平稳→急促→浅慢→停顿) | 循环呼吸层(连续变化,禁突变音效) |
| **42 拟物 UI** | 笔尖 / 翻纸 / 器物 | UICue 总线 |
| **18 炮制**(2026-09-19,承 18 首轮评审 BL-10 补行) | 起炉 / 起货 / 捣药·翻炒·注水·火声行为床 | SFX 总线一次性 cue + 循环声床;**炉火声床禁被 StethoscopeFocus 压没**(18 Visual/Audio);去标注盲测与 18 的 AC-18-22 联合 |
| **2 摄像机 / 44 自身** | `AudioListener` 单挂点(平面 = 主相机,VR = 头显,ADR-020 §七) | 唯一 Listener |
| **45 网络层** | 远端表现态位置(第二 QoS)+ 病人 / 受伤实体锚点 | 空间化锚点(**不自建通道**) |

**数据契约**:`AudioCueDto = { CueId, byte Intensity(0–255), WorldPos Cell(整数格),
PatientId Source(可为 None), bool Looped }`。无 `disease_id`;无 float。

> **2026-09-18 修订(两处)**:
> ① **`Tier` 字段移除** —— 见下方 §Formulas F-44.1 注:档位是**设备级**的(`SetTier`),
> 一个 `AudioMixer` 组是**全局共享**的,逐 cue 带 `Tier` 会让不同档的声源互相踩滤波。
> ② **DTO 与 `IAudioCueSink` 住独立契约程序集(仅 BCL)** —— 原 `AudioCueDto` 承载
> <!-- ⚠️ 2026-09-20 ADR-025 回写加注:「独立契约程序集」称谓作废,现名 = `Sim.Contracts`
>      (AudioCueDto + IAudioCueSink 在 ADR-025 §① 成员列内)。本行「仅 BCL」判据不变。 -->
> `WorldPos` / `PatientId`。若这些类型住 sim 程序集,则 44 引用它们即**违反 AC-44-B1**;
> GDD 从未说明它们住哪。**定型**:契约程序集只依赖 BCL,字段用原语(`int3` / `int`);
> sim → 契约的类型映射由上游在边界处完成。AC-44-B1 的判据同步从 **grep 升级为 asmdef 引用白名单断言**。

**44 不拥有**:素材语义选择(上游)· 音频素材本体(`assets/audio/` 归 44 拥有,走 Addressables 流式)·
语音通话(不做,ADR-018 §五)· VR 音频(**P1b**,ADR-018 §七;层级订正见下注)。

**双向性核对**:上面每个上游系统的 GDD / ADR 都已显式声明其 44 侧接口
(见 §Dependencies 的引用)—— **除两处外可回溯**:
① **42 拟物 UI 已成稿**(`skeuomorphic-ui.md`,2026-09-16;ADR-013 覆盖,44 侧登记为「42 的呈现事件 → UICue」,42 侧回执见 `AC-42-F2` 的 cue 白名单);
② ~~**45 网络层 —— ADR-001 全文未提 44**~~ ✅ **已闭合(2026-09-18,用户裁定 D-D)**:
`ADR-001 §一之二` 现登记 **`IPositionalChannel`**(per-`ActorId` latest-value 表)+
**消费者列表**(44 音频 / 动画表现)+ **病人 / 受伤实体锚点发布者** + **cue 非复制**语义;
44 侧由 **AC-44-D1** 指向该节。

## Formulas

> **44 的「数学」几乎全是映射,不是计算。** 三处硬约束(§Formulas 全部满足):
> ① `Intensity` 与 `Tier` 是 `byte`(0–255 / 0–2),在**呈现层**是渲染参数,**不回写 sim**,
> 不受 ADR-006 的禁 float 约束(它们在定点域之外);但取整数是为了与上游整数语义一致。
> ② **44 不拥有数值** —— 精度档曲线 / 阈值 / 通带参数全部是**8 的 F-8.x 或旋钮的映射输入**,
> 44 只做形状映射。
> ③ 任何「档位 ⇒ 静音」路径 = **AC-44-01 断言失败**(BLOCKING)。

### F-44.1 精度档 → 通带与噪声底(听诊层)

The tier-to-filter mapping formula is defined as:

`filter_params = TierMap[Tier]`(表驱动,无运算)

**Variables:**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| Tier | T | byte | 0 低 / 1 中 / 2 高 | **设备级**:由 `SetTier(TierSource.Local)` 设定,来自本机玩家自己的技能档(2026-09-18 裁定 D-A);**不再是 `AudioCueDto` 字段**(见下注) |
| TierMap | — | table | — | 每档一行的 `{ 通带中心 Hz / 通带宽 Hz / 噪声底 gain / 频段细节 }`;数值**待调**(用户调,见 §Tuning Knobs) |

**Output Range:** 每档一组 { 通带范围, 噪声底 dB, 频段细节 }。**约束:三档的附加音层始终非零**
(AC-44-01);差异必须**可辨**且**非静音型**(AC-44-02)。
**Example:** 低档 = 窄通带 + 高噪声底 + 少频段细节(摩擦声 / 衣料声混进来);高档 = 宽通带 + 低噪声底 + 全细节。

> **⚠️ 2026-09-18 修订(列集自相矛盾 / 悬空符号 —— 三处旧口径不一致)**:
> 原文有**三个名字指同一样东西、两个引用指向不存在的列**:
> ① F-44.1 的列集 = { 通带中心 / 通带宽 / 噪声底 gain / 频段细节数 };
> ② F-44.2 说 `SIGNAL_GAIN`「由 TierMap 驱动」—— **第四列幻影**;
> ③ F-44.5 用 `TierMap[Tier].breathLayer` —— **该列不存在**。
> **定型列集(唯一出处)**:`TierMap[T] = { passband_center_hz, passband_width_hz, noise_floor_db,
> contact_noise_floor_db, band_detail_count }`。
> `SIGNAL_GAIN`(F-44.2)= **派生量**(附加音层增益,由 `band_detail_count` 与素材共同决定),
> **不是 TierMap 的列**。`breathLayer` **删除** —— F-44.5 改写为 `Loop(cue, TierMap[T])`。
> **「频段细节数」给出的是计数 n,须同时给出 n 个频段的中心频率与内容**
> (细湿啰音 > 1 kHz,基础气流 < 500 Hz —— **须钉住这个数量级**,否则「细密爆裂音」无法落地)。
> **量纲钉死**:`passband_*` 单位 **Hz**;`noise_floor_db` / `contact_noise_floor_db` 单位 **dB**(见 F-44.2)。
> **`Tier` 从 DTO 移除的理由**:一个 `AudioMixer` 组是**全局共享**的,逐 cue 带 `Tier`
> 会让同 cell 内不同档的声源**互相踩滤波**;档位本质是**设备级**状态。若确需逐源滤波,
> 用**逐源** `AudioLowPassFilter` / `AudioHighPassFilter` 组件,不用共享组。
> **所有滤波 / 噪声底变化须 ramp ≥ 50 ms**(AC-44-04 现仅写「crossfade」,此处补齐为**硬下界**),
> 否则切换产生爆音 / 相位抵消。

> **设计来源**:`diagnosis-system.md:1588-1591` ——「低技能 ≠ 把啰音静音;低技能 = 更高的接触噪声底、
> 更窄的通带、更少的频段细节。这正是真实的实习医生体验。」

### F-44.2 接触噪声底 / 信噪比(暴露参数)

`SNR_dB = SIGNAL_dB − 10·log10( 10^(NOISE_FLOOR_dB/10) + 10^(CONTACT_NOISE_dB/10) )` —— 以 `Stethoscope` 总线增益实现

> **⚠️ 2026-09-18 修订(量纲破裂 —— 原公式 `SIGNAL_GAIN / (NOISE_FLOOR + CONTACT_NOISE_floor)` 已作废)**:
> 原式三个变量都声明为 **dB**,而 **dB 是对数量 —— dB 除以 dB 求 SNR 无意义**,
> 两个噪声源在 dB 域也**不能相加**。具体退化:NOISE=+30、CONTACT=−30 ⇒ **除零**;
> 两者同为负时比值是「看起来像正数的巧合」(如 −6/−85=0.071);
> 且原文的例子「低档 SNR ≈ 1:1(= 0 dB)」**根本取不到**。
> 另:**dB 越接近 0 越响** ⇒ 原文「噪声永不为零」用 dB 写是**反的**(静音 = −∞ dB)。
> **修正式**:把两个噪声源**先线性化再相加**,信号与合并噪声**相减**得 SNR(dB);
> 区间钳位 **−24 … +24 dB**。`SIGNAL_dB` 由 `TierMap.band_detail_count` 与素材共同决定;
> `NOISE_FLOOR_dB` / `CONTACT_NOISE_dB` = `TierMap.noise_floor_db` / `TierMap.contact_noise_floor_db`。
> **「噪声永不为零」的 dB 侧正确表述**:`noise_floor_db` 有一个**有限下界**
> (即 `READ_FLOOR_MIN > 0` 映射为「噪声线性幅度 > 0」),**不是**「dB 值 ≠ 0」(dB=0 是最响)。

**Variables:**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| 信号 | SIGNAL_dB | float(dB) | 待调 | 附加音层电平;派生自 `TierMap.band_detail_count` + 素材(见 F-44.1 注) |
| 噪声底 | NOISE_FLOOR_dB | float(dB) | 待调(有限下界) | 背景噪声 |
| 接触噪声底 | CONTACT_NOISE_dB | float(dB) | 待调 | 听诊器摩擦 / 衣料声;随 Tier 变化(= `TierMap.contact_noise_floor_db`) |

**Output Range:** `SNR_dB` 随精度档上升(高档更高),钳位 **−24 … +24 dB**。
**硬约束:`READ_FLOOR_MIN > 0` 在音频侧有对应实现**(AC-44-06)。
**Example:** 低档 `SNR_dB ≈ −6 dB`(噪声几乎淹没信号);高档 `SNR_dB ≈ +12 dB`(信号清晰可辨)。

### F-44.3 语声变体选择(变体库混合)

`variant = SelectVariant(CueId, Tier, Intensity)` —— 查表选变体,交叉淡化

**Variables:**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| cue | CueId | enum | 上游解析结果 | **上游(8 / 13)把病种语义解析成 cue id 后发出**(如 `Voice_Cough_Damp`);44 只收结果,**不读 `disease_id`**(规则八) |
| 精度档 | Tier | byte | 0–2 | 设备级(见 F-44.1) |
| 强度 | Intensity | byte | 0–255 | 来自 `AudioCueDto.Intensity`;查表前先经 §Intensity 映射归一 |
| 变体库 | VariantLib | table | 男/女 × 强/中/弱 × 数种句尾 | 素材侧;每组录多个变体 |

**Output Range:** 一个具体变体 clip + 交叉淡化参数。**硬约束**:禁五项独立调制
(音高 / 气声 / 语速 / 断句 / 共鸣)—— 那是「变声器」不是「病人说话」(AC-44-03 / 04)。

> **⚠️ 2026-09-18 修订(幽灵输入)** —— 原式为 `SelectVariant(PatientSemantics, Tier, Intensity)`,
> 其中 **`PatientSemantics` 不在 DTO 里**,且 44 **被禁读 `disease_id`**(规则八)——
> 这是一个**44 输入面上不存在的自变量**。**改为 `CueId`**:病种语义的解析**本来就在上游**,
> 上游把「哪种咳」编进 cue id 后再发;44 只按 cue id 查变体表。

### F-44.4 空间化锚点(整数格 → float)

`worldPos = LatticeToWorld(Cell)` —— 表现层换算,禁反推

**Variables:**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| 整数格 | Cell | WorldPos | 格坐标 | ADR-015 §三 的单一整数格 |
| 表现态位置 | worldPos | float3 | 世界坐标 | 视觉层 float(ADR-015 两层世界) |

**Output Range:** float 世界坐标,用于 AudioSource 3D 空间化。
**硬约束:** **禁从 float 反推逻辑格**(ADR-015 / ADR-018 §二)。
联机下远端实体(玩家 / 病人 / 受伤敌人)的 `worldPos` 来自 **ADR-001 §一之二 `IPositionalChannel`**
(per-`ActorId` latest-value 表)的表现态位置,44 不自建通道、只读取锚点。

### F-44.5 呼吸层档位 ≠ 音量(连续变化语义)

`breathLoop = Loop(cue, TierMap[Tier])` —— 循环层,档位改滤波不改音量

**Variables:**

| Variable | Symbol | Type | Range | Description |
|----------|--------|------|-------|-------------|
| 呼吸层 cue | cue | CueId | 平稳 / 急促 / 浅慢 / 停顿 | 9 的连续变化语义(状态量),经 13 表现映射触发 |
| 档位 | Tier | byte | 0–2 | 设备级(见 F-44.1) |

**Output Range:** 循环呼吸层的**连续**变化;**无突变音效**(AC-44-10)。
**硬约束**:转危 = 玩家自己听出来,不得用「突变音效」标记阈值跨越
(`disease-simulation.md:1120-1121`);「停顿 vs 停止」必须可听辨,但**不许用音效硬报**
(`disease-simulation.md:1118`)。

> **⚠️ 2026-09-18 修订**:原式 `Loop(cue, TierMap[Tier].breathLayer)` 引用了**不存在的列**
> (见 F-44.1 注)。改为 `TierMap[Tier]`(整行参数参与滤波)。

### F-44.6 Intensity(byte 0–255)→ 声学量(2026-09-18 新增)

`Intensity` 在旧稿中**无任何声学映射**:数据契约声明它存在、F-44.3 拿它当选择器,
但**无公式把 0–255 映到增益 / 爆裂音密度 / 交叉淡化参数** —— `Intensity=10` 与 `250`
**听起来一模一样**。本式补齐。

```
u = Intensity / 255            // 归一 [0,1]
gain_scale   = GAIN_MIN  + u × (GAIN_MAX − GAIN_MIN)
density_mult = DENS_MIN  + u × (DENS_MAX − DENS_MIN)   // 附加音层出现密度倍率
bucket       = (u < BUCKET_LOW) ? 弱 : (u < BUCKET_HIGH) ? 中 : 强
```

| Variable | Type | Range | Description |
|----------|------|-------|-------------|
| `GAIN_MIN` / `GAIN_MAX` | float(dB) | 待调 | 强度映射到的增益下限 / 上限 |
| `DENS_MIN` / `DENS_MAX` | float | 待调(≥0) | 附加音层密度倍率下限 / 上限 |
| `BUCKET_LOW` / `BUCKET_HIGH` | float | 待调,`0 < LOW < HIGH < 1` | 256 → { 强 / 中 / 弱 } 的分桶阈值 |

> **上游归一未还清**:`Intensity` 的来源归一化 `Project(Sign_j) → [0,1]`(`D-8-4`)**8 侧未定**
> —— 本式**只定义 44 侧映射**,上游给什么归一值由 8 决定(见 §依赖未还清)。
> **值域冻结**:全部数值 = **用户自己调**(`feedback-user-owns-balance-values`);本式交付**形状**与**变量表**。

### F-44.7 世界语境呼吸路径(隔门听呼吸 —— 幻想的主通道,2026-09-18 新增)

**这是 §Player Fantasy「门后的人还在喘气」的载体** —— 旧稿中它**没有总线、没有滤波、
没有衰减曲线、没有多病人规则**(见规则四 / 听觉优先级表的历史缺陷)。

`worldBreath = Spatial(cue, Cell, occlusion)` —— 世界空间 3D 呼吸声,独立于听诊层

| Variable | Type | Range | Description |
|----------|------|-------|-------------|
| `Cell` | WorldPos | 整数格 | F-44.4 换算为 float;病人 / 受伤实体的**已发布锚点** |
| `min_distance` / `max_distance` | float | 待调 | 3D 衰减的起止距离;**max 内须仍可辨** |
| `occlusion_lowpass` | float(Hz) | 待调 | 门 / 墙遮挡的低通截止;**穿门 ≠ 静音,是变闷** |
| `bus` | — | — | 走 **Ambience 组的世界语境子通道**或独立 send(**不得**复用 `Stethoscope` 总线 —— 后者是听诊器贴耳声) |

**多病人规则**:同一 `Cell` 内 N 个病人 → **按距离分别空间化混音**,不合并成一条。
**与听诊层的关系**:世界语境呼吸(未贴听诊器)低通 + 衰减;贴耳后切 `StethoscopeFocus`
快照 → 走上层 `Stethoscope` 总线(通带 / 噪声底由 TierMap 驱动)。**两层是不同的声音形象**,
不是同一条声的远近。

> **值域冻结**:全部数值 = **用户自己调**(见 §Tuning Knobs 新增行)。

## Event Table Schema(2026-09-18 新增 —— `OQ-44-4` 升格)

> **为何从此 OQ 升格为本节**:AC-44-09 是 **BLOCKING 构建门**,而它的载体(schema)原被推迟到
> `OQ-44-4`「实现时定」—— **BLOCKING 门骑在一个 Open Question 上**,是本项目**已命名的失败模式**
> (见 ADR-022「引用却无登记」)。门必须先有载体。**本节只定 schema 的形状,不定数值**。

音频事件表(`assets/data/audio_events.json` → ADR-014 两阶段烘焙 → `*.cooked`):

```json
{
  "schema_version": 1,
  "cue": "BreathLayer_Urgent",
  "assets": ["sfx_breath_base_loop_urgent.wav", "sfx_breath_adventitious_fine.wav"],
  "tier_params": { "0": {...}, "1": {...}, "2": {...} },
  "loop": true,
  "bus": "Stethoscope",
  "whitelist_category": "ContinuousPhysiology",
  "trigger_source": "ContinuousPhysiology"
}
```

### `whitelist_category`(内容侧 —— 五类,承规则二白名单)

`PlayerAction` · `PlayerExamAction` · `WorldPerceptible` · `ContinuousPhysiology` · `MusicLayer`

### `trigger_source`(时机侧 —— 闭枚举,堵「阈值时刻的咳嗽」)

| 合法 | 禁止(状态播报) |
|------|----------------|
| `PlayerAction`(玩家动作) · `PlayerExamAction`(玩家检查) · `WorldPerceptible`(可感知世界声) · `ContinuousPhysiology`(持续生理声) · `EncounterMusicLayer`(遭遇乐层,G1–G3 约束) | `VitalsCrossed`(体征阈值跨越) · `ThresholdCrossed`(通用阈值跨越) · `OutcomeResolved`(结果已定) |

> **`trigger_source` 是本次修订最关键的一个字段** —— 它把 AC 从「查内容」升为「**查时机**」。
> 没有它,一条在危险阈值那一 tick 发出的咳嗽**在内容侧完全合法**、断言全绿,
> 但它**就是**那条被禁止的警报。字段存在 = 上游在**声明**它为什么发这条 cue;
> 声明为 `VitalsCrossed` = 构建失败。

### 校验规则(EditMode + CI 门)

1. `whitelist_category` 必填且 ∈ 五类;
2. `trigger_source` 必填且 ∈ 合法集(禁止集任一 = 失败);
3. **NOT-RUN 守卫**:`schema_version` 缺失或 cue 行缺上述任一字段 ⇒ 断言**报错退出**
   (不得静默通过 —— 「没检查」与「检查通过」必须可区分);
4. 双重负向夹具(见 AC-44-09)。



> 每条 = 明确条件 + 明确结局。**格式**:`If [condition]: [exact outcome]. [rationale]`。

### 听诊与呼吸层

- **If 玩家低精度档(0)听诊而病人有细湿啰音**:附加音层**非零但更吵**(窄通带 + 高噪声底),啰音**淹没在噪声里但存在**。
  **绝不静音**(AC-44-01)—— 静音 = 玩家以为漏了素材 / 以为病人没病。
- **If 玩家低精度档听诊而病人**无**啰音**:信号 = 基础气流层 + 噪声,无附加音;**
  **不**因为听不出啰音就出现「空洞」—— 那是正常现象(规则四 / F-44.1)。
- **If 档位变化导致通带突变**:交叉淡化 / 平滑过渡,**ramp ≥ 50 ms**(F-44.1 注),禁止「啪」一声切换(爆音 / 相位抵消,AC-44-04)。
- **If 天气层的进入与退出**(2026-09-19,承 5 首轮评审 B-11):同受上条约束 —— 增益变化必须走**对称淡化**,**禁硬切**;淡化的时长参数归 44(**5 不持有**)。⚠️ 淡化 ≠ 提示音:淡化是**连续声床的增益斜坡**,不携带「天气变了」的**离散事件语义**,故不触 AC-44-09 白名单;若淡化被做成**可听辨的 onset 音形**则违 AC-44-09(与 5 侧 AC-5-10 的去标注盲测联合)。
- **If 呼吸层从「急促」过渡到「停顿」**(转危):呼吸循环**连续**变慢 / 变浅,**无突变音效**(AC-44-10)。
  **停顿(昏迷)vs 停止(死亡)**:两者必须**可听辨**—— 停顿有极轻微的背景气流尾,停止完全无声;
  **但绝不**用「叮」宣告是哪个(9 的 AC-21 / ADR-018 §六)。
- **If 呼吸 cue 的循环句柄被上游重复 BeginLoop**:44 检测到同源重复循环,**以最后一次为准**,释放旧句柄;
  不叠加两条呼吸层(防「两层呼吸」bug)。
- **If 上游 EndLoop 传了过期句柄**:44 忽略;循环由唯一持有者收尾,过期句柄不产生播放错误。
- **If 上游 EndLoop 因丢包 / 断线永不到达**(2026-09-18 增补):循环 cue **自评兜底** ——
  从最新快照 `Progress` 自行求值是否该结束;或重连时拉流重放重建句柄(须 45 侧登记)。
  **不得**出现「呼吸层永不停止」(ADR-001 `ReorderBuffer` 超界丢弃已知可致此)。
- **If 附加音层播放中 13 发来一条咳嗽 cue**(2026-09-18 增补,医学准确性):
  咳嗽 cue **不得**调用 `EndLoop` 停掉呼吸循环 —— 天真实现会「咳嗽 ⇒ 停呼吸 ⇒ 重启」,
  临床上等价于**「啰音被咳掉了」**,**直接违反 AC-44-08**「咳嗽后不消失」。
  咳嗽走独立一次性一次性通道(Voice 总线),呼吸循环继续。
- **If 基础气流层与附加音层各自独立循环**(2026-09-18 增补,相位漂移):
  两层的时钟会漂移 ⇒ 爆裂音漂进**吸气中段** —— 这正是医学上禁止的错误。
  **纪律:附加音层相位锁定到基础循环的周期**(`adventitious_policy.trigger_phase ∈ [0.6, 0.95] × 基础周期 ± jitter`),
  **不得**独立自由循环。

### 语声变体

- **If 变体库缺该 (病种语义 × 精度档) 组合**:回退到最近可用档的变体(高→低),**不静默**;
  编辑期烘焙门拒绝「无任何变体可回退」的 cue。
- **If 强度(Intensity)超出 0–255**:钳位到 0–255(byte 天然钳位);不产生音量突变。
- **If 语声与呼吸层同时播放**:两者走不同总线(Voice vs Stethoscope),**不互压**;
  语声优先仅由 `DialogueFocus` 快照体现,不额外 ducking 呼吸层(规则三)。

### 联机与空间化

- **If 联机下四名玩家技能不同**(2026-09-18 修订,裁定 D-A):**每人按自己技能档**渲染听诊音
  (`SetTier(TierSource.Local)`);脉案字体量亦各自分叉 —— **音与字都分叉,统一的只有事实**。
- **If 远端玩家在音频触发瞬间断线**:44 使用最近一次 `IPositionalChannel` 位置快照;
  断线后 cue 静默过期,不崩溃(表现层降级,非 sim 事件)。
- **If 病人(非玩家)在远端客户端发声**(2026-09-18 增补,**原缺口**):病人 / 受伤实体的
  表现态**锚点发布者已由 ADR-001 §一之二 登记**(9 伤情 / 13 病人 AI 的锚点持有者,**主机侧**);
  44 按 `ActorId` 读表。**无锚点的呻吟 = 无位置的声** —— 这是原稿的**未定义行为**,现由发布者登记封堵。
- **If 上游 cue 在远端客户端如何到达**(2026-09-18 增补,**原缺口 —— 关键**):
  **cue 本身不复制**。各客户端**从本地流副本 + 表现映射自行派生** cue
  (与 ADR-005「客户端持流副本」同构)。**硬前提**:13 的 cue 触发必须是
  **{ 流事件, 版本化烘焙数据 } 的纯函数** —— **禁本地 RNG、禁读表现态位置**,
  否则各客户端会**听到不同的病人**(见 AC-44-D7)。
- **If 同一 `WorldPos Cell` 同时有多个声源**:按**距离归一化** tie-break 混合
  (2026-09-18 修订,裁定 D-B:近者优先,不看类别);不逐帧重排。

### 白名单与边界

- **If 某个 CueId 不在白名单类别中**:AC-44-09 断言失败(BLOCKING,EditMode + CI 门)。
  这是构建期拒收,不是运行期告警。
- **If 上游试图传 `disease_id` 进 `AudioCueDto`**:`PresentationDtoGuard` 递归扫描失败(AC-37-15 一致性)。
- **If 玩家暂停游戏**:切 `Paused` 快照(全总线衰减);暂停期间的循环 cue 挂起,恢复后续播。
- **If 玩家进入听诊动作后长时间无任何 cue 更新**:维持 `StethoscopeFocus` 快照(玩家行为驱动,非状态);
  **不设超时**、不自动退出(任何超时阈值若引入,须登记为调参旋钮,不得写死)。

### 素材与管线

- **If 音频素材未烘焙(开发期直接播原文件)**:开发构建允许,但**玩家构建零 JSON 解析器**
  (ADR-014 继承验证)—— 事件表必须走烘焙。
- **If 素材文件缺失**:编辑期烘焙门拒绝;运行期绝不静默播放「空 cue」(缺失 = 构建失败,不是运行期降级)。
- **If 音量 / 总线增益越界**:暴露参数钳位(音量 0–1,dB -80–0);不做「玩家把音量拉爆」的降级处理(那是玩家的选择)。
- **If Addressables 流式加载造成卡顿**(2026-09-18 增补):素材走流式,首次播放某 cue 可能
  因异步加载而**迟发**。**纪律**:高频 cue(呼吸层 / 脚步)在进入对应生态区 / 场景时**预载**;
  一次性 cue 允许极小迟发,但**不得**在听诊中因加载迟发(听诊窗预载)。**具体预载策略归 ADR-014 §五。**

> **无「the system should feel good」条款** —— 全部边界可机械验证(AC-44-01…13 承接)。
> **⚠️ 2026-09-18 修正**:原文断言「全部边界可机械验证」**与事实矛盾** ——
> AC-44-02 / 08 / 10 是**听测**(不可自动化)。正确表述:**机械可验证者 = 断言类**
> (AC-44-01 / 09 / B1–B4 / C1 / D2–D7);**听测者 = 人工证据**(AC-44-02 / 08 / 10 / 11,
> 须 `production/qa/evidence/` + 签署)。混淆二者 = QA 无法排期。

## Dependencies

> **44 是 Foundation 层,零上游硬依赖**(`systems-index.md:78` 依赖列 = `—`)。
> 但它是**表现层**,消费六个上游系统的**语义 cue**(这些是「表现映射」,不是 sim 依赖)。
> 分类:**硬依赖 = 引擎 / 框架件**;软依赖 = 上游语义提供者(44 缺其一仍能播其余)。

### 硬依赖(44 缺了无法工作)

| 依赖 | 性质 | 出处 |
|------|------|------|
| **ADR-018(音频架构)** | 权威件 —— 边界 / 拓扑 / AC 编号全部由它定 | `docs/architecture/adr-018-audio-architecture.md` |
| **ADR-014(数据管线)** | 音频事件表走烘焙管线(玩家构建零 JSON 解析器) | `adr-014` §五 |
| **ADR-015(世界几何)** | 空间化锚点 = `WorldPos` 整数格;禁反推 | `adr-015` §三 / §七 |
| **ADR-020 §七(相机)** | `AudioListener` 单挂点:平面 = 主相机,VR = 头显 | `adr-020` §七(结清 `adr-018:295` 留白) |
| **ADR-001(联机 pipe 抽象)** | 远端实体(玩家 / 病人 / 受伤敌人)的空间化锚点复用 **`IPositionalChannel`**(per-`ActorId` latest-value 表);44 不自建位置通道 | `adr-001` §一之二(**2026-09-18 修订**:44 侧已登记,+ 消费者 + 锚点发布者) |
| **引擎件**:AudioMixer / AudioSource / AudioListener(3D 空间化 · 快照 · 暴露参数 · 单 Listener 规则) | 长期稳定 API(Unity 6.3) | `docs/engine-reference/unity/modules/audio.md` |

### 软依赖(上游语义提供者 —— 44 消费其 cue)

| 上游 | cue 语义 | 44 的动作 | 双向性(对方已声明 44 侧) |
|------|---------|----------|--------------------------|
| **13 病人 AI**(`adr-016:232-236`) | 咳嗽 / 呻吟 | 变体库选语声 / 一次性 cue | ✅ ADR-016 §六:「13 拥有咳嗽呻吟 → 音频的表现映射,属表现层,不经 sim」 |
| **37 病例系统**(`case-system.md:539-548`) | 结案纸页翻合 / 立案落纸 | 纸面物理声(禁成就音效) | ✅ case-system Visual/Audio 表 |
| **52 随机事件导演**(`random-events.md:977,1058`) | 狗吠 / 马蹄 / 锣声 / 铃声 / 钟声 | 世界声 cue,空间化 | ✅ random-events 契约行「44 音频 \| 44 ← 52」 |
| **8 诊断**(`diagnosis-system.md:1588-1610`) | 听诊层:呼吸两层 + 接触噪声 + 精度档 | `Tier` + `Intensity` 驱动通带 / 噪声底 | ✅ V-8.7 四条硬需求 |
| **9 疾病模拟**(`disease-simulation.md:1111-1121`) | 呼吸连续变化语义 | 循环呼吸层 | ✅ §三 音频需求表 |
| **42 拟物 UI** | 笔尖 / 翻纸 / 器物 | UICue 总线 | ✅ **42 已成稿**(`skeuomorphic-ui.md`,2026-09-16);44 侧登记为「42 的呈现事件 → UICue」,42 侧回执 = `AC-42-F2` 的 cue 白名单 |
| **45 网络层**(`adr-001`) | 远端实体表现态位置(第二 QoS **表**) | 空间化锚点(不自建通道、不新增 QoS、不做语音) | ✅ **ADR-001 §一之二 已显式声明 44 侧**(2026-09-18 裁定 D-D:消费者列表 + 病人 / 受伤实体锚点发布者 + cue 非复制) |
| **48 教学与引导**(`tutorial-and-onboarding.md`) | **教学口述 / 示范者语声 cue** + **字幕供语义**(承 `AC-44-15`,与病人语声同类同规) | 口述 cue(空间化 / 非空间化依场景)+ 语声字幕供语义 | ✅ **2026-09-19(承 48 首轮评审)**:48 已登记 44 上游处(口述 cue + `AC-44-15` 字幕管线);44 侧补本行 |
| **7b 存档位 UI**(`save-slot-ui.md`) | **翻页 / 合册 / 落笔 三个事件名**(`AC-7b-09` 的闭集,承 `save-slot-ui.md:309-310` 的行为反馈白名单) | 音频事件表登记 7b 事件名枚举(`assets/data/audio_events.json`,归 44 侧) | ✅ **2026-09-20 回刷(承 `/review-all-gdds` R-2⑤)**:7b 侧 `save-slot-ui.md:414` 已把「44 的音频事件表」写为 `AC-7b-09` 的载体;44 侧补本行收口 |

### 下游(谁依赖 44)

| 下游 | 依赖内容 | 出处 |
|------|---------|------|
| 8 / 9 / 13 / 37 / 52 的**音频侧 AC** | 凡依赖「音频事件表 + 混音拓扑」的 AC | ADR-018 §Blocks |
| `TR-concept-*` 中依赖「状态反馈」的条目 | 44 承担状态反馈职责 | `systems-index.md:254` |

### 硬约束(跨系统)

1. **44 不订阅 sim 真值、不写三流、不进 sim 程序集**(ADR-018 §一 / ADR-005 门 A)。
2. **`disease_id` 不进音频层**(`PresentationDtoGuard` 递归扫描覆盖 `AudioCueDto`,AC-37-15 一致性)。
3. **无提示音铁律** = 白名单断言(AC-44-09 BLOCKING)—— 三处铁律
   (`disease-simulation.md:1117,1120-1121` · `diagnosis-system.md:1597` · `case-system.md:548`)统一于此。
4. **快照不得用于状态播报**(ADR-018 §三)—— 判据 = 无**非拟物**触发源(规则三)。
5. **听诊精度 = 本机玩家自己的技能档**(2026-09-18 裁定 D-A;`SetTier(TierSource.Local)`);脉案亦各自分叉。
6. **44 不拥有素材语义选择权**(上游解析 → 44 只收 cue id)。
7. **素材本体 = `assets/audio/`**(Addressables 流式),与 `data-core` 数据烘焙组区分(ADR-018 §五 补注)。
8. **VR 音频推 P1b**;P0 不做 VR 音频。
   > **⚠️ 层级订正(2026-09-15,`/consistency-check` 发现)** —— 本条原写 **P1a**,
   > 沿用了 ADR-018 §七 的口径。但 VR **功能**的档位权威是 `game-concept.md:723`
   > 范围阶梯(「P1b 完整成品 = + 1-4 人联机、**VR 急救动作**、…」)与
   > `systems-index.md:178`(P1b = 联机与 VR)。ADR-013 §二 的「VR / world-space 实现推 P1a」
   > 指的是 **world-space UI 栈**,不是 VR 功能的档位 —— ADR-018 §七 / ADR-020 §三 沿用时误读。
   > **VR 音频不可能先于 VR 模式存在** ⇒ 订正为 **P1b**。**ADR-018 §七 须同批订正。**

> **环检查**:44 无任何出向引用 sim 的接口(只入不出,`IAudioCueSink` 单向);不构成环。
> **单向化**:44 → 各上游都是**消费方**,无反向写入 —— 干净单向。

## Tuning Knobs

> **44 的旋钮全部「待调」(数值用户自己调 —— `feedback-user-owns-balance-values`)。**
> 本 GDD 交付**变量表 + 调参旋钮**,不拍定值。每个旋钮:安全范围 / 调太高 / 调太低。

### 听诊层族(F-44.1 / F-44.2)

| 旋钮 | 含义 | 安全范围 | 调太高 | 调太低 |
|------|------|---------|--------|--------|
| `TierMap[0..2]` 的通带宽度 | 低/中/高三档的滤波带宽 | 各档差异**可辨**(AC-44-02) | 高档也像低档(白噪音感) | 三档听不出差别(玩家困惑) |
| `TierMap[0..2]` 的噪声底 | 接触噪声底 / 信噪比 | 噪声永不为零(AC-44-06) | 信号淹没在噪声里(玩不了) | 噪声近零(丢失「实习医生」体验) |
| `CONTACT_NOISE_floor` | 听诊器摩擦 / 衣料声 | 随 Tier 变化 | 变「爽声」/ 摩擦噪声失控 | 消失(丢失低档体验) |
| `passband_center_hz` / `passband_width_hz` | 通带中心 / 带宽(**Hz**) | 高频细节须覆盖 >1 kHz(爆裂音) | 通带盖过气流层(浑浊) | 把爆裂音切掉(听不到啰音) |
| `band_detail_count` + **各频段中心** | 频段细节数与内容 | 须给出每个频段的中心频率 | 素材量爆炸 | 细节不足(高档也听不出)。**量纲:** 基础气流 <500 Hz,爆裂音 >1 kHz |

### 语声变体族(F-44.3)

| 旋钮 | 含义 | 安全范围 | 调太高 | 调太低 |
|------|------|---------|--------|--------|
| 变体库粒度(男/女 × 强/中/弱 × 句尾数) | 变体数量 | 每组合 ≥2 个变体 | 素材量爆炸(重录灾难) | 变体太少(重复感 / 缺组合) |
| 交叉淡化时长 | 变体切换平滑度 | 无爆音 / 相位抵消(AC-44-04) | 切换拖沓(反应迟钝) | 爆音 / 相位抵消 |

### 快照族(ADR-018 §三)

| 旋钮 | 含义 | 安全范围 | 调太高 | 调太低 |
|------|------|---------|--------|--------|
| `StethoscopeFocus` 的 Ambience/Music 压低量 | 听诊聚焦强度 | 玩家主动动作驱动 | 听诊时世界消失(出戏) | 听诊无聚焦感 |
| 快照过渡时长 | 切换平滑度 | 行为驱动 | 切换拖沓 | 突变(爆音) |

### 世界语境呼吸族(F-44.7 —— 幻想主通道,2026-09-18 新增)

| 旋钮 | 含义 | 安全范围 | 调太高 | 调太低 |
|------|------|---------|--------|--------|
| `world_breath.min/max_distance` | 3D 衰减起止距离 | max 内仍可辨 | 整张地图都听得见 | 隔一道门就消失(幻想落空) |
| `occlusion_lowpass_hz` | 门 / 墙遮挡低通截止 | 穿门 ≠ 静音(**必须听得见,只是变闷**) | 穿墙如无物 | 穿门即静音(等于隔墙听不到) |
| 世界语境呼吸总线增益 | 相对 Ambience 的电平 | 距近时压过环境 | 淹没一切 | 被环境盖掉(主通道埋没) |

### 全局

| 旋钮 | 含义 | 安全范围 | 调太高 | 调太低 |
|------|------|---------|--------|--------|
| `GAIN_MIN/MAX` · `DENS_MIN/MAX` · `BUCKET_LOW/HIGH` | Intensity → 声学量映射(F-44.6) | `0<LOW<HIGH<1`,`DENS≥0` | 弱强度也夸张 | 强弱无差别(Intensity 失效) |
| 总线音量(Music / Ambience / Voice / SFX / Stethoscope / UICue) | 玩家可调(无障碍) | 0–1 / dB -80–0 | 玩家自担 | 玩家自担 |
| mono 选项 | 听力辅助(AC-44-13) | 开 / 关 | — | — |
| 滤波 / 噪声底 ramp 时长 | 档位切换平滑度 | **≥ 50 ms**(F-44.1 注) | 切换拖沓 | 爆音 / 相位抵消 |

> **跨旋钮约束**:`TierMap` 的通带与噪声底**必须同向变化**(高档 = 宽通带 + 低噪声底);
> 反向即破坏「档位 = 解析带宽」的语义(AC-44-01/02 会失败)。
> **归属**:全部 44 拥有(音频事件表 `assets/data/audio_events.json`,走 ADR-014 烘焙)。
> **与 8 的关系**:8 的 `READ_FLOOR_MIN > 0` 是**8 的旋钮**,44 的 `NOISE_FLOOR` 是它的**音频侧实现**,
> 两者各自调,但方向必须一致(都「永不为零」)—— 已在 AC-44-06 锁定。

## Visual/Audio Requirements

> **44 的 Visual 侧极小**:它**不新增任何视觉语言**(与 8 / 9 / 37 的负面声明体例一致)。
> 唯一视觉义务 = **无障碍的视觉替代**(规则九)。以下正面定义音频本体。

### 声学方向(承 42 拟物语汇,《双材》)

**44 的声音必须与《双材》视觉同源**:水墨写意的**留白 / 呼吸感** × 黄铜工业的**机械质感 / 精密反馈**。

| 层 | 声学方向 | 素材命名(承 `diagnosis-system.md:1627`) |
|----|---------|----------------------------------------|
| **Music** | 京戏 / 民国氛围;**留白优先,不铺满** | `music_*` |
| **Ambience** | 风 / 鸟 / 村落底噪;4 生态区各一套(ADR-015) | `amb_*` |
| **Voice** | 病人语声**变体库**(男/女 × 强/中/弱 × 句尾);**非参数调制** | `vo_patient_*` |
| **SFX** | 纸面 / 器物 / 脚步;拟物质感(黄铜件、纸页) | `sfx_*` |
| **Stethoscope** | 呼吸两层 + 接触噪声:**核心声道** | `sfx_breath_base_loop_small.wav` / `sfx_breath_adventitious_*.wav` / `sfx_steth_contact_*.wav` |
| **UICue** | 笔尖 / 翻纸(**仅行为反馈**,见规则二) | `ui_*` |

> **⚠️ 2026-09-18 补齐缺失素材族(首轮评审)** —— 旧表相对**已设计的系统**有缺口:
> ① **把脉音效** —— `game-concept.md:571` **点名**要求,ADR-018 亦引用它,**旧表漏了**;
> ② **脚步 × 地形 / 生态区矩阵**(4 生态区 × 材质)—— 旧表只有一句「脚步」;
> ③ **建造 / 医疗器具 / 煎药**(23 建造 · 24 医馆);
> ④ **敌人 AI**(27)脚步 / 受击 / 低吼(25 回填只加了乐层);
> ⑤ **可驯化动物**(ADR-016 §七);
> ⑥ **玩家自己的呼吸 / 脚步** —— `diagnosis-system.md:1603`「主角与病人共用同一套声源系统」**从未落到 44**;
> ⑦ **天气强度层**(+ 室内 / 室外混响);⑧ **七条总线无 Aux / Reverb send 拓扑** ⇒
> 快照只能改增益、**改不了混响**。

| 层(补充族) | 声学方向 | 素材命名 |
|----|---------|---------|
| **Stethoscope** | **把脉**(腕部脉动)—— 与听诊同层 | `sfx_pulse_*.wav` |
| **SFX** | 脚步 × 地形矩阵(4 生态区 × 材质) | `sfx_footstep_{grass,dirt,wood,stone,...}_*` |
| **SFX** | 炮制:起炉 / 起货 / 煎药行为声(**18 炮制**;⚠️ 2026-09-19 发射方改判:原挂 23·24 名下,经 18 首轮评审 BL-10 裁定 = **18** —— 起炉/起货时点由 18 调度产生,23 建造 / 24 机器不持有点火事件)。**命名对齐 18 规约**:`sfx_process_light` / `sfx_process_complete` / `sfx_process_work`;`sfx_decoct_*` 保留为素材别名或并入,以本表为准 | `sfx_process_*`(← `sfx_decoct_*`) · `sfx_build_*` / `sfx_tool_*` |
| **SFX** | 敌人(27)脚步 / 受击 / 低吼 | `sfx_enemy_*` |
| **SFX** | 可驯化动物(ADR-016 §七) | `sfx_animal_*` |
| **Stethoscope / Ambience** | **玩家自己**的呼吸 / 脚步(与病人同源) | `sfx_breath_player_*` |
| **Ambience** | **天气轴**强度层(雨 / 风分级)—— 输入 = 5 的 `Weather.kind/intensity`(cell = 玩家当前格) | `amb_weather_{rain,wind}_l{n}` |
| **Ambience** | **昼夜 / 季节生态轴**(鸟鸣、虫声等)—— ⚠️ **与天气轴分开挂层**(2026-09-19,承 5 首轮评审 B-10):输入 = 5 的 `isNight` / `season_index`,**不随天气起落** —— 若挂进 `amb_weather_*`,会造成「有鸟鸣 = 必晴天」的错误耦合 | `amb_biome_{birds_day,insects_night}_{l{n}}`(命名待 44 自裁) |
| **SFX** | **雪地脚步声 = 脚步×材质矩阵的一个材质档**(承上文 `sfx_footstep_*`),**不是天气层的素材触发** —— 5 只出 `kind`,由 44 侧把 `kind=雪` 映射为**脚步材质切换**(1/44 矩阵),禁开第二通道 | 复用 `sfx_footstep_snow_*` |
| **Aux / Reverb** | 室内 / 室外混响 send 拓扑(七总线之外) | `reverb_preset_{indoor,outdoor,cave}` |

### 听觉信息优先级(ADR-018 §六 白名单的播放序)

多声源竞争时的次序(承 sound-bible 模板的 Audio Feedback Priority)。

> **⚠️ 2026-09-18 修订(重心倒置 + 两个冲突的表)** —— 旧稿把**幻想的主通道(持续生理声 / 呼吸)
> 排在 4/5**,低于笔尖刮纸;无**距离感知**(隔门呼吸凭什么压过近旁的环境音?);
> 且边界案例凭空造了**第三个优先级**「关键 cue > 行为反馈 > 环境」,其中的「关键 cue」
> **未定义且不在白名单五类之内** —— 一个未定义的最高档,正是「叮」从后门溜回来的通道。
> **修订:单一出处 + 按距离归一化 tie-break**(用户裁定 D-B)。**删除「关键 cue」措辞。**

**多声源混合规则(唯一出处)**:

- **同一 `Cell` 内**多个声源 → 按**距离** tie-break(**更近者优先**),不看类别。
- **跨类别**时:**玩家自身动作 / 主动检查声**高于**世界声与生理声**(它们是玩家自己发出的、无空间意义的直接反馈)。
- **世界线索 vs 持续生理声**:**没有绝对档位** —— 两**者都是信息通道**(规则二),
  谁更近谁赢。隔一道门的呼吸**必须能压过**远处的狗吠,反之亦然。
- **环境 / 氛围**永远最低(它是底噪,不是信息)。

> **理据**:狗吠(52 的预告线索)与呼吸(9 的连续变化)**同为支柱一的信息通道**;
> 用**类别**给它们排序 = 体系性地让 52 压过 9;用**空间**排序才是玩家真实听到的世界。
> 「近的听得清、远的听不清」是物理事实,不需要设计判断 —— 也正因此它**不构成状态播报**。

> **⚠️ 本项目特有条目**:上表**没有**「危险警告」类(那是黑名单)。
> 玩家必须自己从世界线索与生理声**推断**危险,而不是从一条「警告音」被告知。

### 硬约束(负面声明)

- **禁**:sting / jingle 宣告状态(「病人转危」「误诊」「结案」「成就达成」);
- **禁**:ducking **报**状态(可以玩家驱动地聚焦);
- **禁**:素材切换到**报**状态(「答对了 ⇒ 播正确音」);
- **禁**:连续水声冒充细湿啰音(AC-44-08,医学受众第一个出戏点);
- **禁**:突变音效标记阈值跨越(AC-44-10)。

### 无障碍(AC-44-12 / 13;2026-09-18 修订,裁定 D-C)

- **视觉替代 = 引用 `AC-13-F1`**(咳嗽 / 呻吟 / 呼吸三类 cue 的默认可见对应物:姿态 / 面色 / 呼吸幅度;
  **由 13 拥有,44 不新造**)。承 `templates/sound-bible.md:126`。
- **显式 P0 非目标**:「**隔墙听觉检测在 P0 无视觉等价物**」—— 幻想本身即「看不到还能听见」,
  视觉替代**需要视线、不能穿墙**,故那条跨房间通道**无法**被重建。
  **代价诚实登记**,不用「双通道冗余」糊过去。
- **替代物不得只用颜色**:「面色 / 口唇发绀」是**纯颜色通道**(WCAG 1.4.1),
  色觉盲玩家读不出「发绀 vs 苍白」—— 须**形状 / 明度 / 姿态**承载(归 13 侧)。
- **44 自己的非视觉义务**:mono 选项 · 各总线独立音量 · **病人语声字幕**(WCAG 1.2.2)。
- **证据类型 = ADVISORY**(不可自动化 —— 承 `patient-ai.md:1152`)。

> 📌 **Asset Spec** — Visual/Audio requirements 已定义。art bible 批准后,
> 运行 `/asset-spec system:audio-system` 生成逐素材的视觉描述与生成提示。

## UI Requirements

> **44 零 UI**(与 42 的栈无关)。它不渲染任何界面元素。

- **44 无任何玩家可见的 UI**:无音量面板(那是 42 / 42 的下游设置界面)、无音频状态指示、
  无「正在播放」提示。
- **唯一交互面** = 设置菜单里的**总线音量滑块 / mono 开关** —— 那属 **42 拟物 UI 框架**
  (ADR-013),不属 44;44 只暴露**暴露参数**(AudioMixer exposed params)。
- **禁**:任何「音频可视化」「音量电平表」「正在播放」的 UI —— 那是把 44 的内部状态画出来,
  与「只触发 / 只渲染,永不持有游戏状态」冲突。

> **📌 UX Flag 不适用** —— 44 无独立屏幕 / HUD 元素;音量与 mono 控件归 42 的设置界面。

## Acceptance Criteria

> **32 条 / 九组**(2026-09-18:原 27 条 → 新增 **AC-44-D7 / D8 / D9 / 14 / 15** 共 5 条,并全组补
> **证据类型**)。承 **ADR-018 §Validation** 的 AC-44-01…13 落在**组二 / 三 / 四 / 五 / 九**
> (逐条落原编号);**组一 / 六 / 七 / 八为本 GDD 新增**(呈现层边界 / 联机 / 空间化 / 管线)。
> 「(BLOCKING)」= 构建门 / 铁律级。
>
> **证据类型列(2026-09-18 新增 —— 首轮评审:旧稿混淆机械断言与听测,QA 无法排期)**:
> `[A]` = 机械断言(EditMode / CI,**可自动化**);`[L]` = 听测 / 人工证据
> (**不可自动化**,须 `production/qa/evidence/` + 签署,承 `patient-ai.md:1152`)。
> **硬标 BLOCKING 于 `[L]` 项 = 谎报可测试性**,必被绕过。

### 组一 · 呈现层边界(ADR-018 §一 —— 与 42 同构)

- **AC-44-B1** `[A]`(BLOCKING):**GIVEN** 44 的实现,**WHEN** 检查其程序集的**引用集**,
  **THEN** 引用集 ∈ **白名单(恰 = BCL + 契约程序集)** 且**不含 sim 程序集**、
  **无任何 `TypeRef` / `MethodRef` 解析到 sim 类型**(含 `IEventSink`)。
  > **2026-09-18 升级(首轮评审:`grep` 是退步)** —— 原判据「grep 断言 `IEventSink.Append` 调用点」
  > **漏掉**:显式接口实现 · `nameof` · 反射 · 重命名。**系统 6 / 42 已把同类断言升级为
  > 正向白名单 / 反射 / IL 扫描** —— 44 必须同格。判据 = **asmdef 引用白名单 + IL / metadata 扫描**
  > (precedent:**ADR-017 门 A**)。契约程序集仅 BCL(见 §Interactions 注②)。
- **AC-44-B2** `[A]`:**GIVEN** `AudioCueDto`,**WHEN** `PresentationDtoGuard` **递归**扫描它,
  **THEN** **无 `disease_id` 字段**(AC-37-15 一致性)。
- **AC-44-B3** `[A]`:**GIVEN** 44 的公开接口,**WHEN** 检查,**THEN** 只有 `IAudioCueSink`(只入不出),
  44 **无反向查询上游的接口**;且**唯一数据入口 = `AudioCueDto`**
  (**不含 `VitalsDto`**;2026-09-18 删除了该旁路,见规则一)。
- **AC-44-B4** `[A]`:**GIVEN** 44 的运行,**WHEN** 追踪,**THEN** 它**不持有任何游戏状态**
  (病人病情 / 病例状态 / 世界状态),只持有「声音正在发生什么」。

### 组二 · 无提示音铁律(ADR-018 §六 —— 支柱一的听觉保障)

- **AC-44-09(BLOCKING)** `[A]`:**GIVEN** 音频事件表的每一行,**WHEN** EditMode + CI 断言运行,
  **THEN** ① `whitelist_category` ∈ 五类 且 ② `trigger_source` ∈ 合法集(**禁止集任意一项 = 失败**)。
  **双重负向夹具(缺一不可)**:注入 `whitelist_category: Sting` 的行 → **必须失败**;
  注入**类别合法但 `trigger_source: VitalsCrossed`** 的行 → **必须同样失败**。
  **NOT-RUN 守卫**:字段缺失 ⇒ 断言**报错退出**(不得静默通过)。
  > **2026-09-18 重写**:原判据 ② 「不存在 CueId → 状态变化枚举的直接映射」在 **44 侧不可证伪**
  > —— 映射发生在**上游发射点**;伪装过的播报(如「结案钟声」)对断言**透明**,
  > 且负向夹具只给 ① 写了。现改为**两个必填字段**(§Event Table Schema),把断言从「查内容」
  > 升为「**查内容 + 查时机**」。
- **AC-44-10** `[L]`:**GIVEN** 病人在呼吸层从「急促」过渡到「停顿」(转危),**WHEN** 听测,
  **THEN** 过渡是**连续**的,无突变音效标记阈值跨越。
- **AC-44-C1** `[A]`:**GIVEN** 快照清单,**WHEN** 检查切换触发源,**THEN** 全部由
  **拟物**触发源(玩家行为 / 世界可感知声)驱动;**无任何快照由非拟物的状态变化触发**。
  > **2026-09-18 改写**:原判据「行为 / 场景状态驱动」**不可证伪** —— 病程**就是一种**「场景状态」。
  > 现收窄为**拟物 vs 非拟物**(见规则三)。

### 组三 · 呼吸两层与精度档(ADR-018 §四 需求① —— V-8.7 债务清偿)

- **AC-44-01(BLOCKING)** `[A]`:**GIVEN** 呼吸附加音层,**WHEN** 在低 / 中 / 高三档下播放,
  **THEN** 附加音层**始终非零**;**断言不存在任何「档位 ⇒ 该层静音」的实现路径**。
- **AC-44-02** `[L]`:**GIVEN** 三档的呼吸音,**WHEN** 听测,**THEN** 差异**可辨**且**非静音型**
  (通带更窄 / 噪声底更高,而非更小音量)。
- **AC-44-08** `[L]`:**GIVEN** 附加音层(细湿啰音),**WHEN** 听测 + 波形检查,**THEN**
  它是**吸气末、断续、细密的爆裂音**(搓头发 / 撕 velcro),**咳嗽后不消失**;
  **不得**为连续水声;**无机械判据 → 须波形 + 双盲听测留证**。
  > **2026-09-18 增补**:① 「非连续水声」原无判据 → 改由**波形(瞬态簇)** + 人工签名;
  > ② **相位锁定**:附加音层`trigger_phase ∈ [0.6,0.95] × 基础周期 ± jitter`
  > → EditMode 断言策略字段存在(`[A]` 部分),听测验正确性;
  > ③ **咳嗽交互**:咳嗽 cue **不得** `EndLoop`(见 §Edge Cases)—— EditMode 断言
  > 「咳嗽 cue 的实现不调用 `EndLoop`」。
- **AC-44-14** `[A]`(2026-09-18 新增):**GIVEN** 音频事件表的呼吸类 cue,
  **WHEN** 检查,**THEN** 基础层与附加音层**共用同一节拍时钟**(附加音层相位锁定,非独立循环)。

### 组四 · 语声变体库(ADR-018 §四 需求②)

- **AC-44-03** `[A]`+`[L]`:素材形态 = **变体库**(男/女 × 强/中/弱 × 数种句尾),运行时按
  **(`CueId`, 精度档, 强度)** 选变体并**交叉淡化**;
  **禁**对已录语声做五项独立调制(音高 / 气声 / 语速 / 断句 / 共鸣)。
  > **2026-09-18**:原写「病种语义」——**44 读不到**(幽灵输入,见 F-44.3 注);改为 `CueId`。
- **AC-44-04** `[A]`:交叉淡化**无爆音(spike)、无相位抵消**;
  **滤波 / 噪声底变化 ramp ≥ 50 ms**(2026-09-18 补齐硬下界)。

### 组五 · 接触噪声 / 信噪比(ADR-018 §四 需求③)

- **AC-44-05** `[A]`:SIGNAL_dB / NOISE_FLOOR_dB / CONTACT_NOISE_DB 在 `Stethoscope` 总线暴露,
  由 `TierMap` 驱动;**SNR_dB 按 F-44.2 计算并钳位 −24…+24 dB**。
- **AC-44-06** `[A]`:`READ_FLOOR_MIN > 0` 在音频侧有对应实现 —— **背景噪声线性幅度 > 0**,
  但也不毁掉信号。**判据 = 噪声下界有限**(非「dB ≠ 0」,见 F-44.2 注)。

### 组六 · 联机(ADR-018 §五 需求④ —— 2026-09-18 裁定 D-A 重写)

- **AC-44-07** `[A]`:**GIVEN** 联机下同一听诊 cue,**WHEN** 每台设备渲染,**THEN**
  每台设备按**本机玩家自己**的技能档渲染听诊音(`SetTier(TierSource.Local)`);
  **脉案字体量亦各自分叉**(回归测试覆盖)。
  > **2026-09-18 重写**:原判据「四名玩家听诊音层次一致(取主机技能)」**已作废** ——
  > 其依据(单 `AudioListener` ⇒ 每设备一条总线)为假(裁定 D-A)。**新判据 = 各按本机技能**。
- **AC-44-D1** `[A]`:**GIVEN** **远端实体**(玩家 / 病人 / 受伤敌人)的空间化,**WHEN** 检查位置来源,
  **THEN** 复用 **ADR-001 §一之二 `IPositionalChannel`**(**按 `ActorId` 索引的 latest-value 表**,
  2026-09-18 自 `IReplayPipe` 拆出);44 **不自建位置通道 / 不新增 QoS 通道 / 不做语音通道**,
  且**只读该表取声源锚点,禁读其做判定**(D-44 消费纪律)。
- **AC-44-D7** `[A]`(BLOCKING,2026-09-18 新增 —— **原缺口,关键**):**GIVEN** 上游(13 / 9)的 cue 触发,
  **WHEN** 检查其输入集,**THEN** 它 ∈ **{ 流事件, 版本化烘焙数据 } 的纯函数** ——
  **禁本地 RNG、禁读表现态位置**;各客户端由**本地流副本自行派生** cue,**cue 本身不复制**。
  > **为何 BLOCKING**:否则各客户端会**听到不同的病人** —— 静默、不可重现、且只在联机出现。

### 组七 · 空间化与坐标系(ADR-018 §二 · ADR-015)

- **AC-44-D2** `[A]`:**GIVEN** `AudioCueDto.Cell`(整数格),**WHEN** 空间化,**THEN** 由表现层把整数格换算为 float;
  **禁从 float 反推逻辑格**。
- **AC-44-D3** `[A]`:音频空间化**不写 sim**、**不回写三流**。
- **AC-44-D8** `[A]`(2026-09-18 新增):**GIVEN** 世界语境呼吸(F-44.7),**WHEN** 有门 / 墙遮挡,
  **THEN** 施加低通(**穿门 ≠ 静音**);且**同 cell 多病人按距离分别空间化**。

### 组八 · 管线与素材(ADR-018 §五 补注 · ADR-014)

- **AC-44-D4** `[A]`:音频事件表走 **ADR-014 烘焙管线**;玩家构建**零 JSON 解析器**。
- **AC-44-D5** `[A]`:素材本体 = `assets/audio/`(Addressables 流式),与 `data-core` 组区分。
- **AC-44-D6** `[A]`:素材缺失 = **编辑期烘焙门拒绝**(构建失败,不是运行期静默降级)。
- **AC-44-D9** `[A]`(2026-09-18 新增):**GIVEN** 事件表 schema,**WHEN** 烘焙,**THEN**
  每行含 `whitelist_category` + `trigger_source` + `schema_version`(§Event Table Schema)。

### 组九 · 无障碍 / VR / 性能

- **AC-44-11**(**P1b**)`[L]`(2026-09-18 重写):VR 下**听测**无晕动症相关副作用
  (**判据 = 受试者主观报告 + 帧率实测**);头显挂**单 `AudioListener`**(`[A]` 侧)。
  > **原判据「无晕动症副作用」不可测** —— 现拆为**可测的帧率** + **主观报告**两项。
- **AC-44-12** `[L]`(ADVISORY,2026-09-18 重写,裁定 D-C):**三条具名项** ——
  ① 视觉替代 = 引用 `AC-13-F1`(13 已交付,默认可见);② **显式 P0 非目标**:
  「隔墙听觉检测在 P0 无视觉等价物」;③ 44 自己的义务(mono / 总线音量 / 语声字幕)。
  > **原判据「关键音频必须有视觉替代」是标语** —— 未命名通道、未指派系统,且 44 零 UI 无法自渲染。
- **AC-44-13** `[A]`:提供 **mono 选项**。
- **AC-44-15** `[L]`(2026-09-18 新增):**病人语声有字幕**(WCAG 1.2.2);归 42 呈现,44 供语义。
  > **2026-09-20 回刷(承 `/review-all-gdds` R-2⑤)**:**字幕的呈现侧落点 42** 此前**零承载**
  > (`skeuomorphic-ui.md` grep `字幕` = 0)⇒ 本条的文字呈现义务**悬空**(AC 已批准但接收方未立案,
  > 与 7b→44 同型)。**48 已把教学口述字幕挂在本条管线上**(`tutorial-and-onboarding.md:93-97`),
  > 悬空影响面含 48。**落点登记**:42 的呈现义务由 42 的 GDD 承接(2026-09-20 已在
  > `skeuomorphic-ui.md` 登记「语声字幕呈现」行);本条保持「44 供语义」半边不变。
- **AC-44-E1** `[A]`:**GIVEN** P0 构建,**WHEN** 检查 VR 音频,**THEN** **不在 P0 范围**;
  **P1b** 交付切面(挂点 / 快照 / 延迟)已登记。
  > **2026-09-18 修正**:原写「P1a 交付切面」—— 与硬约束 8 的 P1b **自相矛盾**,已改 P1b。
- **AC-44-E2** `[A]`:音频 DSP 占用不超帧预算(16.6 ms 平面 / 11.1 ms VR);
  **音频触发不在 `<50 ms` 急救延迟路径内**(⚠️ 2026-09-20 回刷:原「急救判定本地即时」为 ADR-011 **改判前**口径 ——
  联机判定归主机(Amendment B),本地仅预表现;音频在**预表现**之后触发,延迟路径语义不变)。
- **AC-44-E3** `[A]`:混音拓扑**七条总线**(Master / Music / Ambience / Voice / SFX / Stethoscope / UICue)
  + **Aux / Reverb send** 单一出处,与 ADR-018 §三 一致。
  > **2026-09-18**:ADR-018 自身的总线列表漂移(Summary 与 Decision 写**六条**、漏 `UICue`)
  > —— **ADR 侧同批订正为七条**(见 ADR-018 修订记录)。

### 依赖未还清的判据(集中登记,便于排期)

| 依赖项 | 判据 | 状态 |
|--------|------|------|
| **V-8.7 四条硬需求**(`diagnosis-system.md:1557-1586`) | AC-44-01…07 逐条可验证 | ✅ 本 GDD 落 AC |
| **`TICK_SECONDS`**(D-8-7) | 呼吸层连续变化的时间量纲 —— 呼吸层循环节奏需它 | ⚠️ **未还清**(8 的硬前置) |
| **`Project(Sign_j) → [0,1]`**(D-8-4) | **Intensity** 的来源归一化(F-44.6 的上游输入) | ⚠️ **未还清**(8 的硬前置) |
| **ADR-001 修订** | 第二 QoS = ActorId 索引 latest-value 表 + 病人 / 受伤实体锚点发布者 + 44 消费者登记 | 🔧 **2026-09-18 已改**(见 ADR-001 修订记录) |
| **声学方向 / sound-bible 实例** | 与 `/art-bible` 同批定 | ⏳ 待办 |

> **本节不覆盖的东西(缺口显式化,不作默示)**:
> ① **VR 音频实现**(**P1b**,ADR-018 §七;层级订正见 §缺口第 8 条注)· ② **语音通话**(不做,ADR-018 §五)·
> ③ **玩家可见的音频设置 UI**(归 42)· ④ **音频素材的具体录制**(素材债,content 批兑现)。

## Open Questions

### 开放问题

| # | 问题 | 归属 | 建议解 |
|---|------|------|--------|
| **OQ-44-1** | 呼吸层三档差异**能否被听出来**(AC-44-02 的实测)? | 44 实现期 | spike:低/中/高三档盲听测试;失败则调 `TierMap` |
| **OQ-44-2** | 变体库的**素材量**上限? | audio-director / content 批 | P0 先录最小可用集(每组合 ≥2),P1a 扩 |
| **OQ-44-3** | ~~单 `AudioListener` 在联机下的混音行为~~ | ~~44 实现期~~ | ✅ **已消解**(2026-09-18 裁定 D-A:前提为假,联机 = 各自设备各自 mixer;spike 不再需要) |
| **OQ-44-4** | ~~音频事件表的 schema 细节~~ | ~~44 实现期~~ | ✅ **已升格**(2026-09-18:BLOCKING 门须有载体 → 定型为 §Event Table Schema 小节) |
| **OQ-44-5** | `Unity 6 audio mixer improvements`(post-cutoff)**是否影响本设计**? | spike | `modules/audio.md:4` 自陈知识缺口 —— **实现期实测**;基础 API 长期稳定,接口层不受影响 |
| **OQ-44-6** | 京戏 / 民国氛围曲的**史实考据边界**? | 支柱五 + `/art-bible` | 未证实的史料不得落盘;音乐方向由 audio-director 定,考据走 `sourcing-brief` |
| **OQ-44-7** | 「停顿 vs 停止」的可听辨**具体怎么做**(AC-44-10 / `disease-simulation.md:1118`)? | 44 实现期 | spike:呼吸尾音设计;失败则退回「玩家看动画」 |
| **OQ-44-8** | 病人 / 受伤实体的**表现态锚点发布者**由谁实现? | 45(P1b) | ✅ **已登记**(2026-09-18,ADR-001 §一之二 发布者表):9 伤情 / 13 病人 AI 的**主机侧锚点持有者**;44 按 `ActorId` 读表。**实现随 45 走 P1b**(发布者代码未写,契约已定)。

> **2026-09-18 修订**:`OQ-44-3` 消解(前提被证伪)、`OQ-44-4` 升格(Schema 定型)、
> `OQ-44-8` 新增(锚点发布者,原缺口)。

### 已结案(留档,不再追踪)

| 问题 | 结案 | 出处 |
|------|------|------|
| **44 的呈现层边界** | ✅ **与 42 同构**(只触发 / 只渲染) | ADR-018 §一(2026-09-15) |
| **语声是否走参数调制** | ✅ **降格为变体库混合** | ADR-018 §四 / `diagnosis-system.md:1567-1575`(2026-09-14 复核) |
| **联机是否分人分混** | ⚠️ **已重开并改判** —— 原「统一取主机技能」的前提(单 `AudioListener` ⇒ 每设备一条总线)**为假**;2026-09-18 用户裁定 D-A:**按本机玩家自己技能档** | ADR-018 §五(2026-09-14 原裁定)→ 本 GDD 规则七(2026-09-18 裁定 D-A) |
| **无提示音如何验收** | ✅ **白名单断言机械化**(不再是 prose) | ADR-018 §六 / AC-44-09 |
| **VR 音频何时做** | ✅ **P1b**(P0 不做) | `game-concept.md:723`(权威)+ ADR-018 §七(2026-09-18 订正 P1a → P1b) |
| **`AudioListener` 挂点** | ✅ **平面 = 主相机,VR = 头显** | ADR-020 §七(2026-09-15 结清 `adr-018:295`) |
| **44 是否走 ADR-014 烘焙管线** | ✅ **走**(单一数据管线) | ADR-018 Alternatives 5 |

## 修订记录(2026-09-18 · 首轮 `/design-review` 落盘)

> **首轮评审**:`--depth full`,8 位专家(full adversarial)+ creative-director 综合;
> 裁定 **MAJOR REVISION NEEDED**(8 项阻断)。**同日全修并落盘**(用户选 [A] 现在就修)。
> 用户四项 D 裁决:**D-A** 重开联机裁决→本地档 · **D-B** 距离归一化 tie-break ·
> **D-C** 接受无障碍 P0 非目标 · **D-D** 现改 ADR-001。**机制数值仍冻结**(用户自己调)。

| # | 阻断项(来源) | 修法 | 落点 |
|---|--------------|------|------|
| 1 | **F-44.2 公式量纲破裂**(systems-designer / audio-director / sound-designer) | dB/dB 除法 → 线性化后相减:`SNR_dB = SIGNAL_dB − 10·log10(10^(NF/10)+10^(CN/10))`,钳位 ±24 dB | §F-44.2 |
| 2 | **AC-44-09 不可执行 + 可绕过**(sound-designer / qa-lead / audio-director) | schema 增 `whitelist_category` + **`trigger_source`**(查时机);`OQ-44-4` 升格为 §Event Table Schema;双重负向夹具 + NOT-RUN 守卫 | §规则二 · §Event Table Schema · AC-44-09 |
| 3 | **「单 Listener ⇒ 每设备一条总线」为假**(unity-specialist / game-designer / network-programmer) | 重开 2026-09-14 裁决 → `SetTier(TierSource.Local)`;**ADR-018 §五 理由重写**、Alt-4 拒绝理由改 | §规则七 · AC-44-07 · ADR-018 §五 |
| 4 | **幻想主通道无总线**(audio-director / game-designer) | 新增 **F-44.7 世界语境呼吸路径**(3D + 遮挡低通 + 衰减 + 多病人规则) | §F-44.7 · AC-44-D8 |
| 5 | **TierMap 列集自相矛盾 / 悬空符号**(sound-designer / systems-designer) | 定型唯一列集;删 `breathLayer`;钉 Hz/dB 量纲;`Tier` 移出 DTO;ramp ≥50 ms | §F-44.1 · §F-44.5 |
| 6 | **Intensity 无声学映射**(sound-designer) | 新增 **F-44.6**(增益 / 密度 / 分桶) | §F-44.6 |
| 7 | **远程 cue 来源 / 病人锚点未声明**(network-programmer) | 写清 **cue 不复制**(客户端自派生)+ 病人锚点发布者;**ADR-001 现改**(第二 QoS 按 ActorId 索引 + 消费者登记) | §Edge Cases · AC-44-D7 · ADR-001 |
| 8 | **DTO 引用 sim 类型 / AC-B1 grep 退步**(unity-specialist / qa-lead) | 定型独立契约程序集(仅 BCL);AC-44-B1 升为 asmdef 白名单 + IL 扫描 | §Interactions 注② · AC-44-B1 |

**推荐项同批修**:`VitalsDto` 幽灵入口删除(规则一)· `DialogueFocus` 快照自相矛盾(规则三 / AC-44-C1)·
`EndLoop` 永不到达 · 咳嗽 vs 呼吸层相位锁定(AC-44-14)· 快照 / 脚本参数归属分离 ·
优先级表合并 + 删「关键 cue」· 无障碍三条具名项 + P0 非目标(裁定 D-C)· VR P1a/P1b 三方矛盾 ·
证据类型 `[A]/[L]` 全组标注 · 缺失素材族(把脉 / 脚步矩阵 / 建筑 / 敌人 / 动物 / 玩家自身 / 天气 / 混响)·
AC 计数 27 → 32 · `OQ-44-3` 消解 / `OQ-44-4` 升格 / `OQ-44-8` 新增 · Addressables 卡顿边界。

**跨 ADR 涟漪(同批)**:`adr-018` §三(七总线漂移)· §五(理由重写)· §六(白名单增 `trigger_source`)·
§七(P1a→P1b)· §Key Interfaces(schema 增字段)· `adr-001`(第二 QoS 表化 + 44 消费者 + 锚点发布者)。

### 结案(2026-09-18):用户裁定接受修订、**免二轮** → ✅ Approved

**免二轮 = 显式风险接受,不是「已核对」** —— 8 项阻断由**本轮撰写 + 修订的同一上下文**判修,
**未经独立复核清点**。**重开触发条件**(任一命中即须开新会话跑二轮 `/design-review`):

1. **门 A / 契约程序集实证失败** —— AC-44-B1 的 asmdef 白名单 + IL 扫描在实现期拦下 `AudioCueDto`
   引 sim 类型(原阻断 8 的残留 → 构建失败)。
2. **`IPositionalChannel` 契约被 45 的实现证伪** —— 表化 latest-value 在 P1b 真实网络条件下
   无法承载 per-`ActorId` 锚点(原阻断 7)。
3. **白名单断言被绕过** —— AC-44-09 在**非具名触发源**(基于时序的状态播报)上出现 false-negative,
   证明 `trigger_source` 字段仍不足。
4. **`setTier(TierSource.Local)` 在联机实现中被证伪** —— 听觉熟练度成长轴(裁定 D-A)在 4 人同场
   下产生不可接受的分叉。

**残留未还清判据**(非阻断,排期项):`TICK_SECONDS`(D-8-7)· `Project(Sign_j) → [0,1]`(D-8-4)·
声学方向 / sound-bible 实例(与 `/art-bible` 同批)· `OQ-44-8` 发布者实现随 45 走 P1b。
