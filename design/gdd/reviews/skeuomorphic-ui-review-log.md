# 拟物 UI 框架(系统 42)评审日志

> 评审史轨道:`design/gdd/reviews/[doc-name]-review-log.md`。每条评审按时间倒序追加。

## 裁定落地注记 — 2026-09-22 — **重开触发条件 ④ 已触发(未重开)**

**事件**:重开触发条件 ④(「`OQ-42-1` / `OQ-42-12` 用户裁定落地」)的**两项裁定同日落地**:
- **`OQ-42-1` = 甲「恒定屏幕尺寸」** —— F6 单一化:`scale_world = k_screen × 2·d × tan(FOV_v/2)`(**屏幕占比恒定**);
  **乙案「恒定世界尺寸」作废**,`k_world` 撤出旋钮表。**后果**:42 **新增对 2 的 `FOV_v` 依赖边**(该边**无条件成立**,
  原「⚠️ 半 · 取决于裁定」解除);2 侧反向义务 `O-15` 闭合;装载期断言 ④ 由**条件断言升常驻**。
- **`OQ-42-12` = 甲「取 `min`」** —— F7 单一化:`s = min(W_screen/W_ref, H_screen/H_ref)`;乙案(加权 `match`)
  作废,权重 `w` 不再定义。F7 装载期断言 `s > 0` **自此可由表达式写判据**(原不可写)。

**状态**:**本触点已触发** —— 按首轮约定,条件 ④ 达成即「可重开二轮评审」。
**是否据此重开 42 的 `/design-review` 由用户决定**(本注只记录触点达成,不代裁重开与否;
**2026-09-22 用户已答:不重开,继续设计面 OQ 批裁轮**)。
其余三条件(① spike 落盘 ② `AC-42-B4` 降级路径启用 ③ F4 三层口径被证伪)均**未触发**。

**同批(2026-09-22)另两项 42 OQ 裁定**(不属触发条件 ④ 的登记范围,另注):
**`OQ-42-3` = 甲「不虚拟化(翻页制)」** —— 全案 UI 列表 / 多页签**一律翻页制(零滚动)**,
`Pages_frame` 语义定为「单页内容函数」(非滚动位置),`LIST_VIRT_THRESHOLD` 幽灵旋钮撤出;
**`OQ-42-10` = 甲「无通用恢复」** —— 42 **不持「上次焦点位置」**,重开任何模态 = 焦点落首可聚焦元素。

**落地面**:`skeuomorphic-ui.md`(F6/F7 段 · 算例表 · 文件头 ④ · §Dependencies §一/§三 · §六 风险表 · §Tuning Knobs §二 · OQ 表两行)·
`camera-and-viewpoint.md`(42 下游行 · `O-15` 两处义务行 · `FOV_v` 旋钮注 · 验收前提清单 ⑤ · `OQ-2-2`)·
`systems-index.md`(row 42 两处 + §1 表)· 本日志 · `docs/consistency-failures.md` 跟进注。

## Review — 2026-09-17 — **结案:✅ Approved(免二轮)**
Scope signal: **L**
Verdict path: 首轮 `MAJOR REVISION NEEDED` → 用户裁定 `[A]` 本轮修订 → **17 项阻断 + 14 项推荐修 +
5 项用户裁定当日全部落盘** → **用户裁定接受修订、免二轮** → 标记 Approved。
Summary: 用户接受首轮修订结果,不再开二轮评审。**这是显式风险接受,不是「已核对」** ——
44 条修订(17 阻断 + 14 推荐 + 涟漪)未经第二双眼睛复核,而「修订自身引入新缺陷」正是
**前提错误**这类问题的高发形态(尤其 **F4 从「跨域全序」重写为「三层口径」**、
**`AC-42-B4` 从「内部实现禁令」收窄为「导出面禁令」**、**G 组 6 条新门**三处结构性改动)。
**重开评审的触发条件(四者任一)**:
① **三组 spike 结论落盘**(焦点桥 / 自定义材质 / 图集)—— 42 的观感承诺与降级路径均压在其上;
② **`AC-42-B4` 的降级路径在实现期被启用**(须验证「只约束导出面」的收窄口径真的够用,
且降级代码确实住在 42 自己的程序集内,否则 `AC-42-A4` 被破);
③ **F4 的三层口径在实现期被证伪**(例:`PanelSettings` 存在非 Overlay 模式 ⇒ L2 不再二值);
④ **`OQ-42-1` / `OQ-42-12` 用户裁定落地**(前者决定是否新增对 2 的依赖边;后者决定 `F7` 取 `min` 还是加权 `match`)
—— ✅ **2026-09-22 两项已裁**(`OQ-42-1` = 甲「恒定屏幕尺寸」· `OQ-42-12` = 甲「取 `min`」,**触点已触发**,见文首「裁定落地注记」;重开与否由用户决定)。

**结案涟漪**:`skeuomorphic-ui.md` 文件头 → **Approved**(含「免二轮说明」+ 重开触发条件四条)·
`systems-index.md` **四处** → Approved(§1 表 row 42 · §11 队列行 · §10 已有 GDD 行 · §design-order 矩阵 row 42)·
`production/session-state/active.md` 本节重写。

**未结项(随结案带入实现期,不阻塞)**:~~`OQ-42-1`(世界锚点恒定屏幕 / 世界尺寸)~~(**✅ 2026-09-22 已裁 = 甲「恒定屏幕尺寸」**)·
~~`OQ-42-3`(列表虚拟化)~~(**✅ 2026-09-22 已裁 = 甲「不虚拟化(翻页制)」**)· `OQ-42-4`(首帧引擎默认焦点策略,须 spike)· `OQ-42-5`(39 是系统还是界面)·
~~`OQ-42-12`(`F7` 取 `min` 还是加权 `match`)~~(**✅ 2026-09-22 已裁 = 甲「取 `min`」**)· `OQ-42-14`(墨龄时钟源 = `ITickProvider` 只读视图是否属 P0 就绪)·
> **✅ 2026-09-22 批裁轮补记**:~~`OQ-42-3`~~ 亦已裁 = **甲「不虚拟化(翻页制)」**(该行划除);
> 连带解除 F5 的 `Pages_frame` 语义双分支 + `LIST_VIRT_THRESHOLD` 幽灵旋钮。
> **本件未结项现为**:`OQ-42-4` · `OQ-42-5` · `OQ-42-14` 三项 + **§七 载体欠账表 15 行**。
**§七 载体欠账表 15 行全部待建**(`tests/` 与 `.github/workflows/` 尚不存在)。

Prior verdict resolved: **首轮条目见下** —— 17 项阻断 + 14 项推荐修全部落盘;二轮未跑(用户裁定免)。

---

## Review — 2026-09-17 — Verdict: MAJOR REVISION NEEDED → **用户裁定 [A] 本轮修订;17 项阻断 + 14 项推荐修当日全部落盘**(~~待二轮评审~~ → **同日用户裁定免二轮,已 Approved,见上方结案条目**)
Scope signal: **L**
Specialists: unity-ui-specialist · ux-designer · accessibility-specialist · art-director · qa-lead · systems-designer(×6 报告,haiku 并行)· creative-director(opus 串行综合)
Blocking items: 17(去重后 **11 条 BLOCKER 级**)| Recommended: 14
Summary: 首轮 `/design-review`(`full`)。**CD 终裁:42 的架构姿态正确** —— 两栈边界(UI Toolkit 主 +
UGUI 补 world-space)· 焦点桥归引擎 · 只渲染不持有状态(承 ADR-013 §9 C3)三条主承重面
**未发现需推翻之处**;17 项阻断**全部是规格空洞、判据错挂与自相矛盾**,不是设计错误 ⇒
按清单一次性补齐,无重架构。CD 同时把五项跨域口径**交用户拍板**(读数条 owner · 刻度条禁令范围 ·
焦点门原子性 · 读数条值的通道 · 是否本轮修订)。

**🔴 五项用户裁定(2026-09-17,均照准)**:
① **[A] 现在就修订**;② 敌人读数条 owner = **27 敌人 AI**(原写 25)—— **态 / 时机归 27,画归 42,
值走 9 的既有 `VitalsDto`(不新增通道)**;③ 「任何刻度条」禁令**收窄为「墨侧禁刻度条」** ——
黄铜侧的錾刻刻度**本身合法**(旧稿同一份 GDD 里「库内零刻度条」与「敌人读数条有錾刻刻度」直接矛盾);
④ 焦点门「原子」取 **systems 版** —— **「原子」= 对外永不可观测到「两门皆开」**,
两门皆关窗口 `0 ≤ W_trans ≤ 1 frame`,**原 `E-3` 删除**;⑤ 读数条的**值**走 **9 的 DTO**。

**🔴 六路专家收敛的三条结构性根因**:
- **`F4` 的前提是假的(最具后果的一项)** —— 旧稿把两栈 z 序写成跨域全序 `S_UITK = φ(S_UGUI)`
  并**在装载期断言 `φ` 严格单调 + 单射**。但 UI Toolkit **Overlay 面板在全部相机之后合成**,
  UGUI World Space canvas 是**相机内几何** ⇒ **两个数分属两个合成域,不可比**,
  那条断言**是在断言一个不存在的对象**。更糟的是它会诱导实现走「调 `φ` 控制谁压谁」这条
  **注定失败的路**(引擎根本不让世界几何盖住 Overlay),得到「调了没用」再误判为 spike 失败。
  **修**:F4 重写为**三层口径**(L1 相机内 / L2 跨域**结构性二值** / L3 覆盖层内),`φ` **删除**,
  断言改锚为**两份域内序表各自无重复**;规则二 / `E-4` / `AC-42-F4` / 算例 / ADR-013 §一
  同批订正。
- **`AC-42-F1` 的主语是空集** —— 旧稿写「**已有** `design/ux/[screen].md` 的界面」,而该目录
  **不存在** ⇒ 判据在空集上**平凡变绿**(本仓反复出现的假通过形态)。**修**:改为**闭集点名六个 P0 屏**
  (① 脉案页 39 ② 存档位 7b ③ 库存容器 20 ④ 设置壳 42 ⑤ 教学 48 **⑥ 医馆面板 24** ——
  第六项是首轮补入:义务在 `AC-42-F5` 而主语不在)+ **`NOT-RUN` 纪律**(任一无 UX 规格者记 `NOT-RUN`
  并阻塞签核)。
- **`AC-42-B4` 与 ADR-013 的降级路径互斥** —— 旧文字断「焦点移动**不可由 42 自行计算**」,
  而降级 = **42 自实现焦点算法**(那时必然要算邻居)⇒ 降级一启用 `B4` 立刻变红。
  **这不是笔误,是把「对外契约」写成了「内部实现禁令」。修**:收窄为**只约束导出面**
  (邻居枚举 / 方向投影 / 几何查询不得出现在 42 的对外类型面上),内部实现不受约束,
  且**降级代码装配位置 = 42 自己的程序集**(否则 `AC-42-A4` 的反向白名单会被破掉 ——
  两道门原来二选一)。

**首轮补齐的其余阻断(逐项已落盘)**:
- **`AC-42-D4` 同时不可判且与渲染路径冲突** —— 「不读 DTO 的数值字段」无作用域(实现可把乘子
  说成「不是数值」),且 `AC-42-F5` **必须**读值。**修**:改**正面白名单**(枚举 / bool / 计数)+
  **限定在调试视图内**。
- **`V-11` 的「运行期只允许降级为续页」零 AC** —— 最易做、最像没问题的违规(`text-overflow`
  省略号,一行代码)**无门可抓**,而它恰是拟物铁律的反面(半截脉案画不在纸上)。
  **修**:新增 **`AC-42-F6`**(降级白名单**逐条枚举** + 开发期显式冲突路径 + 切分策略归内容拥有者)。
- **P0 承诺零 AC 真空(六条)** —— 规则五(元件库唯一出口)· 规则八(设置壳不缓存他系统状态)·
  对比度的「**全档均须满足**」义务 · 动效缩放挂点 · 载体欠账表会漂移 · 幻想的「我的笔迹 / 墨还没干」
  数据路径。**承诺写在正文里、零判据 = 没写。修**:新增 **G 组 6 条**
  (`G1`–`G6`,全 BLOCKING)。
- **`E-1`/`E-2` 的静默死锁与门语义** —— 取「不可观测双开」版后,`E-3`(窗口内吞键)描述的是
  **允许态**,写成 edge case 会把合法态固化成异常、诱导实现做「真原子切换」或「窗口内补发」。
- **`F1` / `F2` / `F5` / `F6` / `F7` 五式口径修正** —— 含循环定义(①`W_min = SL+SR+C_min`
  ②断言 `SL+SR+C_min ≤ W_min` 代入得恒真)、量纲不齐(断言断**面积**而公式给**计数**)、
  空集主语等。
- **`AC-42-C3` 的第二个载体漏登记**(构建期命名 job)· **`D3` 载体登记留白** —— 一并补入 §七。

**Recommended(14 项,均已落盘)**:规则十三(笔迹变体族 / 变体选择会话内稳定 / 墨龄 = 纯函数)·
`OQ-42-14`(墨龄时钟源 = `ITickProvider` 只读视图,非墙钟)· §七 表补登记 9–10 条 AC ·
`OQ-42-7` 闭合(三处 ADR 就地修订全部落盘,含 adr-013 §六 钩子三改四)→ **`IAccessibilityHooks`
补 `ApplyMotionScale`** · 医馆面板入 §UI Requirements 表 + `AC-42-F1` 闭集 · 27 ↔ 42 反向边双向化
(`O-27-1` 结清,27 侧三处回填 ✅)。

**Unresolved(带进二轮)**:~~`OQ-42-1`(世界锚点恒定屏幕 / 世界尺寸 —— 决定是否新增对 2 的依赖边)~~(**✅ 2026-09-22 已裁 = 甲「恒定屏幕尺寸」**)·
~~`OQ-42-3`(列表虚拟化)~~(**✅ 2026-09-22 已裁 = 甲「不虚拟化(翻页制)」**)· `OQ-42-4`(首帧引擎默认焦点策略,须 spike)· `OQ-42-5`(39 是系统还是界面)·
`OQ-42-7` **已闭合** · ~~`OQ-42-12`(`F7` 取 `min` 还是加权 `match` —— 用户裁定)~~(**✅ 2026-09-22 已裁 = 甲「取 `min`」**)· `OQ-42-14`(墨龄时钟源)·
> **✅ 2026-09-22 批裁轮补记**:~~`OQ-42-3`~~ 亦已裁 = **甲「不虚拟化(翻页制)」**(该行自 Unresolved 划除)。
**三组 spike**(焦点桥 / 自定义材质 / 图集)结论落盘 —— 42 的观感承诺与降级路径均压在其上。

**涟漪已落盘**:`adr-013`(§一 z 序条款重写 · §六 钩子三改四 + `IAccessibilityHooks.ApplyMotionScale` ·
`Validation Criteria`)· `enemy-ai.md`(§二 下游表 42 行 ✅ 双向 · `O-27-1` 结案 · §五 双向性核对行)·
`systems-index.md`(row 42 §1 表 / §6 设计序 / §11 队列 / §10 已有 GDD 行 四处 + `OQ-42-1…14`)。

**未结的载体义务**:§七 载体欠账表 15 行**全部待建**(`tests/` 与 `.github/workflows/` 尚不存在),
由 **`AC-42-G5`(双向一致性断言)** 守 —— 表说待建而实际已建、或反之,均失败。

Prior verdict resolved: **First review** —— 本条目为首轮;无前序条目。
