# Cross-GDD Review Report — 2026-09-20

*范围:P0 全部 31 项系统 GDD(截至 2026-09-19 全 Approved)+ ADR-001…022 + TR 注册表 387 条。*
*性质:评审件(只读,不含对 GDD 的修改);处置以本报告 + `systems-index.md` 标注为记账。*
*生成:`/review-all-gdds`(full 模式)。Phase 2(一致性)+ Phase 3(设计整体论)+ Phase 4(跨系统走查)。*

## 方法与证据口径

- 未做 34,583 行全文读;先建抽取件 `/tmp/ragdd/`(163 个:`*.deps / *.knobs / *.fantasy / *.formulas / *.ac` +
  `ALL.*` 聚合 + `PILLARS.md` / `INDEX.md` / `REGISTRY.md`),4 组子代理只读抽取件路径。
- **主会话对全部结论逐条回实文件核 `file:line`**;核验失败的子代理结论已降级或证伪(见 §4)。
- `design/registry/entities.yaml` 的 `entities: 0` / `items: 0` ⇒ 注册表加速不可用,一致性核对靠抽取件全文。
  待办:本评审结案后跑 `/consistency-check` 回填注册表。

## Verdict:CONCERNS

4 组共报 🔴10 / ⚠️20 / ℹ️7。合并同根后 = **6 条根因**,其中 **5 条主会话判定为 Blocking**。
设计方向没有被证伪;被证伪的是「31 项全 Approved」这句话的**账实一致性** ——
多数根因属于「已裁定的东西没有传播到下游 / 记绿的裁定没有落地件」。

---

## R-1 剧情串联在需求账本之外 🔴(用户 2026-09-20 提问升格)

`grep 剧情/故事/串联/结局/通关/编排/他回来` 在 `tr-registry.yaml` 387 条中**零命中**。
机制侧已做完(`case-system.md` 的 `ScriptedChain` / `ChainEligible`、9 的 `DIS_NEURASTHENIA` 豁免类、
39 的空格呈现、8 的「查体全正常」变体);断的是**内容与归属**:

| # | 事实 | 证据 |
|---|---|---|
| 1 | `ScriptedChain(D)` 指向的内容目录**自陈不含 P0**,且通篇没有《他回来了》 | `case-system.md:584` → `content/adventure-catalog.md:24`(`grep -c 他回来了` = **0**)⇒ **引用却无登记** |
| 2 | 「写 37 时」这条委派至今**未兑现**,且住在已 Approved 的件里 | `random-events.md:1204`;同文件自设的「37 落笔并 playtest 前 52 不得标 Approved」已被**无声放宽**(`systems-index.md:88` 52 ✅ 2026-09-18) |
| 3 | 三案链内容债勾选项未勾;`systems-index.md:590` 仍写「内容池固定为兵痞」 | `:614-618` / `:590` vs `:132` / `:396` |
| 4 | `PATTERN_THRESHOLD=3` 走随机病例 ⇒ 三次偶发同病种可**静默吃掉**一次性 `PatternRecognized` | `case-system` Tuning(`PATTERN_THRESHOLD | 3`);无任何 AC 守「三案链可达性」 |

**没有系统拥有 P0 的结局。** 修法(待用户裁):(甲)在 `design/gdd/content/` 补 P0 三案内容件 + 指定串联/复诊触发
归属(动 6-9 个月冻结基线);(乙)在 `docs/p0-gameplay-preview.md` + `game-concept.md` 显式宣告「P0 无剧情推进
系统」;(丙)最小——修悬空指针 + 把 52 的门放宽记进 review-log + 刷 `:590`。**主会话判断:丙为无论如何都要做的底线**。

## R-2 已裁定的事没传播到下游 🔴(同根 5 处)

1. **ADR-011 Amendment B 从未到过 3 和 44**:`input-system.md:279`「判定**归表现层本地即时出结果**」、`:731`、
   **AC-3-B4 `:1170`「判定在本地出结果…主机结算与本地结果一致(重放对比)」** —— 这是 3 的**验收判据**,其字面
   正是被改判废除的那套;全文零命中 `Amendment B` / `EmergencyAttempt` / `预表现`。`audio-system.md:895`
   (AC-44-E2 括注「急救判定本地即时」)同。10 侧已按 C 路改口(`emergency-procedures.md:421`)。**照 3 的 AC 实现
   = 复刻被裁定为不可重放的架构**。
2. **`D-21-31` 的涟漪漏了 24**:`item-database.md:478-483` 已裁「求和 + **唯一钳制点在 F1 正文**,不落 5、不落 24、
   不落 18」;5(`time-and-weather.md:457`)、18(`processing.md:191/:206`)、21a 均已回刷,**24 侧零命中**
   (`clinic-machine.md:176/:245/:349` 仍自家 `clamp`;文件头 `Last Updated: 2026-09-17`)⇒ **双重钳制口径回归**,
   `clamp(clamp(clinic)+raw) ≠ clamp(clinic+raw)` 吞掉医馆饱和、预先封顶「位置即逻辑」奖惩。单一真源被破坏 ⇒ 主会话评 Blocking。
3. **`OQ-25-1` 已于 2026-09-19 裁路甲**(29 侧 `death-and-respawn.md:46` ✅),**25 自身仍标 🔴 待裁**
   (`combat-and-weapon-lines.md:101 / :2210 / :2309`)⇒ 同一编号在两文件状态相反。
4. 陈旧「X 无 GDD / ❌ 单边」集群遍布 6/30/3/42/2/1 的 Dependencies 表(31 项早已全成稿);
   `docs/consistency-report-2026-09-19.md:12-13` 的「时效滞后豁免」只覆盖 entities.yaml 注释,**不含这些行**。
5. 三项外抛义务,被指派侧零承接:7b 的存档事件名枚举 → 44(`audio-system.md` grep `7b|存档位` = **0**);
   字幕 → 42(`skeuomorphic-ui.md` grep `字幕` = **0**,而 44 的 AC-44-15 与 48 的 AC-48-15 都假设它存在);
   2 的 `AC-2-22⑥` 自己写着「⑥ 系统 3 **尚未登记** ⇒ 本 AC 当前仍红」(`camera-and-viewpoint.md:1276-1278`),
   3 侧 grep `O-16` = **0**。

## R-3 记绿的裁定没有落地件 🟡→🔴(第 3 种失效形态)

- **玩家致死通路**:25/29 一致宣布「别人可以杀你」并按路甲解绑 AC,但 **9 的 F4 死亡支只有 `self_limit=false`**
  (`disease-simulation.md:1062`),逐实体 `SelfLimited(entity,d)` **在 9 侧不存在**;29 自陈「未结:归 9 的下一轮」
  (`death-and-respawn.md:110`),25 的 `AC-25-6-08` 维持 `NOT-RUN`(`combat-and-weapon-lines.md:2073`)。
  ⇒ 死亡两件套(掉落 + 手生)与 AC-29-02/05/08/10/17 在 P0 **不可执行,且不会被任何测试发现**。
  处置:上述 AC 显式挂 `BLOCKED-BY-9`,不得记绿。
- **索引登记的「唯一真环」被自己系统的 BLOCKING AC 证伪**:`systems-index.md:541-543`「**唯一的真环:
  `30 技能 ↔ 37 病例`** —— 病例触发技能成长」vs `case-system.md:369`「**P0 的 37 不发任何数值奖励**」+
  `AC-37-32 :1062`(反射扫描全部出向载荷,禁 `xp`/`level`/`reputation` 字段)。按索引排实现 → 把「结案→成长」
  写进 37 → 被自家 AC 判红;按 37 为准 → 「唯一真环」消解,30↔37「只暴露两接口」的单向化理由失去对象。
  **需裁定,非文字问题**。

## R-4 同一改判只落地一半 🟡

- **10 的 R-6(跳过不再等值)只改了结果侧**:`skill-system.md:546` 验收清单仍写「跳过小游戏与手动完成获得
  **同等经验**」,且是**勾选项**(实现期当规格读)。经验等值 + 零时序成本 ⇒ 「永远跳过」在时间预算内仍严格占优
  —— 正是 R-6 攻击的同一论证的另一半。
- **判断类技能的死亡豁免无源**:`death-and-respawn.md:329`「诊断/辨证**不参与掉级**」并以 `AC-29-14` 声明
  「唯一出处 = 30 §4.3」;主会话读 §4.3(`skill-system.md:379-398`)——**只有 `floor(level×0.95)`,无任何豁免**。
  豁免是无源规则,且把死亡惩罚从「骨」移到「肉」,与 `game-concept.md:160-165`「主角也有身体」是**支柱原文级**张力。
- **`MotorSuppressed` 单 bool vs lease(真缺口,归 1)**:`player-controller-and-movement.md:1512-1526` 的注块
  **自证**互斥——4/10/25「互不知晓」+ 单个 bool ⇒ 4 的一次交互 `Release(false)` 顺手清掉 10 仍持有的压制
  (丢失更新)。4 的 `AC-4-19`(BLOCKING)与 1 的 `AC-1-23`(BLOCKING)**不可能同真**,注块原话「本条与
  『三者互不知晓』互斥…视为已知缺陷」。**全案唯一需要现在拍方向的存量互斥**(`OQ-4-13`,归 1:
  单 bool + 仲裁序 / per-source 位图 + lease / 改写现文)。

## R-5 横轴无量纲:31 份 Approved 件已越过「写第一个 `Step` 之前标定」的纪律 🔴

`OQ-8`(同场实体数)/ `OQ-25-8`(tick 频率)仍无值,而 10 份 GDD 的可校验性押在其上:25 的 `cooldown_ticks` /
`Decay_injury(Δtick)`、10 的 `JITTER_MAX` / `L_eval ≤ TICK_PERIOD`、18 的 `duration_ticks`(自陈
`BLOCKED-BY-OQ-25-8`)、8 的 `STALE_WINDOW` / `RECHECK_WINDOW`、27 的五个 timeout、52 的 `ROLL_INTERVAL`、
5 的 `WEATHER_BLOCK_TICKS`、30 的 `NOVELTY_COOLDOWN`、3 的 `DWELL_DURATION`、9 的 `TICK_SECONDS`。
时钟族混用(5/9/25 用 tick、7a 用墙钟 5 分钟、3/2 用毫秒)⇒ **难度曲线的「彼此一致」目前只是修辞**,
这些 AC 中部分已被记绿。处置:对上述 AC 统一改标 `BLOCKED-BY-OQ-8 / OQ-25-8`,**不替它们填数**。

> **✅ 同日结案注(2026-09-20 · 用户裁定)**:本条的根因已裁 —— `OQ-8` = **在场才模拟 ·
> `PATIENT_APPEARANCE_CAP = 24`**;`OQ-25-8` = **20 Hz(`TICK_SECONDS = 0.05`,`TICK_PERIOD = 50 ms`)**;
> ADR-017 复评门阈值同批 = 同场表现实体 ≥ 100 或 表现层帧时间 ≥ 8 ms。上列 10 份 GDD 的 `BLOCKED-BY`
> 标记已逐条解除(解除的是**量纲前置**,不是**数值** —— 各旋钮值仍归用户数值轮)。时钟族混用的**换算
> 可能性**已建立(tick ↔ 秒),惟 `TICKS_PER_DAY` / `TICKS_PER_SEASON` 仍待 5 侧定值(「日」的秒长未裁)。

## R-6 P0 的验证目标风险(设计整体论侧,不违支柱)⚠️

- **注意力预算**:床旁 30 秒判断链需同时管理 **9 条**主动流(位移/取景/五通道查体/病程/手感/开方/补给/病例/脉案),
  一次出诊 **13 条**——超出 4±1 约三倍。P0 砍掉的 5 条是生存类常规流,新增的 8 条全来自医疗判断层 ⇒ **注意力
  总量未降,只换了科目**。全案无一处对「同时管理流数」做过预算或验收(13 为本次首算)。
- **6 条潜在支配策略**:分料严格优于混料(`InQ=min`)、穷举布局优于先判断(`DEMOLISH_REFUND_RATIO` P0 恒 1 +
  24 即时读数)、**轻装出门最优**(52 读 `CarryLoad`=药量、20 定义=全部器物总重,`OQ-20-7` 未裁 ⇒ 带建材误报
  「带药多」)、永远跳过小游戏、无脑冲(判断类不掉级);「晾着昏迷者」在 P0 无对自动词。
- **手感不可学**:三扇门是隐藏判据 ⇒ 校准不可得(`emergency-procedures` Tuning,R-7 第 3 条);跳过路径取消了
  支柱①「小游戏最后几秒的时间压力」(`emergency-procedures.md:318`);P0 只有 2 个样本且无原型先行门(`OQ-10-12` 未裁)。
- **反馈方言缺上位裁决**:24「读数当场变了」/ 53「世界不打分但它记得(延迟不可归因)」/ 10「隐藏判据」三种方言
  各有理据、互不引用,作用在同一具身体同一间医馆。
- **经济侧 0/9 条环路可在现行数值下校验**,根因共同:21a 数值全冻结 + **21b 物品清单未立项未估算**。耗材/器械
  在 P0 **有语义、有呈现、无来源**(唯一能把材料变器具的 19 = P1a)⇒ 20 的「多带柳树皮 = 少带一卷绷带」取舍塌成一端。
- 17 份文档的「无提示音/无数字条」禁令**核验通过**;幻想主干约 24/31 收敛(「慢、读、挣得、不打分」);
  支柱对齐无违规系统。

### ℹ️ 三条(不建议动)

`TODMult` 来源列陈旧(机制一致);`ENV_MOD_MIN < 0` 承重约束单向记账;30 的 `INSIGHT_BONUS`(`skill-system.md:513`)
在 P0 **无任何落地消费点**(加成条件全绑辨证/P1a),旋钮表却带 5%/30% 数值 ⇒ 未来双重乘算风险。
另:`AC-29-05` 的 0/9/19/38/57 与 `skill-system.md:552`「60 级损 3 点」**互洽**(良性冗余,缺同源断言)。

---

## 4. 主会话否掉/更正的子代理结论(供判断可靠性)

1. **G1-W-5「51 侧 AC-51-B9 是借来的红」——不成立**:`telemetry-analytics.md:689` 的「10 的 GDD(尚无)」是
   OQ 表内陈旧短语,而 AC-51-B9(`:573`)**已写成「落地后」条件式**,不判红 ⇒ 降为 ℹ️ 回刷。
2. **G3-W-10「`OQ-27-3` 三处一致宣布待数值轮」——不准确**:`enemy-ai.md:1494` 已「✅ 2026-09-17 解除与
   `O-27-3` 耦合」,只有「与 1 的速度同批核」仍待数值轮。

## 5. 建议处置次序

| # | 处置 | 性质 | 需拍方向 | 状态 |
|---|---|---|---|---|
| 1 | R-4 的 `OQ-4-13`(MotorSuppressed lease) | 存量互斥 | **是(归 1)** | ✅ **已裁已落(2026-09-20,用户裁 per-source lease)**:1 侧轴 4 表重写(位图 + OR 聚合 + `Acquire/Release(LeaseSource)`,`LeaseSource` 闭集 `Self`/`Emergency`/`Combat` append-only 禁重排;29 不扩权)+ `AC-1-23` 注块改「已裁」+ §Interactions 4/10 两行调用面同步;4 侧 `OQ-4-13` 行转 ✅(`AC-4-19` 转可运行,载体未建前 `NOT-RUN`);索引 row 1 转 Approved |
| 2 | R-3 的「唯一真环」裁定(改索引措辞 vs 37 开成长触发) | 索引 vs AC 互斥 | **是** | ✅ **已裁已落(2026-09-20,用户裁「37 开成长触发」)**:调和形 = **37 的流事件(`CaseClosed`/`PatternRecognized`)作 30 同源图样类新颖度的派生数据源,30 只读病例流**(与 51 同构,承 ADR-019 §三);**`EmitGrowth` 调用方仍是 8,P0 不变,`AC-37-32` 不松动**。三处同步:索引 §7 裁定注 · `case-system.md` 出向 30 行 + §环与单向化行 · `skill-system.md` §6 37 行;索引 row 37 转 Approved |
| 3 | R-1 甲/乙/丙(补内容 / 宣告无剧情 / 最小修) | 内容债 + 悬空指针 | **是(甲/乙/丙)** | ✅ **已裁已落(2026-09-20,用户裁甲 = 补充完整剧情内容;剧情骨架由用户口述,评审侧扩展)**:新件 `design/gdd/content/he-returns.md`(序幕听诊器 / 一·帐篷 / 二·手册 / 三·第一课 / 终幕;§四 成员显式点名 甲教书先生·乙挑夫·丙守夜老人,均 `INJ_HEMORRHAGE`;§五 第二幕复诊逐条归 52/37/8/11/39/53;§七 红线判据)。回刷四件:`case-system.md` `ScriptedChain` 指针改指该件 + 残留注闭合 · `random-events.md` 规则十一后注(52 侧四项脚本条目:三案之「来」×3 + 甲复诊)+ OQ 行转部分已定 · 索引 §11 内容债勾 `[x]`(残留:`patient_id` 绑定值归 7a/ADR-014;史实断言交 40)。**2026-09-20 二次修订(用户裁「新版正典,老版让位」+ Opus 裁决核)**:剧情正典升格为 `content/campaign-arc.md`(P0→P2 八章总谱;「幕」专留《他回来了》两幕,红线句已加限定词);`he-returns.md` §二/§三 让位、§四~§十 机制章继续有效(本行四处下游账不悬空)。新增承重项 **W-1**(三案纯伤情 ⇒ 按 `AC-37-12` 可能不入 F-37.1 ⇒ `PatternRecognized` 可能永不触发)—— 用户**显式风险接受**,复诊走 52 脚本条目不受影响,实施退路(三案挂同一 `DIS_*`)登记于 `case-system.md` W-1 注块。同批范围裁定:P0 允许帐篷一次新建(`game-concept.md:697` 注;⚠️ 2026-09-20 一致性检查轮**追加口径**:用户澄清「P0 只作为测试,也要能新建;终局可像《英灵神殿》做世界建设」⇒ 23 规则六已补帐篷例外 + 方向注)**· 止血草进 17 闭集(`foraging.md` `AC-17-01b` 不记绿至 21a 行落)· 草木灰进 11 药集(`processing.md` 烧焦边择案 · 9/11 行归实现轮)|
| 4 | R-2 三处回刷(3/44 的 Amendment B · 24 的 D-21-31 · OQ-25-1 口径) | 纯回刷 | 否 | ✅ **已完成(2026-09-20)** |
| 5 | R-3 玩家致死通路:9 侧补 `SelfLimited` 落点;29/25 AC 挂 `BLOCKED-BY-9` | 落地件 | 否(归 9 实现轮) | 🔶 **已标 29 侧 8 条 + 25 侧 AC-25-6-08**;9 侧落点**未动**,归 9 的 GDD 轮 |
| 6 | R-5 的 AC 统一改标 `BLOCKED-BY-OQ-8 / OQ-25-8` | 纪律回填 | 否 | ✅ **已完成(2026-09-20)**:9 文件 11 处(见下) |
| 7 | R-6 的 42/48/17/23/24/52 方向问题 | 设计裁决 | 各归其 OQ | — |

## Phase 4:跨系统场景走查 — **未覆盖**

走查组(4a–4c)尚未回投;报告时此节留位,回投后追加,不重写整份。3–5 个多系统时刻(出诊链 / 急救判定链 /
死亡重链 / 布局重排链 / 三案链)的逐步走查将在该节完成。

> **回刷完成注(2026-09-20,处置次序 4/5/6 项)**:处置 4(R-2 回刷)与 6(R-5 纪律回填)已执行完毕;
> 处置 5 的 29/25 侧 AC 已挂 `BLOCKED-BY-9`,9 侧 `SelfLimited` 落点归 9 的 GDD 轮。
> **R-5 落点清单(9 文件 11 处)**:`emergency-procedures.md`(`JITTER_MAX` 旋钮)、`diagnosis-system.md`
> (AC-8-20 / AC-8-26)、`enemy-ai.md`(AC-27-21 / AC-27-35)、`random-events.md`(AC-52-14 / AC-52-44 /
> `ROLL_INTERVAL` 旋钮)、`skill-system.md`(`NOVELTY_COOLDOWN` 旋钮)、`input-system.md`
> (`DWELL_DURATION` 旋钮)、`time-and-weather.md`(`WEATHER_BLOCK_TICKS` 旋钮)、`combat-and-weapon-lines.md`
> (`cooldown_ticks` / `trauma_half_life` 旋钮)、`disease-simulation.md`(`TICK_SECONDS` 旋钮)。
> 处置 1/2/3/7(需方向裁定)维持「待用户」未动。

## 附:各组原始清单(已核,供回溯)

- G1(2a/2b/2c/2f)🔴3(B-1 AM-B 传播 · B-2 OQ-25-1 口径 · B-3 MotorSuppressed)/ ⚠️6 / ℹ️4 + 未覆盖 3。
- G2(2d/2e)🔴0 / ⚠️3(W-1 EnvMod 双钳 · W-2 24→9 恢复通道无求值点 · W-3 dose 界不含 10)/ ℹ️3 + 未覆盖。
- G3(3a–3g)🔴4(B-1 真环 · B-2 死亡通路 · B-3 支柱四单腿两旋钮 · B-4 耗材无来源)/ ⚠️11 / ℹ️3 + 未覆盖 6。
- 核实为真但属**已登记 D-项**(不重复立案):D-21-29 / D-21-32(AC-18-19 不得记绿)/ D-21-33 / D-21-34 /
  D-9-B / `OQ-20-7`(CarryLoad 双口径)/ `OQ-29-5`(DEATH_COOLDOWN 未定)/ `O-11→10` + `OQ-11-12`
  (single_dose_max 不含 10)。
