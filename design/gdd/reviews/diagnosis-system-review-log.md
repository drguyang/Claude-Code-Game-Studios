# 评审记录 · 系统 8《诊断与体征揭示》

| 字段 | 值 |
| ---- | ---- |
| 目标文档 | `design/gdd/diagnosis-system.md`(1609 行 · 8 节齐全 + Visual/Audio · UI · Open Questions) |
| 评审日期 | 2026-09-14 |
| 评审轮次 | **首轮**(`design/gdd/reviews/` 此前不存在) |
| 模式 | `/design-review` **full**(六名专家并行 + 高阶综合) |
| **裁决** | **NEEDS REVISION** |
| Scope Signal | **L** —— 不推翻任何支柱/边界,但 12 项 blocking 横跨数据列、公式钳位、护栏、联机归属、音频实现,连带 9/30/42/44 四份改动 |

## 参评专家

`game-designer`(玩法/幻想)· `systems-designer`(公式/数值)· `qa-lead`(50 条 AC)· `ux-designer`(交互/UI)· `network-programmer`(确定性/联机)· `audio-director`(音频)· `creative-director`(高阶综合)

## 完整度

**8/8 节齐全**,另有 Visual/Audio、UI Requirements、Open Questions 三节超额。
**缺陷**:`:30` 仍写「⚠️ 本文件目前只有骨架」,与顶部 Status「11/12 全节完成」自相矛盾。

## 依赖图

- ✓ `disease-simulation.md`(9)· `skill-system.md`(30)—— 两条上游边闭合
- ✗ `prescription-system.md`(11)· `case-system.md`(37)· 脉案(39)· 拟物UI(42)· 音频(44)· 医疗后果(53)—— **六份下游 GDD 全部不存在**(设计顺序上属预期),8 有约 15 条 AC 因此无落点

## 必改(blocking)

| # | 发现 | 来源 |
| --- | --- | --- |
| 1 | `:30`「只有骨架」与 Status「全节完成」并存 —— 删其中一句 | 主评审 |
| 2 | **R-8.2 五组体征(姿态/呼吸/触感/病史/阴性)没有 `tier_named` 列**,而 F-8.1 承重式、AC-8-11、AC-8-32 全依赖它 ⇒ 公式依赖的数据列不存在 | qa-lead M1 / 主评审⑤ |
| 3 | **F-8.2 `TierIndex` 在 `Skill<10` 与 `Skill≥50` 无定义**(4 槽 vs 5 区间)⇒ Lv0–9 与 Lv50–60 整段无词表输出,而这两段正是「粗档」与「满档」本体;`tier_named=Lv1` 不落在 `DIAG_TIERS` 是其症状 | systems-designer |
| 4 | **`NEG_WEIGHT_j > 0` 未入约束族**(取 0 ⇒ `L*_j` 为空集,C-3 抓不到);F-8.3 对「阳性存在但 `Sign_j<READ_FLOOR`」未定义(R-8.1 说 `neg_weight` 阳性留空) | systems-designer |
| 5 | **F-8.6 缺两道护栏**:`float` 宽度钉死 + 禁 FMA 收缩依赖(首选预计算定表);缺**跨平台逐位一致 AC**(现 AC-8-1 只测同平台双跑,可全绿而联机分叉) | network-programmer |
| 6 | 「8 不持状态」与「旧」态/`RECHECK_WINDOW` 所需三时刻矛盾 —— 声明住 39,修正 §Interactions 入向表与 AC-8-3 | network-programmer |
| 7 | **`EmitGrowth` 加权威子句(主机唯一)** —— 否则客户端本地跑 8 可自刷诊断等级 | network-programmer |
| 8 | `:930`「显示词条,同时标『把握不足』」删「标」字,与 V-8.3/UI-8.4 对齐 | 主评审③ |
| 9 | `tier_named` 取自 30 的 `DIAG_TIERS` ⇒ **未登记的双向耦合**,登记之(30 改档值击穿 8 的构建校验与 AC-8-11) | systems-designer |
| 10 | **AC-8-17 与 AC-8-22 正面冲突** —— 照 AC-8-17 实现 AC-8-22 必挂,改写 AC-8-17 | qa-lead M2 |
| 11 | **联机脉案归属未设计**(每人一本 vs 共享)⇒ 规则五在联机下被击穿 —— **待用户裁定** | game-designer M7 |
| 12 | **V-8.7 三处**:联机分混(单 AudioListener 不可实现)· 语声「运行时调制」降格为变体库混合 · 把脉音触发条件医学错误(普通人躺下听不到自己心音) | audio-director |

## 推荐修改

- AC-8-43(零图鉴/零数值)· AC-8-44(手柄焦点导航)· AC-8-23/24 从 ADVISORY **升 BLOCKING** —— 拟物无血条支柱与技术偏好硬约束的唯一守门
- 静态守门 grep 改类型引用级断言 + Roslyn;守门清单补 `Mathf.*` 全族;AC-8-19/26 去像素级判据
- 删 `Publish` 措辞,统一 `EmitGrowth`/`VoteEvent`;常数表统一 `1/2` 语域
- §Overview 与 §Player Fantasy 锚点二的「Lv1–9 只有基础体征」旧口径清干净
- **跨文档**:`skill-system.md:430/457`「体征**层解锁**等级」回改(裁定⑨后层=精度档)
- 阻塞登记表拆两张(D-x 债务 / 未撰写文档),CI 输显式 skip 计数

## 锦上添花

`Project` 三重载改名 · AC-8-3 漏列 tick/事件输入 · `Project(σ)` 量纲说明 · F-8.2 `:812`「未查/无异常」措辞与四态打架 · 手写变体素材量(≈900–1200 张)计入内容债 · 啰音应为相位锁定瞬态簇而非 loop

## 专家分歧与裁决

| 争点 | 各方 | creative-director 裁决 |
| --- | --- | --- |
| clamp 是否破坏 AC-8-F1 | 主评审设问 vs systems-designer | **systems-designer 对** —— clamp 单调、等价式成立;真洞是 `NEG_WEIGHT=0` 造成空集 |
| `L*_j ≥ tier_named_j` 无约束 | 主评审=blocking vs game-designer=规格不自洽 | **game-designer 大体对**(严重度偏低);但 qa-lead 的「`tier_named` 列不存在」使其**换性质为 blocking** |
| 8 出浮点是否破坏 ADR-005 | systems-designer=跨平台逐位一致 vs network-programmer=FMA 分叉 | **双方各对一半** —— 设计上**不违反**,规格上**未证明**。修护栏,不改规则 |
| V-8.7 是亮点还是违宪 | 文档自述亮点 vs audio-director=内核违宪 | **audio-director 基本对**,但「掩蔽而非门控」这个核**对且符合裁定⑨** —— 只改三处实现层 |

## 必须由用户裁定

1. **联机脉案归属** —— 每人一本 vs 共享一本(gates blocking #11)
2. **音频联机精度分混做不了时的降格** —— 「音频精度=主机技能」vs「音频不分档」
3. **自限病下规则七反馈哑 + 三味药 ⇒ 试药枚举可绕过承重墙** —— 加可见信号 vs 接受并定价
4. **锚点一承诺(检出)与机制真实交付(鉴别)对齐** —— 改文案 vs 加机制
5. **旗舰示例的共病确定性** —— 「风湿热→心衰」按概率/种子显式化 vs 人手一例
6. **《他回来了》手写病名在手柄/VR 的输入通路** —— 降级为已见列表 vs 加手写通道

## 成熟度判断

> **地基合格但自称封顶的草稿。** 规则五、不泄漏、四法出生自带三处承重浇得很好,但「11/12 完成」言过其实 —— 现在是**可裁决**的设计,不是**可实现**的规格。

## 处理

用户裁定(2026-09-14):**全修 blocking 12 项** · 本记录落盘 · 同步 `systems-index` §10/§11。

## 修订落盘(2026-09-14,同日)

**12 项 blocking 已全部兑现 + 六项用户裁定已并入正文。** 逐项落点:

| # | 落点 |
| --- | --- |
| 1 | 删 `:30`「只有骨架」段,改为复核结论块 |
| 2 | R-8.2 **五组全部补 `tier_named`** —— 姿态组新增组头注释 + 逐行覆盖(7 行);呼吸组 5 行;触感组 7 行;病史组组头;阴性组新增 `tier_named` **列**(4 行,全 Lv20) |
| 3 | **F-8.2 重写为计数式** `TierIndex = #{b ∈ SLOT_BOUNDS : b ≤ Skill}` —— 对全体 `Skill` 有定义;`tier_named` 取值域明定为 `{1} ∪ SLOT_BOUNDS = {1,10,20,50}`;`35` 明令不得用作 `tier_named` |
| 4 | **C-6**(`NEG_WEIGHT_j > 0`,构建期硬失败)+ **`NEG_WEIGHT_FALLBACK`**(阳性体征读不出时的兜底权重,公式常量非词表字段)+ F-8.3 修补 (a)(b)(c);`AC-8-F4` 扩为阳性 / 阴性各测一遍 |
| 5 | **G-4**(位宽钉死 `System.Single` + 禁 FMA 收缩依赖)+ **AC-8-F5**(跨平台逐位一致);`AC-8-6` 扩为三查 (a)(b)(c) |
| 6 | §Interactions 入向表补 **9 的 `ITickProvider`** 与 **39 的时刻查询**两行 + 矛盾裁决块(规则在 8 · 值在 39);`AC-8-3` 扩为四类合法输入并断言无 tick 持久字段 |
| 7 | **铁律④ `EmitGrowth` 主机唯一** —— 由 `IIdAuthority` 门控,客户端只呈现不发起 |
| 8 | `:930` 行改写为「绝不加任何标注 / 徽章 / 颜色」;`AC-8-13` / UC-8-F1 表同步(「把握不足」只由墨色 / 笔迹承载) |
| 9 | **D-8-9** 登记(30 ↔ 8 双向耦合)+ **C-8**(`SLOT_BOUNDS ⊂ DIAG_TIERS` 构建期断言)+ Tuning Knobs `SLOT_BOUNDS` 行 |
| 10 | **AC-8-17 重写** —— 与 AC-8-22 的分工明定为「真值 vs 形态」,判据改为「客观阳性体征永不产出 `polarity = 阴性` 的**证据**」 |
| 11 | **铁律⑤:每人一本** + **D-8-10**(39 按 `player_id` 分册)+ Edge Cases ⑤ 展开 |
| 12 | **V-8.7 四处** —— ② 降格为变体库混合;③ 新增需求④(联机精度取主机技能);V-8.5 把脉音触发条件改为「安静 · 屏息 · 侧卧压耳」;D-8-8 / OQ-8-4 同步 |

**六项用户裁定的落点**:①→铁律⑤ + D-8-10 · ②→V-8.7 需求④ · ③→Edge Cases ⑥ + D-8-11 ·
④→D-8-6 提级为 P0 阻塞(合并规则本身归 9,8 侧只改债务等级与 AC-8-34 注)·
⑤→§Player Fantasy 锚点一 + `skill-system.md` §2 锚点一(只改文案,不动机制)·
⑥→§UI-8.4 边界条款 + 规则六注(病名册三条判据)。

**连带修正(复核推荐项中已做的部分):**
- `AC-8-23` / `AC-8-24` / `AC-8-43` / `AC-8-44` 由 ADVISORY **升 BLOCKING**
- `C-7` 把「`L*_j ≥ tier_named_j`」从散文断言升为**可检验约束**
- 铁律 / 硬需求 / 约束族的**计数口径**统一(三条边界铁律→五条 · 三条硬需求→四条 · C-1…C-5→C-1…C-8)
- **跨文档**:`skill-system.md` `:32`(锚点一文案)· `:430`(`DIAG_TIERS` 描述)· `:457`(AC 措辞)三处回改

**未做(留待后续):**
- 「静态守门 grep 改类型引用级断言 + Roslyn」「`Mathf.*` 全族」「`Project` 三重载改名」「`Project(σ)` 量纲」「常数表 `1/2` 语域统一」—— 属**锦上添花**层,未在本轮
- 六份下游 GDD(11 / 37 / 39 / 42 / 44 / #53)**仍不存在** —— 8 的 D-8-3 / D-8-8 / D-8-10 / D-8-11 待它们落盘时回收
- **数值仍全部 `*待裁*`** —— 依硬约束「数值用户自己调」,本轮**未动任何数值**

**结论:8 由「可裁决的设计」进为「可实现的规格」**(在 D-8-4 / D-8-7 两项硬前置还清后)。
**建议下一步**:9 的复核(须在 8 之后)· 并优先还清 D-8-6(已提级 P0 阻塞)。
