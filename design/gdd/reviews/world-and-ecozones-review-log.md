# 世界与生态区 (World & Ecozones, #6) — Review Log

## Review — 2026-09-16 — Verdict: MAJOR REVISION NEEDED(首轮 `/design-review`)→ **Approved**(修订后用户裁定)

Scope signal: **L**(单系统,但它同时是 1 / 17 / 23 / 24 / 27 / 36 / 52 的**共同上游** ——
本轮的根因有 4 条落在**跨系统缝**上,而非 6 自身)
Specialists: game-designer · level-designer · systems-designer · unity-specialist · qa-lead ·
network-programmer · narrative-director · accessibility-specialist(均 **haiku 并行**)
→ 收束:**creative-director**(**Opus,串行单发**)
Blocking items: **8 条根因**(41 条发现归并)| Recommended: 见根因表 | Prior verdict: 首轮 —— 本文件此前不存在

Summary(creative-director 收束):**「所有权写得很精确,但没有一条拥有 `X ⊕ Y`」**。
本的边界裁决 —— **POI 定义 = 派生态 / 状态 = 模拟态进世界流 / 写者 = 6**(ADR-021)、
**逻辑整数层 + 视觉层两层世界**(ADR-015)—— 经跨领域**全部通过**,不需重做。
坏在**三条组合缝无人持有**、**判据多为不可判定的「负存在断言」**、
**一条公式自相矛盾**、以及**一处文档内自相矛盾**(R-6-14 vs 状态机表)。
⇒ **不按 41 条修 —— 按 8 个根因修**(与 1 / 3 的首轮同一纪律)。

### 根因诊断(本轮的承重结论)

| # | 根因 | 命中者 | 严重度 |
|---|------|--------|--------|
| **1** | **AC 判据是「负存在断言」** —— 「无第二坐标类型」「无浮点」「无兜底路径」**不可判定**(无法反射证明一个你没设想的类型不存在);且多处点名 **AST** 而 AST 不是 Unity 测试原语 | `qa-lead` + `unity-specialist`(**两方独立命中**) | 🔴 最高 |
| **2** | **三条组合缝无人拥有** —— ① 静态导航格 ⊕ 建造占用 ② 视觉边界 ↔ 整数多边形 ③ 导航格驻留模型;全是**静默**失败类 | `level-designer` + `systems-designer` + `unity-specialist` | 🔴 |
| **3** | **F-6-3 自相矛盾** —— 闭集语义对**共享边的两区同时成立** ⇒ 返回**两个** `ecozone_id`,而 `AC-6-20④`(单值)**不可达** | `systems-designer` + `qa-lead` | 🔴 |
| **4** | **关卡工具无系统归属** —— 6 的**全部输入**由一件「关卡工具」手工产出,而它无系统行 / 无 ADR / 无 GDD | `level-designer` + `producer` 视角 | 🔴 |
| **5** | **文档内自相矛盾** —— 状态机表把 **27 敌人 AI** 列为 T2 触发方,而 **R-6-14 明写 27 不拥有 POI** | `game-designer` | 🔴 |
| **6** | **F-6-1 在边界值下真空** —— `SPEED_MAX = 0` / `MAX_DT = 0` 时**任何 `LATTICE_SIZE` 都通过**;且「取等号 = 零余量」是**错的** | `systems-designer` + `qa-lead` | 🔴 |
| **7** | **A-6-1 在接口上物理不可能** —— 44 的 `AudioCueDto` **无 `terrain_id` 字段**,而 44 规则六明写「不拥有素材语义选择」⇒「同源」无所依托 | `audio-director` + `unity-specialist` | 🔴 |
| **8** | **发现门(反幻想第 2 条)评级过低** —— `AC-6-23` 是 **ADVISORY**,而它是**数据泄露**(清单化世界),不是美术走查项 | `narrative-director` + `game-designer` | 🔴 |

### 用户裁定(2026-09-16,三项;经一次多标签问答)

| # | 议题 | 裁定 |
|---|------|------|
| ① | 下一步 | **现在就修订(全 8 条根因)** —— 本会话内逐项修订落盘,不推迟 |
| ② | **RB-2 幻想分层** | **分两层:P0 观察面,推断层推 P1a** —— 6 只交付 **P0 观察面**;地理推断层(地理 → 发病率因果模型 / 玩家推断记录 / 可读反证)归 **8 诊断 + 37 病例 + 34 公卫**,在 **P1a** |
| ③ | **关卡工具** | **另开 ADR 立新系统**(**ADR-022**)—— **不**并入既有系统行 |

### Blocking Items → 处置表(2026-09-16 当日完成)

| # | 根因 | 处置 | 落点 |
|----|------|------|------|
| 1 | **负存在断言不可判定** | 全部改为**正面判据**:`AC-6-01` 改**正面白名单**(类型集合 ⊆ 整数域)· `AC-6-03` 改 **IL 扫描边界程序集 public API** · `AC-6-07` 改**值级随动夹具 + Roslyn 源头白名单** · `AC-6-10` 改**可访问性 + 运行期计数** · `AC-6-18` **删调用栈子句**保留类型级 · `AC-6-21③` 改**结构穷尽性** · `AC-6-22` 改 **IL 扫描零浮点指令** | A 组 / B 组 / C 组各条 |
| 2 | **三条组合缝无人拥有** | **新立 R-6-15**:缝 1 合成 = **23**(`EffectiveWalkable`)· 缝 2 边界**一次定义两处派生** · 缝 3 导航格驻留表态(chunk 切片)· 并声明 chunk 键 = ADR-015 §四 | 新 **`O-6-10` / `O-6-11` / `O-6-12`** |
| 3 | **F-6-3 自相矛盾** | **F-6-3 全文重写**为**开集语义 + 正则化**:`Interior(e)`(严格,边界不计)⇒ 无命中则 `min(boundary_hits).id`;⇒ **单值由裁决序保证**,不再依赖「恰好一个多边形含它」。补实现约束:**横轴不是 y**(`[z0,z1)` 半开)· **int64 溢出**(ADR-012 F7)· **退化多边形**(零面积 / 重合顶点)入烘焙期失败集 · `NONE = -1` 哨兵 | F-6-3 · `AC-6-19`(+④⑤)· `AC-6-20` |
| 4 | **关卡工具无系统归属** | **新立 R-6-16** + 用户裁定**另开 ADR-022** ⇒ 登记 **`O-6-9`**;并声明:ADR-022 落盘前 P0 单区内容的产出路径**在架构上无主**(P0 内容开工硬前置)。**✅ 同日结清:ADR-022 已落盘**(Tooling 层 · 行 54 · §三 导出契约 · §四 C1–C6) | R-6-16 · `O-6-9` · §Cross-References「待建 ADR」 |
| 5 | **27 被误列为触发方** | **触发方白名单冻结为 `{4, 25, 37}`**(**27 移出** —— 它读导航格,不读也不写 POI 状态);最终归属 = `OQ-6-1`;实现期先按冻结集写判据,裁定后改的是**白名单常量**不是状态机 | §States「合法转移」表 + 白名单注 |
| 6 | **F-6-1 边界真空** | 补**前置守卫**:`LATTICE_SIZE > 0 ∧ SPEED_MAX > 0 ∧ MAX_DT > 0 ∧ SAFETY_MARGIN > 1`(**消解真空**);补 `SPEED_MAX = SPEED_MODE_MAX × K_TERRAIN_MAX × K_CONTEXT_MAX` 展开;**订正**「取等号 = 零余量」为假(零余量点是 `SAFETY_MARGIN = 1`) | F-6-1 · `AC-6-04`(a/b 拆分)· `AC-6-05` |
| 7 | **A-6-1 不可实现** | **归属链修正**:6 提供 `terrain_id` 表 → **1 的表现层**(它有 UnityEngine 访问权)解析 `terrain_id → CueId` → 44 只播。⇒ 登记 **`O-6-14`**;**A-6-4 档位订正 P1a → P1b**;**新增 A-6-5**(POI 环境声 = **接近度的函数,永不是 `PoiState` 的函数**) | §Visual/Audio 二 · `O-6-14` |
| 8 | **发现门评级过低** | **`AC-6-23` 升格 ADVISORY → BLOCKING**(数据泄露 = 与 `AC-37-15` / `AC-42-D1` 同级);判据 = `PresentationDtoGuard` **递归**反射扫描 | `AC-6-23` · 计数节 |

### 因根因 2 / 3 派生的口径订正(跨文件,本轮一并落)

1. **ADR-021 落 Amendment H** —— §④ 有界性由 `|POI| × |STATE|` 收紧到 **`2 × |POI_DEF|`**
   (`§Validation Criteria:259` 就地改);Amendment 声明:若 `OQ-6-7` 判负则本 Amendment 与
   F-6-4 / `AC-6-15` **同时作废**,须另开 ADR。
2. **`architecture.yaml` 补 `ActorCellEntered`** —— 世界流 interface 列表 + `IEventSink` 路由白名单
   (此前只在 `IPlayerMotor` 契约出现 ⇒ **与 `CompoundTriggered` 同型的失败模式**:引用却无登记)。
3. **新登记 6 条义务 `O-6-9…O-6-14`**(关卡工具 / 23 占用合成 / 视觉边界同源 / 导航格驻留 /
   四区名史实 / terrain_id 解析权)。
4. **四区名声明架空** —— `租界城镇 / 贫民窟 / 战乱前线 / 深山疫区` 按
   `historical-anchors.md:141-146` 的**第二条出路**处理:**显式声明架空**,不锚定年份 / 地点;
   具体考据归 **40 史实资料库**(`O-6-13`)。
5. **通用口径收窄** —— 「装载期断言在空值上恒红」的旧口径**拆为两半**:
   (a) 校验器存在性 + 违反夹具(✅ 今天可测)vs (b) 真值存在性(`AC-6-25` EXTERNAL);
   并订正 `AC-6-07`(它是**派生关系**断言,与取值无关,不受该口径影响);
   补 **`Debug.Assert` 在非 Dev 构建被剥离** ⇒ 硬失败须显式 `throw`。
6. **覆盖缺口补锚** —— EC-6-3 / EC-6-5 / EC-6-11 / EC-6-16 / F-6-3 y 轴五条**有约束无 AC** ⇒
   逐条锚到既有 AC(不新增编号,避免膨胀)。

### 计数(修订后)

**27 条** = **24 BLOCKING**(`AC-6-01…22` + **`AC-6-23`(升格)** + **`AC-6-26a`(新增)**)
+ **1 ADVISORY**(`AC-6-24`)+ **2 EXTERNAL**(`AC-6-25` / `26`)。
登记义务 **`O-6-1…14`(14 条)** · 待裁 **`OQ-6-1…9`(9 条)**。

> ⚠️ **旧口径「26 条 = 22 BLOCKING + 2 ADVISORY + 2 EXTERNAL / `O-6-1…8` / `OQ-6-1…7`」已作废。**

### 未结项(不阻塞本轮收尾)

- 🔴 **`OQ-6-1` / `O-6-7`(POI「已发现」的触发方)** —— ADR-021 定了**写者** = 6,未定**触发方**;
  6 刻意不裁(越权)。**P0 只有一个生态区 ⇒ 不阻塞 P0 骨架,但 P0 内容开工前硬前置**
  (后果 = POI 状态永不推进)。若判负须**另开 ADR**。
  ⭑ **2026-09-21 结案**:用户**批准 4 的承接方案**(触发 = 玩家主动交互 · 自报方 `{4,25,37}` ·
  6 仍是唯一写者)⇒ 硬前置解除,**判负路径未触发,无需另开 ADR**;回刷见 `world-and-ecozones.md`。
- 🔴 **`O-6-9`(ADR-022 关卡工具)** —— ✅ **已结清(2026-09-16 当日)**:
  **ADR-022 已落盘**(`docs/architecture/adr-022-level-tool.md`),关卡工具立为
  **Tooling 层正式系统**(`systems-index.md` 行 54,不计入 P0 的 31)。详见下方「同日收尾」。
- 🔴 **`OQ-6-8`(导航格驻留)** —— ✅ **已裁(2026-09-16 当日)**:判**分块流式**
  (chunk 切片,`world_nav_{chunk}.json`);未驻留 chunk 视为全 `block` ⇒ `EcozoneOf`
  **不得假设全图可达**。ADR-015 §四 补注 + ADR-022 §五 / C6 双落。⚠️ 内存实测归实现期。
- **`OQ-6-9`(生态区垂直分层)** —— P1b 关卡设计前;若判需分层须回改 F-6-3。
- **`O-6-13`(四区名考据)** —— 归 40 史实资料库(P1a)。
- **`O-6-10`(缝一 合成点)** —— 归 23 模块化建造的 GDD(尚未撰写)。
- **`O-6-14` 解析方** —— **产出方已由 ADR-022 §三 登记**;「谁把 `terrain_id` 解析成 `CueId`」
  仍待 1 / 44 的 GDD 承接。
- **第二轮 `/design-review`** —— **✅ 2026-09-16 用户裁定:不做第二轮,直接 Approved**。
  (同样的裁定先例:系统 1 / 2 / 3 均为「首轮 + 修订落盘 → 用户裁定接受不再重评审」。)
  ⚠️ 若将来因实现期发现新问题而重开评审,仍须**新会话**(`design-review` 与 `design-system` 不得同会话)。

## 收尾判定 — 2026-09-16 — Verdict: **Approved**(用户裁定)

首轮 **MAJOR REVISION NEEDED**(8 条根因 / 41 条发现归并)→ **8 条根因当日全部修订落盘**
→ **用户裁定不做第二轮,接受修订**。⇒ 本系统状态 **`Approved`**(2026-09-16)。

> **裁定依据(用户 2026-09-16)**:与系统 1 / 2 / 3 同一处置 —— 评审的价值在「根因是否被真正修掉」,
> 而不在「再跑一遍」。8 条根因的处置表已逐条列出**落点**(见上),且其中 4 条(②③⑥⑧)
> 已由后置的一致性核对 / TR 实测独立验证;第 4 条根因由 **ADR-022** 独立成件闭合。
> **不做第二轮 = 接受**「本 GDD 的判据已由评审方与被评审方共同过一遍」这一事实,**不等于**
> 「判据已实现」—— `AC-6-*` 的签署仍须实现期实测(见 file header 的 `Last Verified` 待回填项)。

**Approved 后的硬前置门(必须活着带走,不得静默丢失)**:

| 门 | 内容 | 何时必裁 | 判负的后果 / 出路 |
|----|------|---------|------------------|
| **`OQ-6-1` / `O-6-7`** ~~🔴~~ ⭑ **已裁 2026-09-21(批准 4 方案)** | **POI「已发现」的触发方归谁** —— ADR-021 定了**写者** = 6,**未定触发方**;6 刻意不裁(越权) | ~~**P0 内容开工前**~~ ✅ 已按期裁定 | ~~无人自报 ⇒ POI 状态**永不推进**,状态机成死代码;须**另开 ADR**,不得在 GDD 内打补丁~~ —— 判负路径未触发;回刷见 `world-and-ecozones.md` |
| **`O-6-10`** | **静态导航格 ⊕ 建造占用 → `EffectiveWalkable` 的唯一合成点** | 23 的 GDD 撰写时 | 两个答案 ⇒ 玩家建了墙 AI 仍穿墙;或双方各自合成 ⇒ 非确定 |
| **`O-6-14` 解析方** | `terrain_id → CueId` 的解析权(产出方已由 ADR-022 §三 登记) | 1 / 44 的 GDD 撰写时 | 6 的 `TERRAIN_TABLE` 交付了却无人消费其音频面 |
| **`O-6-13`** | 四区名(租界城镇 / 贫民窟 / 战乱前线 / 深山疫区)的史实核验 | 40 撰写时(P1a) | 违反支柱五;**医学 / 历史受众第一个出戏点** |
| **`OQ-6-8`** ⚠️ | 导航格分块流式的**内存实测**(判据已定,实测未做) | 实现期第一个寻路之前 | 若实测超预算 ⇒ 回改 `ADR-022 §五` 的分片粒度(非另开 ADR) |

**已由 ADR-022 当日结清(不再是门)**:`O-6-9`(关卡工具立系统)· `O-6-11`(C5 边界单一源)·
`O-6-12` 工具侧(C6 切片)+ ADR-015 §四 / ADR-014 §五 两处补注 · `O-6-14` **产出方**。


### 同日收尾 —— ADR-022 落盘(2026-09-16)

用户裁定 ③ 的执行:**ADR-022「关卡工具(内容产出的正式系统 · 两层世界单一版权威)」已落盘**。

| 项 | 内容 |
|----|------|
| **系统归属** | 关卡工具 = **Tooling 层正式系统**(编辑期 / 构建期,**不运行期**)—— **新类别**,`systems-index.md` **行 54**;**不计入 P0 的 31 项** |
| **程序集** | `tools/level/`(UnityEngine / UnityEditor 引用自由 —— **门 A 不约束**,因其**不进构建**) |
| **唯一作者** | 两层世界的**逻辑层全部整数数据**只能由本工具导出;第三方(GAIA / MapMagic 2)**只进视觉层**(承 ADR-015 §六) |
| **导出契约** | `assets/data/world_{geometry,ecozones,poi,resources,buildslots,nav_{chunk},terrain}.json` → ADR-014 两阶段烘焙 → `world_*.cooked` |
| **六项一致性检查** | **C1** 两层漂移(告警)· **C2** 可走性同源(**硬失败**)· **C3** 导航格↔NavMesh(告警)· **C4** 多边形合法性(**硬失败**)· **C5** 边界单一源(**硬失败**)· **C6** 切片完整性(**硬失败**) |
| **结清** | `O-6-9`(立系统)· `O-6-11`(C5)· `O-6-12` 工具侧(C6)· `O-6-14` **产出方**(§三)· `OQ-6-8`(§五) |
| **悬空判据落地** | `ADR-015 §Validation:317` / `ADR-016 §Validation:442` 的「两层 / 导航格一致性检查**在关卡工具中存在**」—— 此前**无执行点**(无物被指定为「关卡工具」),现指向 **C1–C6** |
| **涟漪** | `systems-index.md`(类别 + 行 54)· `tr-registry.yaml`(`TR-leveltool-001…008` · 221 → **229** 条)· `traceability-index.md`(§12 + 汇总)· `architecture.yaml`(`level_tool_authoring` 契约)· `adr-015 §四` 补注 · 本 GDD 的 `O-6-9` / `O-6-11` / `O-6-12` / `O-6-14` / `OQ-6-8` / `R-6-16` / §Cross-References 回填 |
| **残留** | ① `O-6-12` 的 **ADR-015 §四 就地修订**已退化为**一句补注**(已落);② `O-6-14` 的**解析方**(1 / 44 的 GDD)未承接 |

> **计数**:`TR` 由 **221 → 229**(+8,全 `covered`);实测累计 **135 covered / 19 partial / 75 gap**。
> **ADR-021 Amendment H** 的 `§Validation Criteria:284` **已实测为 `2 × |POI_DEF|`**
> (首轮评审时记的「`adr-021:259` 未改」= **当时**状态,现已闭合)。

### 评审方法说明

- 并行专家(8 名)**全部 haiku**;最高评级综合(creative-director)**单发 opus 串行** ——
  遵循项目纪律(`feedback-sequential-opus-reviews`:并行 Opus 会 429)。
- **承重发现逐条独立验证**后才接受 ——
  `ActorCellEntered` 白名单缺失(实测 `architecture.yaml` 世界流行 + 路由行均无)、
  `adr-021:259` 未改(实测)、`AudioCueDto` 无 `terrain_id`(实测 `audio-system.md:143` + `grep` 零命中)、
  四区名引用点(实测 `concept-benchmark.md:37`)。
- **两名专家的越权项已下调**:game-designer 主张「状态机不可独立测试」⇒ 实际可用**合成事件**独立测试,
  降级为 SHOULD(底层问题归 RB-2 / RB-3);level-designer 的两项重述为建议;
  network-programmer 的 F-10 多数已由 ADR-020 **Amendment G** 解决(残留 2 行未同步,即上述 `architecture.yaml` 订正)。
